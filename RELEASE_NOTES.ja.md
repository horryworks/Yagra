# リリースノート

<!-- ## Unreleased は、出荷済みだがまだバージョンの付いていない挙動変更の置き場です。
     Yagra は main への push ごとにテストサーバへデプロイされるため、変更はリリースのタグ付けより
     ずっと前に稼働に入ります。**変更を出荷するのと同じコミットで**ここに項目を足してください —
     レスポンスの形、ステータスコード、既定値、クエリの意味、削除した挙動など、運用者や API
     クライアントが気づきうるものすべてです。リリース時に `/docs` がこの節を新しい `## v<x>` に
     畳み込み、空の Unreleased を残します。空であるのが平常状態で、節自体が無い場合は誰かが
     この仕組みを消したということです。 -->

## Unreleased

### 破壊的変更
- **API トークンが所有アカウントとして動作するようになり、所有者を特定できないトークンは失効します。**
  これまでのトークンは独立した身分でした。`api_tokens` は自身のロールを持つだけで `users` への
  リンクが一切なく、発行したアカウントを削除・無効化・降格してもトークンには何の影響もありません
  でした。マイグレーション `0057` が `created_by` のユーザー名を手がかりに各トークンを所有者へ
  紐付けます。**発行元アカウントが既に存在しないトークンは紐付けできず、認証されなくなります**。
  一覧には「所有者なし」として表示されるので、意図して失効させてください。該当するトークンは、
  できればサービスアカウント(後述)を所有者にして再発行してください。
- **Top-N 系 4 エンドポイントの応答が、配列そのものからオブジェクトに変わりました。**
  `GET /api/v1/metrics/top`、`/metrics/interface-top`、`/metrics/interface-delta`、
  `/alerts/top-nodes` は `{"entries": [...], "partial": false}` を返します。従来ボディ全体だった
  ものが `entries` に入ります。`/metrics/interface-heatmap` は形を保ったまま同じ `partial`
  フィールドが増えます。`partial` は、そのランキングが呼び出し元アカウントに見えるグループだけを
  対象としており、本来見えるはずの行が欠けている可能性があるときに `true` になります — Top-N は
  グループを知らないメトリクスストアが順位付けするため、スコープ付きアカウントでは後段で絞り込む
  ことになり、結果が短くなりうるからです。可視範囲に制限のないアカウント（現時点では全アカウント）
  では常に `false` です。WebUI は追随済みですが、これらを読む外部クライアントは `entries` を
  読むよう変更が必要です。
- **`:latest` の意味が「開発トランク」から「最新の安定リリース」に変わりました。** これまでは
  `main` への push のたびに `ghcr.io/horryworks/yagra-*:<sha>` が公開されて `:latest` が動いていた
  ため、`docker-compose.deploy.yml` と `docker-compose.poller.yml` の既定値は開発ビルドを引いて
  いました。開発ビルドはもう一切公開されません — レジストリにあるのはリリースだけです。
  `:latest` は `-beta`/`-rc` の付かないリリースがタグ付けされたときに動き、`:<sha>` はリリース
  されたコミットにしか存在しません。`:latest` で `main` を追っていた場合、これからはリリースを
  追うことになります。別のものが必要なら明示的にタグを固定してください。なお `:latest` は
  「最も新しいバージョン」ではなく「最後に push された安定タグ」に付くので、大きなリリースの
  後に hotfix を切ると `:latest` は後退します。
- **`docker-compose.deploy.yml` に `YAGRA_IMAGE_REPO` が増えました**（既定 `ghcr.io/horryworks`）。
  公開リリースを使う限り設定不要です。未リリースのビルドを置いたプライベートレジストリを開発機
  から指すために存在します。

### 新機能
- **REST API が API トークンを受け付けるようになりました。** これまで `yat_…` トークンは `/mcp`
  専用で、REST API は `401` を返していたため、無人の自動化はパスワードを保持して毎回ログインする
  しかありませんでした。トークンは自身が使用できる**面(surface)**を持つようになり、`rest` を
  含むトークンはセッショントークンと同じように `/api/v1` で使えます
  (`Authorization: Bearer yat_…`)。既存のトークンは `mcp` のみを持つため、アップグレードによって
  AI クライアント向けに発行した資格情報が監視設定を変更できるものに変わることはありません。REST
  に届かせるのは、発行時の明示的な選択です。
  使用面によらず 2 つの制限があります。**ユーザー管理はできません**(自身の後継を発行できる資格情報
  は、元の失効を生き延びてしまうため)。また、サインイン中のアカウントを特定するエンドポイント
  (`GET /api/v1/auth/me`、個人ダッシュボード)は `403` を返します。
- **API トークンに所有者と有効期限が付きました。** 実効ロールは
  `min(トークンのロール, 所有者の現在のロール)` になるため、アカウントを降格するとそのトークンも
  同時に狭まります。アカウントを無効化・削除するとトークンは失効します。有効期限は任意です:
  `POST /api/v1/api-tokens` は `expires_at` を受け取り、省略すれば従来どおり無期限です(連携を
  動かすサービスアカウントには妥当な選択です)。Settings ▸ API tokens に所有者・使用面・有効期限、
  そして認証されない場合はその理由が表示されます。
- **サービスアカウント。** `POST /api/v1/users` が `kind: "service"` を受け取ります。パスワードを
  持たず、ローカルフォームでも SSO でもサインインできない機械アカウントです。API トークンを所有
  するためのもので、設定した担当者が異動しても連携が動き続け、無効化すればそのアカウントが所有する
  資格情報がまとめて停止します。同リクエストの `password` は任意になり、サービスアカウントに対して
  は**無視ではなく拒否**します。最後の管理者を守るロックアウト防止は、人間がサインインできる
  アカウントだけを数えるようになったため、サービスアカウントが唯一の管理者になることはありません。
- **`YAGRA_PAT_OIDC_IDLE_DAYS`**(既定 `30`)は、SSO でプロビジョニングされたアカウントが所有する
  API トークンが、所有者の沈黙をどれだけ生き延びるかを決めます。IdP 側でアカウントが無効化されても
  Yagra には通知されず(アカウント表は SSO ログイン**成功時**にしか更新されません)、所有者が
  サインインしなくなることが唯一の手がかりだからです。ローカル/サービスアカウントは対象外です。
- **URL 監視が資格情報を提示できるようになりました。** 新しい `http_auth` 資格情報の種別で
  Basic / Bearer / カスタムヘッダーに対応します。URL 監視に紐付けると、ポーラーがそれを提示して
  プローブします。資格情報は保存時にエンベロープ暗号化され、配信時にポールジョブへ埋め込まれます
  — SNMP 資格情報と同じ経路で、ポーラーは資格情報ストアを読みません。これまで作成できるのに
  消費する側が無かった `api_token` 種別は、Bearer トークンとして受理するようにしました。
  資格情報を提示する監視では TLS 検証が必須です（違反時は `400 credential_needs_tls`）。
- **ポーラーが停止時にノードを引き継がせます。** SIGTERM を受けたポーラーは `leaving` を立てた
  最後のハートビートを送るようになり、core は即座にそのポーラーをプールのハッシュリングから外して
  ノードを再割り当てします。従来は停止とネットワーク分断を区別できず、3 拍欠落（30 秒）を待って
  いました — しかも再起動がその窓に収まるとリングは一切変わらず、対象ノードは再起動の間ずっと
  ポーリングされませんでした。ローリングアップグレードが操作者の手を借りずに数秒で引き継ぎます。
- **監視ギャップに、失われた受動データが記録されます。** ギャップの行に、そのポーラーがバインド
  していた受動リスナー（`syslog:514`、`trap:162` など）を記録します。ポーリングしたメトリクスは
  再接続時にポーラーのバッファから埋め戻されますが、syslog・トラップ・フローエクスポートは
  埋め戻せません。イベントログの説明のつかない空白と、既知の欠損とを区別できるようになります。
  （SNMP インフォームは例外で、送信側が ACK を受け取るまで再送します。）
- **トップバーのグローバル検索が動くようになりました。** シェルの構築以来ずっと配置されていた
  ものの永久に無効で、コードには「検索エンドポイントがまだ無い」というコメントが残っていました。
  実際には `GET /api/v1/nodes/search` はノードピッカーの実装時から存在していました。ノードを
  デバウンス付きで検索し、矢印キーでの移動、`Ctrl`/`Cmd`+K と `/` でのフォーカス、Enter で
  ノードを開けます。検索対象がノードのみであることはポップオーバーに明記しています —
  アラート・イベント・グループにはサーバ側検索が無く、ノードだけの結果を全体検索のように
  見せるのは何も無いより悪いためです。モバイルではトップバー下に開く行として提供します。
- **Topology ▸ ジオマップが実際のページになりました。** これまで「Coming soon」のプレースホルダー
  でしたが、座標を持つノードグループごとにピンを打ち、そのグループの配下ノードの最悪状態で色分け
  して世界地図の上に表示します。ホイール/ドラッグ/ピンチでのパン・ズームと、ピンからそのグループ
  のノード一覧への遷移に対応します。ダッシュボードのジオマップウィジェットと同じグループ別
  ヘルス集計を読むので、同じ拠点が 2 つの画面で違う色になることはありません。地図の輪郭はアプリに
  同梱してあり、タイルサーバも外部通信も新規依存もありません — 監視コンソールは「ネットワークが
  壊れたときに開くもの」であり、インターネットを必要とする地図はまさに必要なときに真っ白だから
  です。
- **グループの座標を再び設定できます。** `PUT /api/v1/node-groups/{id}/geo` は v0.1.19 で
  「呼ばれていないエンドポイント」として削除されましたが、ダッシュボードの Geo マップ
  ウィジェットは座標を読み続けていたため、データを入れる手段が無いウィジェットが残っていました。
  エンドポイントを復活させ、グループのダイアログに緯度・経度の入力を追加しました。
- **Troubleshoot の「通知する」が実際に通知します。** 選択肢は提示され保存もされていましたが、
  消費する側がありませんでした。どの画面にいても完了通知が出て、レポートへのリンクが付きます。
  このブラウザで Yagra を開いている間だけ動作する点は、コントロール自身に明記しました。
- **アクティブアラートの行からミュートできます。** アラート ▸ アクティブ の「ミュート」が
  動作するようになりました。ノードを固定し、発火したメトリクスを事前入力した状態でミュート
  ダイアログを開くので、ノード全体ではなくトリアージ中のチェックだけを正確に対象にします。
  `POST /api/v1/mutes` が既に受け付けているロール（オペレータと管理者）にのみ表示され、
  ビューアには提示して拒否するのではなく最初から出しません。ミュートが止めるのは**通知**だけ
  で、アラート自体は一覧にも履歴にもそのまま残ります。
- **永久に無効だった「外部で開く」を削除しました。** 実装時から無効のまま出荷され、
  PagerDuty / JSM への深いリンクを約束していましたが、どう設定しても供給できないものでした
  — Yagra がこれらの連携先として保持しているのは送信先エンドポイントであって、インシデント
  ごとの URL ではないためです。ACK 済みピルが既に確認元のツール名と担当者を示しており、
  それが同じ情報の正直な形です。API の変更はありません。

### バグ修正
- **URL 監視を編集すると資格情報の紐付けが消えていました。** フォーム自身のコメントには「これは
  replace なので全フィールドを明示送信する」と書かれていながら、資格情報だけ送っていませんでした。
  紐付けを消費する側が無い間は見えませんでしたが、上記の機能が入ると無関係な編集だけで監視が
  ログアウトするところでした。
- **ノードインベントリのフィルタが黙って最大 100 件で切れていました。** API は上限を 500 に
  クランプしてそれを最大値として文書化していた一方、クエリ側が 100 に再クランプしていたため、
  数千件に一致するフィルタでも 100 件しか表示されず、切り詰めた旨の表示も出ませんでした。
  上限を 1 つの定数 (500) に統一してエッジとクエリの双方がそれを使うようにし、フィルタ結果が
  上限に達した場合はツリーに通知を表示します。
- **アップグレード後も、ブラウザがアップグレード前の画面を使い続けることがありました。** nginx が
  SPA に対して `Cache-Control` を一切返しておらず、ブラウザはヒューリスティックな鮮度判定
  （RFC 9111 §4.2.2）にフォールバックして、キャッシュ済みの `index.html` を数時間再利用しうる
  状態でした。イメージはドキュメントルートを丸ごと置き換えるため、その古いページが指すハッシュ
  付きアセットは新しいイメージには存在せず、しかも SPA のフォールバックがその要求に
  `index.html` を返していたため、ブラウザは `text/html` として配信されたスクリプトを拒否し、
  理由の手掛かりが無いまま何も表示しませんでした。`index.html` を `no-cache`（読み込みごとに
  再検証。変更が無ければ 304 のまま）に、`/assets/` 以下のハッシュ付きアセットを 1 年間
  `immutable` に、存在しないアセットは HTML ではなく 404 を返すようにしました。

## v0.1.19

**「なぜ」を尋ね、「どのポーラーが何を見ているか」を知る。** 今回の目玉は 2 つです。**AI による
根本原因分析**は発生中のアラートから実行できます — Yagra はもともとインシデントを組み立てています
（カスケードの根本原因、メトリックの異常、受動イベント、その周辺の支配的なトラフィック）。そこに
足りなかった「最後に人間が書く一文」を言語モデルに尋ねます。プロバイダを設定するまでは無効で、
アラート経路はこの機能に一切依存しません。もう 1 つは **ノード→ポーラーの可視化**です。各ノードが
所属プールと実際に自分をポーリングしているポーラーを表示し、プールはインベントリツリーから割り当て
られます — フォルダ単位も可能で、これがツリー唯一の一括割り当て手段です。土台としては、ノースバウンド
API が**自身の OpenAPI 3.1 文書をハンドラから生成して公開**するようになり、WebUI の型もそこから生成
されます。手書きの TypeScript 3,340 行が消え、契約のドリフトという問題群が構造的に閉じました。

### 破壊的変更
- **未認証リクエストは API 全体で 503 ではなく 401 を返すようになりました。** Yagra はエンドポイントが
  必要とするサブシステムが未構成のとき 503 を返します（スケルトンモード）。約 100 のハンドラがこの
  可用性チェックを権限ガードより**先**に実行していたため、認証情報を一切持たない相手が「構成済みか
  未構成か」を判別できていました。ガード順序を API 全体で統一し、認証・認可が先、可用性チェックが後に
  なります。認証済みの呼び出しに変化はなく、未構成サブシステムへの未認証リクエストだけが 503 から
  401 に変わります。
- **`VITE_API_BASE` の意味がベースパスからオリジンに変わりました。** 従来は既定値 `/api/v1` で相対パスの
  前に付けていましたが、既定値が空になり `/api/v1` は各パスの一部になりました。影響を受けるのはこの変数を
  上書きして WebUI をビルドしている場合だけで、対応は末尾の `/api/v1` を外すことです
  （`VITE_API_BASE=https://core.example.net`、`…/api/v1` ではなく）。併せて、ライブ更新のストリームが
  自分では `/api/v1` を付けておらず、この変数を設定すると API クライアントと別のホストを指していた不整合も
  解消しました。
- **`GET /api/v1/thresholds` が封筒形式になり、件数上限が付きました。** レスポンスが
  `StoredThreshold[]` から `{ "items": [...], "total": <n>, "truncated": <bool> }` に変わり、
  サーバは 1 リクエストあたり最大 **500** 件のルールを返します（`?limit=` で減らすことはできますが
  増やすことはできません）。WebUI は同じイメージに同梱されており併せて更新済みなので、影響を受けるのは
  **外部の自動化のみ**です。また、このエンドポイントへの未認証リクエストは 503 ではなく **401** を
  返すようになりました（API 全体の方針に合わせ、サブシステムが構成済みかどうかを開示する前に認証を
  行います）。ルールの読み取りには View ではなく **ManageConfig** が必要です — 閾値の集合は「いつ誰を
  呼び出すか」の設定だからです — のでパブリックダッシュボードでも閉じたままです。
- **`POST /api/v1/api-tokens` がグループスコープを拒否するようになりました。**
  `{"scope": {"Groups": [...]}}` を送るとトークンを発行せず **400 `unsupported_scope`** を返します
  （`scope` を省略するか `"All"` を送ってください）。グループスコープはどちらの経路でも強制されて
  いませんでした — `/mcp` はグループスコープ付きトークンをそもそも拒否し、REST のエンドポイントは
  スコープを一度も参照していません — ため、発行されていたのは「最小権限に見えて、実際には使えないか
  無制限かのどちらかである」認証情報でした。WebUI はこの項目を提供していないので、影響は API
  クライアントのみです。読み取り経路が実際にスコープで絞り込むようになった時点で再び受け付けます。

- **レポートとグループの 4 エンドポイントが、API の他の部分と同じステータスコードを返すようになりました。**
  レポートの削除 3 本（`DELETE /api/v1/reports/definitions/{id}` / `/runs/{id}` / `/schedules/{id}`）は
  `200 {"ok": true}` を返していました（他の 24 本の削除はすべて `204 No Content`）。今後は
  **204**（ボディなし）です。`POST /api/v1/node-groups` は生成した ID を捨てて 204 を返す唯一の
  作成系エンドポイントでしたが、他の 20 本と同じく **201** と `{"id": "<uuid>"}` を返します。
  `POST /api/v1/reports/schedules` は `{"id": …}` を 200 で返していましたが **201** になります。
  ステータスコードを厳密に判定するクライアントや、削除応答から `ok` を読むクライアントは修正が
  必要です。WebUI は同じイメージに同梱されており、どちらも読んでいませんでした。
- **`GET /api/v1/rca/{id}` と `POST /api/v1/rca` が、レポートの中身を記述するようになりました。**
  `body` フィールドは型なしの JSON として公開されていたため、生成クライアントでは AI の回答と
  その根拠がまるごと `unknown` になっていました。回答（`summary` / `root_cause` / `dependents` /
  `next_steps` / `confidence` / `raw`）と、その根拠となったインシデント文脈のスキーマを文書に
  追加しました。**ワイヤ上のバイト列は変わりません** — 記述が欠けていただけです。型を取り込むには
  クライアントを再生成してください。

- **Troubleshoot 解析の実行が、両経路とも Operator 権限に揃いました。** これまで REST API では
  **Admin**、MCP では **Viewer** で通っており、オンコールの運用者が WebUI では弾かれるのに、
  同じ人が AI クライアント経由なら同じ解析を実行できる状態でした。どちらも「アラート応答」権限
  （Operator 以上）を要求するようになり、実行中ジョブのキャンセルも同じです。解析は設定を何も
  変えないので、従来の Admin 要求は実質レート制限の代用であり、その役割は既に admission 制御
  （同時実行数・分間レート）が担っています。過去の実行と所見の**閲覧は従来どおり Viewer に開放**
  されています。**Viewer 権限の API トークンでは `/mcp` から解析を開始できなくなります。**

- **どこからも呼ばれていなかった 4 つのエンドポイントを削除しました。** `GET /api/v1/rca/{id}`、
  `GET /api/v1/reports/definitions/{id}`、`PUT /api/v1/node-groups/{id}/geo`、
  `POST /api/v1/events/alerts/close` は到達可能でしたが、WebUI からも MCP ツール面からも、文書化
  された自動化からも呼ばれておらず、入口のない機能のためにリクエストを受け、公開契約に載り、
  テストを抱えていました。今後は `404` を返します。それぞれが返していたデータは今も取得できます
  — RCA レポートは生成元の `POST /api/v1/rca` が返し、レポート定義は
  `GET /api/v1/reports/definitions` が返します。**うち 2 つは整理ではなく機能の喪失です:**
  グループの地図座標はもう設定する手段がありません（Sites マップウィジェットは読むだけで、書き手
  がいなくなります）。またイベント由来アラートの手動クローズもできなくなり、従来どおりルールの
  TTL で解消されます。どちらかが必要であれば、UI を伴う形で復活させられますのでお知らせください。

### 新機能
- **AI による根本原因分析を、必要なときに実行できます。** Yagra はもともとインシデントの根拠を
  組み立てています — 依存抑制がカスケードを根本原因に帰属させ、インシデントタイムラインがメトリックの
  異常・受動イベント・その周辺の支配的なトラフィックを集めます。どれだけ相関を取っても出てこないのが
  「最後に人間が書く一文」です。**発生中のアラート**にボタンが追加され、まさにその根拠に基づいて言語
  モデルにその一文を尋ね、要約・推定される根本原因・それで説明が付く配下ノード・次に取るべき手順を
  返します（確信度と、モデルの生の回答も併せて保持します）。
  **設定するまでは無効です。** プロバイダ設定が無ければクライアントも認証情報も外向き通信も存在
  しません。アラート経路からこの機能を呼ぶ箇所は一切無いため、プロバイダが応答しても、タイムアウト
  しても、そもそも未設定でも、ヒステリシス・抑制・重複排除・通知の挙動は完全に同一です。プロバイダは
  **Vertex AI** / **Gemini** / **Claude** から 1 つを選び、その認証情報は他のシークレットと同じ封筒
  暗号で封印され、保存後は書き込み専用になります。レポートの生成には **Operator** が必要で（説明を
  必要とするのはポケベルを持っている人だからです）、狭い権限ではなく同時実行数・レート窓・文脈
  キャッシュで上限を掛けます。生成済みレポートの閲覧は View、プロバイダ設定は Admin です。プロンプトに
  引用される機器出力はフェンスで囲われ、各プロバイダのエンドポイントは設定項目ではなくコンパイル時
  定数なので設定画面が持ち出し経路になることはありません。そして **Yagra がネットワーク機器を設定する
  手段は依然として存在しません**。
- **各ノードが「どのポーラーがポーリングしているか」を表示し、プールをインベントリツリーから割り当て
  られるようになりました。** ノード→ポーラーの割り当ては以前から存在したものの、実質的に不可視でした
  — 「このノードはどのポーラーが見ているのか」を答えるには `redis-cli` を叩くしかなく、ノードの
  プールはどの画面も送っていない API フィールドからしか書けませんでした。ノード詳細に**プール**と
  **ポーリング元**が表示されます。後者はハッシュリングから再計算するのではなく**コアが実際に配布した
  ワーキングセット**から読む 5 状態の回答（割当済み / 保留 / レガシー fan-out / Meraki / 不明）なので、
  ノード側の答えとポーラー側のノード一覧が食い違うことはありません。**設定 ▸ ポーラー**からは各
  ポーラーの担当ノード一覧をその場で展開できます。プールはノードとフォルダの両方で編集でき、
  **インベントリツリーで右クリックするとプールのチップ列**（既存のプール＋継承＋カスタム）が出ます —
  ツリーには複数選択が無いため、これが唯一の一括割り当て手段です。ノードの実効プールは「自身 → 最も
  近い祖先フォルダ → `default`」の順に解決され、各チップはそのプールに**稼働中のポーラーがいるか**を
  示します。いないプールに割り当てると、ジョブが誰も購読していないサブジェクトへ配信され、ノードは
  黙って監視されなくなるからです。
- **URL / DNS 監視の設定を、作成後に編集・削除できるようになりました。** これまではノード追加
  ダイアログで作成し、ノード詳細で表示することはできても、URL・タイムアウト・リゾルバ・レコード
  種別を変更するにはノードごと削除して作り直すしかありませんでした（エンドポイントは最初から
  存在し、呼び出し元だけが無い状態でした）。ノードの URL/DNS ヘルスカードに ⋮ メニューが付き、
  **編集**と**監視設定を削除**が行えます。編集フォームは期待する HTTP ステータス（2xx すべて／
  コード列挙／範囲指定）を含む全項目をカバーします。監視設定を削除してもノードはインベントリに
  残り、記録済みの履歴も保持され、以後チェックされなくなるだけです。他の監視設定変更と同じ権限
  （Admin）が必要です。

- **API が自身の OpenAPI 3.1 文書を `GET /api/v1/openapi.json` で公開するようになりました。** 文書は
  ハンドラそのものから生成されます — パス・クエリパラメータ・リクエストボディ・レスポンス形状・
  エラーコードのすべて — ので、「誰かが書き残したもの」ではなく**サーバが実際に行っていること**を
  記述します。`/api/v1/version` や `/api/v1/config` と同様に認証不要です（在庫・設定・状態を一切
  含まず、全デプロイで内容が同一のため）。任意の OpenAPI クライアントジェネレータをこの URL に
  向けられます。WebUI 自身の型と API クライアントもこの同じ文書から生成されるようになり、
  **何も検査していなかった 3,340 行の手書き TypeScript** がなくなりました。

### 改善
- **グループスコープによる可視範囲制限（ADR-014）が、読み取り面の全体で強制されるようになりました。**
  ノードに紐づくデータを返すすべてのエンドポイントが、呼び出し元アカウントのフォルダグループ
  スコープで絞り込みます。SQL の一覧には索引の効く述語が入り、ランキングやメモリ内集計は取得後に
  絞り込まれ、SSE の 2 本のライブストリームはスコープ外ノードのイベントを流さず、ノード単体の
  経路は見えないノードに対して `403` ではなく **`404`** を返します（ノード ID の総当たり探索を
  許さないため）。リクエストボディでノードを指定する運用操作（確認応答・ミュート・メンテナンス
  ウィンドウ・即時ポーリング・分析の実行）も同様に検査されます。**現時点でスコープを保持できる
  アカウントは存在しない**ため、既存のデプロイの挙動は何も変わりません — これはスコープを発行
  できるようにする前に必要な、強制の側の実装です。なお絞り込みが原理的にできない 5 つの読み取り
  （保存済みレポート、フリート状態のタイムライン、合計スループット、フリート横断の `/flow/*`
  集計）は、フリート全体の数値を返す代わりに `403 scope_unsupported` で拒否します。いずれも
  集計済みで保存・計算されており、絞り込みの手がかりとなるノード単位の帰属が残っていません。
- **アクティブアラートの行が、UUID 2 つではなく「どの機器で何が壊れたか」を表示するようになりました。**
  従来は `● 550e8400-… 7c9e6679-… 2分前` と読めていました。ノードは名前に解決され（ID は他の画面と
  同様ホバー時のみ）、チェック ID は（ノードとメトリックの一方向ハッシュなので解決できる名前が
  存在しません）**そのチェックが実際に測っていたもの**に置き換わります — 例：
  `icmp_rtt_ms above 100 (was 450)`。この情報は元々すべてのアラートに保存され、アラート ▸ 履歴には
  表示されていましたが、トリアージ画面だけが出していませんでした。両画面は同じ整形関数を通るように
  なったので、食い違うことはありません。
- **設定 ▸ システムヘルスがフローストアを表示し、サーバー自身の判定を示すようになりました。**
  ページの総合判定は ClickHouse を数えていたのに行が無かったため、フローストア障害が
  「すべて到達可能」に見えていました。5 つのバックエンドストアすべてとバスを列挙し、
  「すべて到達可能」/「劣化」バッジを**サーバーの回答から**表示します（行から再計算しません）—
  次に列挙を忘れた依存があれば、目に見えて食い違います。
- **ポールループの健全性がワーキングセットの配布状況を表示します。** ワーキングセットとして配布して
  いるプール数と、ジョブ単位の配信にフォールバックしているプール数（後者は 0 でなくなると琥珀色に
  なります — そのプールに稼働中の登録ポーラーが無いという意味です）、スナップショットと差分の配布数、
  割当ミラーの書き込み数を追加しました。設定 ▸ ポーラーにも各ポーラーの初回登録日を示す
  **登録日**列が付きます。
- **アプリのアイコンを Yagra ウェブサイトと同じものに統一しました。** ブラウザタブの favicon と、
  上部バー / モバイル上部バー / サインイン画面のマークが、旧「二重輪」から**「Y に見えるトポロジの
  フォーク」**（1 つのルートノードが 2 つに分岐する形）に変わります。オレンジの朱印・配色・サイズは
  従来どおりです。タブのアイコンはキャッシュのためスーパーリロードが必要な場合があります。
- **イベント検索の絞り込みとページングが、取り込み時刻ではなく発生時刻を基準になりました。**
  PostgreSQL 経路は取り込み時刻を、VictoriaLogs 経路は既に発生時刻を使っていたため、有効なログ
  ストアによって同じ検索が違う行・違う並び順を返していました。両者とも発生時刻に統一されます。
  時刻順ログである以上避けられない副作用として、リモートポーラーが再接続してバッファした結果を
  再送すると（store-and-forward）、既にスクロールして通過したページより**後ろ**に古いイベントが
  挿入されることがあります — これは VictoriaLogs 経路の従来からの挙動と同じです。
- **フラッピング監視ウィジェットがノード名を表示するようになりました。** これまで生のノード UUID と
  チェック UUID を表示していましたが、「発生中のアラート」一覧と同じ流儀で、ノード名（UUID はホバーで
  表示）と、フラッピングしているチェックが何を測っているかを表示します。

### バグ修正
- **WebUI が知らない状態のレポート実行が「失敗」と表示されていました。** ステータスバッジの
  フォールバックが、未知の値をすべて赤（critical）で描画していたため、**実際には成功した実行が
  壊れたように見える**ことがありました。特にローリングアップグレード中、古い WebUI が新しいコアの
  書いた行を読むときに顕在化します。実行状態・トリガー・スケジュール周期を API 契約上の閉じた集合
  （`queued`/`running`/`succeeded`/`failed`/`unknown` など）にし、バッジを状態ごとのマップ
  （フォールバックなし）に置き換えました。本当に未知の状態は「不明な状態」として中立に表示します。
  **ワイヤ上の変更はありません** — 同じ文字列を、記述しただけです。
- **DNS 監視の失敗理由が、内部トークンのまま表示されていました。** ノード ▸ DNS の解決結果列に
  `nx_domain` / `serv_fail` / `depth_exceeded` など 9 種類が**翻訳されずそのまま**出ていました。
  今後は両言語で文章として表示されます（「名前が存在しない（NXDOMAIN）」など）。
- **1 つのイベントストリームに限定したアラートルールが、黙って全ストリームに広がることがありました。**
  このビルドが知らないソース種別を指定したイベントルールは「種別フィルタなし」として解釈され、
  マッチャはそれを**任意の種別**と読みます。結果として、syslog・トラップ・Webhook のすべてに対して
  発火していました。今後そうしたルールはエンジンに載せず、ログに記録して、コアが理解できるように
  なるまで待機します。
- **プールを移動したノードを、元のポーラーがポーリングし続けていました。** スケジューラはノード行
  だけからプール一覧を組み立てていたため、最後のノードが抜けたプールは一覧から消え、二度と再調整
  されませんでした。そのポーラーはコアプロセスが生きている限り古いワーキングセットをポーリングし
  続け、移動したノードをすべて二重にポーリングします。復旧にはポーラーの再起動が必要でした。ノード
  走査の前に、稼働中の全プールを一覧に必ず入れるようにしました。これまではデータベースを直接編集
  しないと踏めない経路でしたが、WebUI からプールを編集できるようになると運用者が最初に行う操作に
  なるため、同時に修正しています。
- **フィルタ付きのフロー転送先が、コレクタに必要なテンプレートデータグラムを落としていました。**
  フィルタ付きの転送先はデコード済みのフローレコードを判定しますが、NetFlow v9 のテンプレート定義
  のみを載せたデータグラムにはレコードが 1 件もありません。そのため、テンプレートをレコードなしの
  データグラムで別送するエクスポータでは、フィルタ付きコレクタが**永久に復号できないデータセット**を
  受け取り続けていました（無言で、恒久的に）。テンプレート定義があり、フローレコードが 0 件の
  データグラムはフィルタを迂回するようになります。判定基準は意図的に「レコード 0 件」ではなく
  「テンプレートがあってレコード 0 件」です — テンプレート未知のデータセットもデコード結果は 0 件に
  なりますが、そちらはコレクタに何も与えないので従来どおりフィルタが判断します。テンプレートを毎回
  同梱するエクスポータは元から影響を受けません。実機のコレクタに対する検証で発見しました。
- **1 つのノードに URL 監視と DNS 監視の両方が設定でき、DNS 側が一度も実行されませんでした。**
  「ノードはちょうど 1 種類」というガードが DNS 側の書き込みにしか入っていなかったため、DNS 監視中の
  ノードへ URL チェックを足す操作が受理・保存され、URL を先に解決するスケジューラは DNS チェックを
  永久に実行しませんでした。両方の書き込みが同じガードを通るようになり、優先順位の定義も 2 箇所から
  1 箇所になりました。
- **MCP の `get_fleet_summary` が、観測していないノード状態を省いていました。** REST 側の集計は 6 状態
  すべてをあらかじめ埋めるのに対し、MCP ツールは実際に存在した状態しか入れていませんでした。その
  ため `states["warning"]` を読む AI クライアントには、WebUI なら 0 が返る場所でキーそのものが無く、
  「警告データは存在しない」と自信を持って報告される状態でした。両経路が同じ集計を返します。併せて、
  ノードが 1 台も無いときのデータカバレッジが 0% ではなく 100% と読まれるようになり、新規インストール
  のたびに「監視の死角」ウィジェットが点灯して無視されるようになる、ということがなくなります。
- **鍵の不備や DB 到達不能が「入力が不正です」と表示されていました（OIDC 設定）。** ID プロバイダの
  保存は**あらゆる**失敗を `400 invalid_provider` として内部エラー文字列を添えて返していたため、鍵
  暗号鍵の問題や DB 書き込み失敗が入力ミスとして報告され、内部メッセージがそのままワイヤに出ていました。
  今後、入力不備はどの項目かを述べる 400、内部障害は原因をログに残す不透明な 500、サインイン中に IdP へ
  到達できない場合は 500 ではなく 502 になります。
- **正規表現によるイベント検索が、VictoriaLogs では大文字小文字を区別し、PostgreSQL では区別して
  いませんでした。** 有効なログストアによって同じ正規表現が違う行にマッチしていたため、どちらも
  区別しないように揃えました。なお**プレーン語**の検索は、VictoriaLogs ではトークン単位、PostgreSQL
  では部分一致のままです — 転置インデックスは語の先頭以外の部分一致を全ブロック走査なしには扱えず、
  実機計測で 0.22 秒に対し 30 秒でした — のでこの 1 軸だけは意図的な差異として残し、トークンの内側まで
  探したいときは検索ボックスの正規表現トグルを使ってください。
- **ポーラーの store-and-forward バッファがディスクを使えるようになりました。** コンテナイメージが
  スピル用ディレクトリを作成しておらず、`/var/lib` は非 root の実行ユーザーには書き込めないため、
  起動時に警告を 1 行出したあとメモリのみで動作していました。そのためメモリ上のリングを超える長さの
  バス断ではスピルされずに古いポーリング結果が捨てられ、断の最中にポーラーを再起動すると保持していた
  ぶんがすべて失われていました。ディレクトリを適切な所有者でイメージ内に作成し、テストサーバ構成では
  名前付きボリュームを与えたのでコンテナ再作成をまたいでスピルが残ります。
- **ノードが二重に追加される問題。** 監視追加ダイアログの送信ボタンがリクエスト中に保護されて
  いなかったため、ダブルクリックでノードが 2 つ作られていました。またダイアログが前回の失敗メッセージを
  保持していたため、失敗後にキャンセルして開き直すと空のフォームの上に古いエラーが表示されていました。
- **OpenAPI 文書が「REST API でパーソナルアクセストークンが使える」と記述していました（誤り）。**
  公開していた `bearer` スキームはセッショントークンの代替として `yat_…` を挙げていましたが、この API
  の認証エッジはセッショントークンしか受け付けないため、契約どおりに実装したクライアントは全呼び出しで
  401 になっていました。記述を実態に合わせました — パーソナルアクセストークンは `/mcp` の MCP 面を
  認証するもので、REST API には `POST /api/v1/auth/login` で得たトークンが必要です。
- **ダイアログがキーボード操作を内側に保持するようになりました。** これまで Tab を押すと開いている
  ダイアログの外（背後のページ）へ抜けてしまい、`aria-modal` が「背後は不活性」と伝えているにも
  かかわらず、キーボードやスクリーンリーダーの利用者がオーバーレイに隠れた入力を操作しうる状態でした。
  Tab / Shift+Tab はダイアログ内を巡回するようになり、トラップするのは最前面の 1 枚だけです。
- **通知ベルが機能するようになりました。** 発生中アラート件数を表示しながらクリックを無視していた
  ベルが、デスクトップ・モバイル両方の上部バーで「発生中のアラート」を開くようになりました。
- **ダークテーマで 3 色が読みづらかった問題。** パレットの 4・5・6 番目がダーク面でもライトテーマの
  値のままでした。これらはグラフだけでなく、Troubleshoot のレポート本文の大半と、受動イベント／
  キャパシティのダッシュボードウィジェットでも使われています。
- **しきい値が生カウンタを判定しなくなりました。** カウンタ系メトリクス（`if_hc_in_octets` や
  errors / discards など、収集カタログがカウンタと宣言しているもの）へのしきい値は、単調増加する
  生の累計値を境界と比較していたため、`above` ルールは一度超えると永久に発火したまま、`below`
  ルールは機器再起動によるカウンタリセットのたびに誤アラートを出していました。カウンタのサンプルは
  OK として扱われるようになり（これにより既存のラッチ済みアラートも通常の回復経路で解消されます）、
  カウンタ系メトリクスへの `POST /api/v1/thresholds` は **400 `counter_metric`** を返します。
  レート／使用率はクエリ時導出のままです（ADR-012）。しきい値はゲージに設定してください。
- **レポートの削除失敗が黙って閉じなくなりました。** レポートテンプレート／保存済みレポート／
  スケジュールの削除が失敗してもエラーを握りつぶしてダイアログを閉じ、成功したように見えていました。
  他の削除ダイアログと同じく、確認ダイアログが開いたままエラーを表示します。またレポートビルダーと
  スケジュールダイアログは保存中に Cancel を無効化します。

### セキュリティ
- **SSO アカウントを無効化しても、本人はサインインし直せていました。** ローカルパスワードのログイン
  経路は以前から無効なアカウントを拒否していましたが、SSO のコールバックは「IdP が本人だと言っている」
  から直接セッション発行へ進み、`users.enabled` を一度も読んでいませんでした。そのため SSO で
  プロビジョニングされたアカウントを無効化しても、稼働中のセッションが失効するだけで、次の SSO
  ログインが新しいセッションを発行してしまい、**IdP 側で止める手段を持たない運用者にとってこの操作は
  事実上の無効**でした。SSO 経路でも無効なアカウントを拒否し、他のコールバック失敗と同じ不透明な
  `oidc_denied` を返します。
- **API トークンで認証された変更操作が、匿名として監査記録されていました。** `audit_mw` は Bearer を
  *セッション*ストアだけで解決しており、トークンはそこに存在しません。トークンが `/mcp` 専用の間は
  この経路に到達しませんでしたが、REST を開けば到達します。Bearer はリクエストごとに一度だけ解決
  されるようになり、監査行にはアカウントと資格情報の両方(`svc-ci (token:grafana)`)が記録されます。
  トークン経由の変更も、責任を負う人物にたどれます。
- **URL 監視が IPv6 のループバック / リンクローカル宛先を受理していました。** エッジのバリデータは
  URL のホストを IP アドレスとして解釈して SSRF になりうる宛先を弾いていましたが、URL パーサが返す
  IPv6 リテラルは**角括弧付き**（`[::1]`）で、アドレスパーサはこれを拒否します。そのため IPv6 の
  宛先はすべてホスト名として扱われ、ブロックを丸ごと素通りしていました。URL 監視の作成・編集で
  `http://[::1]/` や `http://[fe80::1]/` が通っていたということです。**これは持ち出し経路ではなく、
  「何もしていなかったチェック」でした** — ポーラー側が実際のプローブ時に同じアドレスを拒否するため、
  実リクエストが飛んだことはありません。URL のホストを解釈する箇所は他に 3 つあり、そちらはいずれも
  正しく実装されていました。4 箇所とも 1 つの実装を共有します。

## v0.1.18

**受信した受動データを、そのまま次へ渡す。そして可視化する。** Yagra は syslog・SNMP トラップ・フロー
エクスポートを受信できていましたが、本リリースではそれらを**転送**できるようになりました。フィルタ付きの
ティー（tee）で SIEM やコレクタへ、バイト単位そのまま、または再構築して、UDP／TCP／TLS で送ります。さらに
長期のクエリ用に正規化された行を流し込む **BigQuery** 宛先も追加しました。あわせて、**DNS 名前解決監視**を
第一級のノード種別として、受動イベント／フローストアに対する **11 個の新しい Troubleshoot 分析**、
**15 個すべての分析に専用のレポート画面**、受動イベントとトラフィックフローの**新規ダッシュボード
ウィジェット 13 個**を追加しています。

### 新機能
- **転送（ティー）宛先** — 新しい **Settings ▸ Forwarding** 画面で、「このフィルタに一致するものはすべて
  そこへも送る」という宛先を定義します。送信はリーダーの core だけが行うため、ファイアウォールで許可する
  のはポーラーごとではなく**1 か所**の送信元だけで済み、機器ごとに 2 つ目のエクスポート先を設定する必要も
  なくなります。
  - **syslog と SNMP トラップ**は**バイト単位そのまま**中継します。解析済みフィールドとは別に元の
    データグラムを保持しているため、コレクタは機器が送ったものをそのまま受け取ります。元データが存在
    しない場合は、忠実な RFC 5424／SNMPv2c メッセージを再構築して送ります。
  - **フローエクスポート**（NetFlow v5/v9/IPFIX、sFlow）はテンプレートのデータグラムも含めてそのまま
    中継します。集約済みのフローレコードでは代用できません — バケット化・上位 N の切り捨て・5 タプルの
    畳み込みはいずれも不可逆だからです。フローのフィルタは **any-record**（1 レコードでも一致すれば
    データグラム全体を転送）判定です。レコードは再エンコードなしに取り除けないためです。
  - **syslog over TLS**（RFC 5425）は、システムのトラストストアに加え、宛先ごとの任意の CA 証明書で
    コレクタを検証します。
  - **BigQuery 宛先**は `tabledata.insertAll` で**正規化された型付きの行**（イベント 1 件＝1 行、フロー
    レコード 1 件＝1 行）を流し込みます。ライブのストリームを複製するのではなく、数か月分の履歴を
    クエリするための経路です。行は独立しているため、ここでのフローフィルタは**レコード単位で厳密**に
    効きます。テーブルは日次パーティションとクラスタリング付きで自動作成されますが、データセットは
    作成しません — データ所在地を Yagra が勝手に決めないためです。
  - 各宛先は上限付きキュー・レート制限・サーキットブレーカを備え、**黙って劣化することはありません**。
    バイト完全一致を約束した宛先に元データが渡らなければカウントされ、元バイトを供給できないポーラーは
    画面上に名指しで表示されます。
- **DNS 名前解決監視（ノード種別）** — URL を監視するのと同じ感覚で*名前*を監視できます。ノードを組み込みの
  **DNS name resolution** プロファイルに紐付けると、名前が解決できるかどうかと、dig のような再帰的な
  **CNAME チェーン**を記録します。履歴は**チェーンが実際に変化したときだけ**追記されます（TTL の
  カウントダウンやラウンドロビンの並び替えは変化として扱いません）。数値サマリ（`dns_up`、
  `dns_resolve_ms`、`dns_chain_length`、`dns_answer_count`）はグラフ化・アラート対象で、`dns_up` の既定
  しきい値が投入済みなので、新しいモニタは作成した時点でアラートが機能します。
- **受動イベント／フロー向けの新しい Troubleshoot 分析 11 種** — 受動系は `event_storm`、`event_flap`、
  `severity_shift`、`rule_gap`、`auth_probe`。フロー系は `traffic_anomaly`、`talker_shift`、
  `new_destination`、`flow_scan`。さらにメトリクス・イベント・フローを横断して読む `saturation` と
  `incident_correlate`。従来どおり分析は**読み取り**であり（機器 I/O は行いません）、同じ同時実行数・
  レート制限の下で動作します。
- **すべての Troubleshoot 分析に専用レポート。** これまで実質的なレポートがあったのはアノマリー分析だけで、
  残る 14 ツールは「coming soon」のトーストを出すだけでした。いまは 15 種すべてが、それぞれの検出結果に
  合わせた画面を持ちます — `incident_correlate` の SVG **インシデントタイムライン**（シグナルが届いた
  「順序」こそが原因を指し示すため）、`flow_scan` のスキャン形状の散布図、`saturation` のシェアメーターと
  キャパシティ文脈など。全レポートが CSV エクスポートと `?job=` ディープリンクに対応します。
- **受動イベント／トラフィックフローのダッシュボードウィジェット** — カタログに 2 セクション、**13 個**の
  ウィジェットを追加しました。イベントフィード、件数推移、種別ミックス、上位トラップ、トリアージ、
  ノイズの多い送信元、ルールカバレッジ。加えて上位トーカー、上位 AS、上位ポート、プロトコルミックス、
  会話 Sankey、トラフィックトレンド。フロー系ウィジェットはノード単位ではなく**フリート全体**（全
  エクスポータ）を読みます。
- **フロー／イベント向けの新しい MCP ツール** — `flow_fanout` と `event_stats` を追加し、`top_flows` に
  プロトコル／ポート／ピア／ASN／方向のフィルタと AS 名解決が加わりました。`run_analysis` は新しい分析
  種別をすべて受け付けます。

### 改善
- **メトリクス／ログストアのコンテナイメージを固定。** `docker-compose.yml` と
  `docker-compose.deploy.yml` が `victoria-metrics:latest`／`victoria-logs:latest` を参照していましたが、
  いずれも明示的なバージョンを指すようにしました。無関係な `docker compose pull` が履歴の下でストレージ
  エンジンを入れ替えてしまうことはなくなります。更新はバックアップを取ったうえで意図的に行ってください —
  固定済みの Rust ベースイメージと同じ方針です。
- **負荷時のストア&フォワードの退避が軽くなりました。** バス障害中に結果をバッファリングするリモート
  ポーラーが、退避 1 件ごとにファイルシステムの空き容量を問い合わせる（非同期ランタイム上での）システム
  コールを発行しなくなりました。読み取り値はキャッシュされ、書き込んだバイト数で差し引かれるため、安全
  フロアは遅れてではなく**早めに**作動します。
- **トラフィックフローの表示が見やすくなりました** — 会話のエンドポイントに最小ラベル幅を設けて長い
  アドレスでも読めるようにし、会話 Sankey は無制限に伸びずサイズ上限を持つようになり、送信元／宛先の
  AS 番号をアドレスと併記するようにしました。
- **フリート全体のフローエンドポイント。** フロー API はノード限定ではなくなりました。系列、上位トーカー、
  会話、上位ポート、プロトコル、上位 AS のいずれもフリートスコープを取れます。新しい `/events/stats`
  エンドポイントは、イベントログと同じフィルタでカテゴリ別・時系列のイベント集計を PostgreSQL または
  VictoriaLogs 経由で返すため、サマリとログの数字が常に一致します。

### 修正
- **知らないチェック種別に出会ってもポーラーが停止しなくなりました。** ワーキングセットのスナップショット
  チャンクを要素ごとにデコードするようにしたため、未知のチェック 1 件がチャンク全体を失敗させ、シーケンスに
  欠落を作り、そのポーラーを再同期ループに陥らせる（結果としてそのポーラーの*すべて*のポーリングが止まる）
  ことはなくなりました。DNS に限らず、今後追加されるあらゆるチェック種別に対する恒久的な修正です。
- **DNS チェック同士が互いを打ち消さなくなりました。** ターゲット単位の single-flight ポーリングガードは、
  DNS モニタが設計上リゾルバのターゲットを共有するため、1 サイクルにつき 1 件を残して他をすべて落として
  いました。グローバルガードを使うようにしています。
- **AS ドリルダウンが Conversation flow を壊さなくなりました。** AS で絞り込むと会話テーブルと Sankey が
  500 を返し（集約に、絞り込み対象の列と同じ名前のエイリアスを付けていたため）、他のパネルだけが更新されて
  画面に古いデータが残っていました。
- **Troubleshoot レポートのディープリンクが正しく解決されます。** 直近ジョブの取得範囲より古い `?job=` は
  何も要求されていないかのように表示され、別ツールに属する `?job=` は無関係な検出結果の上に誤ったレポートを
  描画していました。レポートシェルがジョブを直接取得し、その結果を読めるレポートへリダイレクトします。

### セキュリティ
- **転送は、そもそも漏れない作りにしています。** TLS 宛先には**証明書検証を無効化する手段がありません**。
  BigQuery 宛先には意図的に**生ペイロードの列がありません** — 中継されたデータグラムは運用者が選んだ
  コレクタに一度渡るだけですが、テーブルは残り続けます。生バイトの列があれば、syslog 本文に日常的に現れる
  資格情報が恒久的に外部でクエリ可能になってしまうためです。注意書きが読まれることに期待するのではなく、
  API が BigQuery に対する verbatim モードを拒否します。サービスアカウントの署名には定数時間の RSA 実装を
  使用しています。
- **メンテナンスされていない TLS 依存を除去。** `rustls-pemfile`（RUSTSEC-2025-0134、上流でアーカイブ済み）
  を削除し、証明書と鍵の解析は同じコードを `rustls-pki-types` から直接利用するようにしました。サプライ
  チェーンのポリシーチェックは advisories・bans・licenses・sources のすべてでクリーンです。
- **クライアント側のオープンリダイレクトの脆弱性を解消**（フロントエンドのルータ更新）、あわせてビルド
  ツールチェーンのパストラバーサルの脆弱性も解消しました。前者は利用者に影響するもの、後者はビルド時のみで
  製品には同梱されません。

## v0.1.17

**AI アシスタントから Yagra を操作。** Yagra に組み込みの **MCP（Model Context Protocol）サーバ**が
加わりました。オプトインかつ認証付きのツール面を `/mcp` に公開し、AI クライアント（Claude Code/Desktop
など、MCP 対応アシスタント）から稼働中の監視状態を照会し、自然な言葉で診断を実行できます。既定は無効で、
有効化してもほとんどが読み取り専用です。数少ない書き込みツールは権限で保護・監査され、機器の設定は一切
変更できません。

### 新機能
- **MCP サーバ（AI／自動化ツール面）** — `YAGRA_ENABLE_MCP` で API ポートの `/mcp` に Streamable HTTP の
  MCP エンドポイントを公開します。**15 個のツール**を提供します。読み取りツールはフリート概要、ノード、
  ノード状態、アクティブ／履歴アラート、メトリクス、トポロジ、トラフィックフロー、受動イベント
  （syslog／トラップ／Webhook）に対応し、オンデマンドの **Troubleshoot 分析**（アノマリー／相関／
  キャパシティ／フラップ）と、**3 個の書き込みツール**（アラートの確認、メンテナンス枠の開始、即時
  ポーリング）を備えます。ツール出力はサニタイズされ（監視資格情報がシステム外に出ることはありません）、
  **ネットワーク機器を設定・変更するツールは存在しません**。
- **API トークン** — 新しい **Settings ▸ API tokens** 画面で、MCP アシスタントのような非ブラウザ
  クライアント向けに、長寿命の `yat_` トークン（ロールとグループスコープ付き）を発行します。生のトークンは
  作成時に**一度だけ**表示され、保存されるのはハッシュのみです。発行・失効は管理者操作で、監査されます。

### 改善
- **オンデマンド分析にレート・同時実行の上限。** Troubleshoot 分析ランナーが、同時に実行する分析数
  （`YAGRA_ANALYSIS_MAX_CONCURRENT`、既定 4）と 1 分あたりの開始数（`YAGRA_ANALYSIS_RATE_PER_MIN`、
  既定 30）を制限するようになりました。飽和時は処理を積み増さずに API が `429` を返します。UI と新しい
  MCP 分析ツールの双方のコストを抑えます。

### セキュリティ
- **MCP 書き込み操作のツール単位の認可と監査。** すべての MCP 書き込みツールが呼び出し元のロール（RBAC）
  を再確認し、監査エントリを記録します。フェイルクローズ設計で、呼び出し元を認可できない場合は書き込みを
  拒否します。**Viewer** トークンは読み取り専用のままです。
- **フロントエンドのビルドツールチェーンの High 深刻度 2 件を解消。** 推移的な開発依存（`brace-expansion`、
  `js-yaml`）を更新しました。いずれもビルド時のみで製品には同梱されませんが、依存ツリーはクリーンに
  なりました。

## v0.1.16

**ライセンスとフロー取り込みの堅牢化。** Yagra は **GNU Affero General Public License v3.0
（AGPL-3.0-only）** の下で提供されるようになりました。あわせて本リリースでは、フローストアが
混雑・低速でもシステム全体に影響しないよう、トラフィックフローの取り込み経路を堅牢化しています。
新しいユーザ向け機能はありません。既定のインストールは v0.1.15 と同じ挙動のまま、新しい
ライセンスで動作します。

### ライセンス
- **AGPL-3.0-only へ再ライセンス。** すべてのソースファイルに SPDX ヘッダを付与し、リポジトリは
  `AGPL-3.0-only` で提供されます。Yagra は通常ネットワークサービスとして運用されるため、AGPL
  **§13** に注意してください: **改変版**をネットワーク越しに他者へ利用させる場合、そのソースを
  提供する必要があります。AGPL 以外の条件での利用（プロプライエタリ製品への組み込みなど）には
  **商用ライセンス**を提供できる場合があります（README を参照）。

### 改善
- **フロー取り込みが低速なフローストアから隔離されました。** ClickHouse フローライタをフローバス
  コンシューマから分離し、行を境界付きキュー経由で受け渡すようにしました。ClickHouse が低速・
  ハングしても `yagra.flows` サブスクリプションが詰まって無言でメッセージを落とすことはなくなり、
  高負荷時はフロー行を（ロス許容の設計に従い）破棄・計上して、取り込みを止めません。
- **フローのエクスポータ解決がバッチごとにノードテーブルを再読込しなくなりました。** 送信元 IP が
  登録ノードでないエクスポータ（loopback から export するルータで一般的）からのフローは、以前は
  バッチごとに全ノードテーブルを再スキャンしていました。未マップのエクスポータは最大 1 回のみ
  再試行するようになり、数万ノード規模でもフロー取り込みが軽量に保たれます。

## v0.1.15

**ネットワークを流れるトラフィックを可視化。** Yagra はネットワークの**フロー**情報（NetFlow・IPFIX・
sFlow）を収集・分析できるようになりました。これまでの死活・性能監視と並べて、ネットワークを流れる上位の
トーカー・ポート・プロトコル・AS（自律システム）を確認できます。受信した SNMP トラップも読みやすい名前に
解決され、アクティブアラート画面は大規模障害時でも快適に動作します。

### 新機能
- **トラフィックフロー監視（NetFlow v5/v9・IPFIX・sFlow v5）** — 機器のフローエクスポートをポーラ
  （NetFlow/IPFIX は `:2055`、sFlow は `:6343`）へ向けると、Yagra が収集し、エッジで集約（時間バケット
  ごとに上位 N 件）して、専用の **ClickHouse** フローストア（保持 1 か月・ロス許容）に保存します。各ノード
  に **Flow** タブが追加され、上位トーカー・上位ポート・上位プロトコル、そして会話の **Sankey** 図を表示
  します。トーカー／ポート／プロトコルをクリックして絞り込めます。フローはポーラ単位のオプトインで、
  メトリクスとは完全に独立しています。無効のままなら挙動は変わりません。
- **フローの AS（自律システム）補完** — フローの通信相手を、オフラインの IP→ASN テーブルから **AS 番号と
  名前**に解決します。**Top AS** カードとドリルダウン（AS をクリックしてそのフローを絞り込み）、AS 単位の
  会話ビューを提供します。AS を送出しないエクスポータ（多くの非 BGP 機器）でも自動的に補完します。データ
  セットは Docker 内の更新用サイドカーで最新に保たれ、稼働中のコアへ**再起動なしでホットリロード**されます。
  補完はオプトインです。
- **リモート拠点のポーラでもフローを収集** — 拠点のポーラがローカルのフローエクスポートを受信し、（認証
  付きの）バス経由でコアへ転送できます。ポーリングと同じ分散ポーラ構成でフローが機能します。
- **SNMP トラップの名前解決** — 受信した SNMP トラップ（v1/v2c）が、トラップ OID を**人間可読な名前**に
  解決し、**組み込みのトラップイベントルール**を同梱し、トラップバッジを表示します。トラップが生の OID では
  なく名前付きイベントとして届きます。

### 改善
- **大規模障害時もアクティブアラートが快適** — アクティブアラートのトリアージ一覧を仮想化（ウィンドウ表示）
  しました。数千件のアラートが同時発生しても、全件ではなく画面に見える行だけを描画するため、最も必要な
  ときに画面が重くなりません。
- **フロー補完のリフレッシュが滑らかに** — IP→ASN データセットの定期リロードでコアが一瞬止まることが
  なくなりました（大きなテーブルの解析を非同期ランタイム外で実行）。

## v0.1.14

**マルチコア構成と分散配置のためのハードニング。** セッションがコアのフェイルオーバーを越えて維持される
ようになり、各リモートポーラには自分のプールに必要な資格情報だけを渡せるようになりました — ネットワーク
越しにポーラを配置するサイトのメッセージバスをより堅牢にします。いずれもオプトインで、既定の単一ノード
構成の挙動はこれまでどおりです。

### 新機能
- **セッションがコアのフェイルオーバーを越えて維持（オプトイン）** — 高可用構成では、各 `yagra-core` に
  共有のセッション署名鍵をマウントすると（`YAGRA_SESSION_KEY_FILE`）、ログインがどのコアでも受け付けられ、
  コアの再起動やフェイルオーバーをまたいで有効になります。HA のフェイルオーバーで全員が再ログインを強い
  られることがなくなります。ログアウト、ユーザーの無効化、ロール変更、パスワードのリセットは、すべての
  コアに即時に反映されます。鍵を設定しなければ、セッションの挙動はこれまでどおり（コアごとのトークン）です。

### バグ修正
- **セキュアなリモートバスでメトリクスの後追い補完が動作** — TLS と認証を有効にした NATS バスでは、再接続
  したリモートポーラがバッファ済みメトリクスを publish する権限を持たず、store-and-forward の後追い補完
  （v0.1.13 の新機能）がセキュアなバス上で再送できていませんでした。ポーラが backfill チャネルへ publish
  できるようになり、分断期間のメトリクスが意図どおり埋め戻されます。平文の単一ノードバスには影響ありません。

### セキュリティ
- **メッセージバスのポーラ単位資格情報（オプトイン）** — NATS バスをリモートポーラへ公開する構成で、コアが
  各ポーラに対しそのポーラのプールだけにスコープした資格情報を発行できるようになりました。各ポーラは自分の
  **プール**のジョブと機器資格情報だけを受け取り、全ポーラが 1 つのバスアカウントを共有してフリート全体に
  到達できる従来の状態を解消します。単一のリモートポーラが侵害された場合に見える範囲を大幅に狭めます。バスの
  TLS＋認証設定と併せて有効化してください。既定では無効で、有効化するまで共有アカウントの挙動は変わりません。

## v0.1.13

**リモートポーラがネットワーク分断を乗り越えます。** 中央コアから切り離されたポーラは、ローカルで監視を
続け、回線が戻ったときに収集済みのメトリクスを後追いで補完します。WAN の瞬断が、履歴の「穴」ではなく
埋め戻された「ギャップ」になります。

### 新機能
- **リモートポーラの store-and-forward（既定 ON）** — ポーラがコアへの接続を失っても（WAN 障害や
  ファイアウォールの瞬断）、機器のポーリングをローカルで継続し、結果を破棄せずにバッファします。再接続時に
  まとめて再送し、コアは各メトリクスを**本来のタイムスタンプ**で取り込むため、グラフと履歴は分断期間を
  スパイクなしで埋め戻します。アラートは意図的に再送**しません** — アラート評価は「今」から再開するので、
  再接続時に解決済みの古いアラートが大量発報することはありません。バッファは有界で（メモリ上のリングに
  加え、ポーラ再起動をまたいで残るディスクスピル）、最古のデータから破棄するため、ポーラのディスクを埋め
  尽くすことはありません。上限の調整や無効化は `YAGRA_STORE_FORWARD*` 設定で行えます
  （`docker-compose.poller.yml` を参照）。
- **Pollers ページに「最近の監視ギャップ」** — 設定 ▸ ポーラーに、コアがポーラーとの通信を失っていた
  各期間（どのポーラー・どのプール・いつ・どのくらいの長さ）が一覧表示され、監視が途切れていた時間帯と、
  メトリクスが補完されたことを一目で確認できます。

## v0.1.12

**シングルサインオンとコアの高可用性。** 組織の ID プロバイダでサインインでき、コアを複数台で動かして
1台の障害では監視が止まらないようにできます。

### 新機能
- **SSO（OpenID Connect）でサインイン** — 外部 ID プロバイダ（Google Workspace / Microsoft Entra /
  Okta / Keycloak など）を接続し、ローカルアカウントに加えて組織の SSO でログインできます。設定 ▸ 認証
  でプロバイダ（issuer / クライアント ID / クライアントシークレット / リダイレクト URI / スコープ）を
  登録し、IdP のグループを Yagra のロールにマッピングします。アカウントは初回サインイン時に自動作成され、
  ロールは毎回のログインで IdP に追従します。クライアントシークレットは保存時に暗号化され再表示されません。
  プロバイダを有効化するとログイン画面に「SSO で続行」ボタンが表示されます。
- **コアの高可用性（オプトイン）** — 同一の PostgreSQL / Redis / NATS / VictoriaMetrics に対して複数の
  `yagra-core` を稼働させ、そのうち1台（PostgreSQL のアドバイザリロックで選出されたリーダー）だけが
  スケジューリング・ポーリング・アラートを担当します。残りは待機し、リーダー障害時は数秒で自動的に引き継ぎ
  ます（二重ポーリングや通知の重複はありません）。`YAGRA_ENABLE_HA` で有効化し、新しい `/readyz` レディネス
  プローブで稼働中のコアへ API/web を振り分けます。既定は OFF で、単一コア構成は従来と完全に同一です。

### パフォーマンス
- **ノード詳細を単一の共有タイマーで更新** — ノード詳細画面はこれまで1ノードにつき最大7本の独立した15秒
  更新ループを回していましたが、これを単一の共有 tick に統合しました。ブラウザタブが非表示の間はポーリングを
  停止し、復帰時に即座に更新します。

## v0.1.11

**モバイル WebUI の仕上げ（パリティ第3弾）** — 操作にマウスが必要だった残りの画面をタッチ対応にしました。
ダッシュボードのタイルは長押しで並べ替え、トポロジマップは2本指ピンチでズーム、レポートビルダーは電話幅に
収まるよう再配置します。

### 改善
- **ダッシュボード編集をタッチで操作** — カスタマイズ中、タイルの移動ハンドルを長押しすると並べ替えでき
  （通常のスワイプはボードのスクロールのまま）、編集コントロールのタップ領域も拡大しました。ボードが1列
  全幅に畳まれる電話では、縦積みレイアウトを変えられないリサイズグリップを非表示にしています。デスクトップ
  のマウス編集は従来どおりです。
- **トポロジマップを2本指ピンチでズーム** — 依存関係マップを2本指ピンチでズーム、2本指でパンできるように
  しました（従来のドラッグパン・ホイールズームも維持）。マップ上のズームコントロールもタッチ向けに拡大して
  います。
- **レポートビルダーが電話に収まる** — レポートのタブが潰れず横スクロールし、ビルダーのセクション設定は
  シート内で全幅に縦積みされ、タブとセクション並べ替えコントロールのタップ領域も拡大しました。

デスクトップの表示は変わりません。

## v0.1.10

**モバイル WebUI の仕上げ（パリティ第2弾）** — 電話レイアウトが未対応だった設定 / 管理画面を整えました。
詰まった行アクションは「⋯」メニューに集約し、ユーザー一覧はカード化、横に広いマトリクス / グリッド画面は
列が潰れずスクロールします。

### 改善
- **行アクションを電話では「⋯」メニューに集約** — ユーザー一覧と、複数アクションを持つ設定一覧（通知
  ルーティング、資格情報、監視プロファイル、イベントルール、イベントソース、分類ルール、メンテナンス枠）
  で、カードの編集 / 有効・無効 / 削除を単一のオーバーフローメニューにまとめ、カードが詰まらないように
  しました。デスクトップは従来どおりインラインのアクション行のままです。
- **電話で見やすいユーザー一覧** — 各アカウントを縦積みカードに再構成し、ロール選択とアクションを全幅の
  独立行に配置、ロール選択のタップ領域も拡大しました。
- **ロール / ディスカバリ画面が電話でスクロール** — ロール×権限マトリクスとディスカバリのインポート
  グリッドを、電話幅では先頭列を固定したまま横スクロールできるようにし、列が潰れて操作不能になるのを
  解消しました。
- **トラブルシュートを電話に最適化** — 分析の起動ドロワーを全幅にし、実行コントロールのタッチターゲット
  を小画面で拡大しました。

デスクトップの表示は変わりません。

## v0.1.9

**WebUI のスマートフォン対応、数万ノード規模へのスケール、そして一連のセキュリティ強化** — WebUI が
スマートフォン画面に対応し、インベントリ / トポロジ / ダッシュボードが大規模環境でも快適に動作し
（フリート全体をブラウザに読み込むのではなくサーバ側で検索）、セッション・ログイン・TLS の案内・
メトリクス取り込みが強化されました。

### 新機能
- **WebUI のスマートフォン / モバイル対応** — WebUI がスマートフォンサイズの画面に対応しました。ナビ
  ゲーションドロワーとボトムシートを備えたモバイルアプリシェル、カード形式のテーブル・フォーム・ノード
  詳細、小さな画面でもノード詳細に到達できるペイン切り替え、より見やすいイベント / インターフェース
  表示、モバイル / デスクトップレイアウトを強制する Preferences のトグルを追加しました。デスクトップの
  表示は変わりません。

### 改善
- **数万ノード規模へのスケール** — インベントリツリーを仮想化し、グループのメンバーを遅延読み込み
  （開いていて表示中のグループのみ取得）するようにし、トポロジをサーバ側でページングし、グループごと・
  フリート全体の状態集計をサーバ側で計算するようにしました。ライブなノード状態は、ページ全体の再取得
  ではなくストリームで差分的に届きます。大規模なインベントリでも快適に動作します。
- **インベントリ全体を読み込まないフリート全体検索** — ノード一覧のフィルタと Troubleshoot のスコープ
  ピッカーがサーバ側で検索（デバウンス・上限付き）するようになり、ノードピッカーも同じ入力補完を使う
  ため、ノードを探すのにブラウザがフリート全体を保持する必要がなくなりました。
- **フリート全体で生 ID ではなく人間可読な名前を表示** — 参照されているノード・グループ・プロファイルが、
  Dependencies・分類ルール・Mutes・Maintenance の各画面で名前に解決されるようになり（ホバーで ID を
  コピー可能）、名前解決が先頭ページだけでなくフリート全体をカバーするようになりました。
- **大規模環境でのメトリクス取り込み / ポーリングの高速化と安定化** — ポーリング結果をマッチャーと非同期
  バッチライターで処理し、SNMP テーブルウォークを 1 セッションにまとめ、依存関係の抑制の再スイープを
  差分化し、アラート設定とスイープ仕様の解決を設定世代ごとにキャッシュするようにしました。大規模フリート
  でのスループットが安定します。
- **ノード詳細のインターフェース一覧の高速化** — ノードのインターフェースごとのスループットと状態を、
  インターフェースごとに複数のクエリを投げるのではなく、ノード全体で一定数の時系列クエリで読み込むように
  しました。多ポートのスイッチでもインターフェース一覧が格段に速く開き、更新も軽くなります。
- **チャート / ヘルス画面の応答性向上** — インターフェース・混雑リンクのヒートマップ・ホストリソースの
  各チャートが独立した系列を並行取得し、URL 監視プローブがプールされた HTTP クライアントを再利用する
  ようになり、これらの画面のレイテンシが短縮されました。
- **エッジ取り込みの既定上限を引き上げ** — syslog / SNMP トラップの送信元ごと・全体のレート制限の既定値
  を引き上げ（毎秒 200 / 5000 メッセージ）、シャーシ規模のイベント量に対応できるようにしました。
- **レポートビューアと確認ダイアログの改善** — レポートビューアがアプリ標準のダイアログ（キーボード
  フォーカス処理、モバイルではボトムシート）になり、レポート・テンプレート・スケジュールの削除がブラウザ
  ネイティブのプロンプトではなくアプリのテーマ付き確認ダイアログを使うようになりました。
- **コンテナのヘルスチェック** — コアイメージが本物のヘルスチェックで稼働状態を報告するようになり、web
  コンテナはコアが応答可能になるまで起動を待つため、コールドスタート時の初回リクエストエラーを避けられ
  ます。

### バグ修正
- **余白と強調の一貫性** — 一部の WebUI スタイルが未定義の余白トークン（余白がつぶれて表示される）や
  スケール外のフォントウェイトを参照していました。これらを共通トークンに統一し、余白と強調が一貫した・
  テーマ対応の表示になるようにしました。
- **チャートの系列色がテーマに追従** — 時系列チャートの色をテーマの系列パレットから取得するようにし、
  ライト / ダークモードに追従し、ステータス（警告 / 重大）色を流用しないようになりました。

### セキュリティ
- **セッションを失効でき、有効期限が付くように** — ユーザーの無効化・降格・パスワードリセット・削除が、
  そのアカウントの有効なセッションを即座に無効化するようになり、セッションにアイドル期間と絶対的な有効
  期限が付きました。新しいログアウトエンドポイントは現在のトークンをサーバ側で失効します。従来はベアラー
  トークンがコア再起動まで有効で、侵害されたアカウントに対する管理操作が発行済みトークンを遮断できません
  でした。
- **ログインのブルートフォース対策** — ログインエンドポイントに、失敗の繰り返しに対するアカウントごとの
  指数的ロックアウトと、全体の試行レート上限を導入し、パスワード推測攻撃と、パスワードハッシュを繰り返し
  計算させる CPU 枯渇の経路を抑制します。
- **取り込み時のメトリクス名の検証** — ポーラーから届くメトリクス名を、時系列ストアに入る箇所で再検証する
  ようにし、不正なメトリクス名が余計な系列やラベルを注入できないようにしました（カーディナリティ / データ
  整合性の保護）。
- **WebUI / API の TLS 案内** — WebUI イメージとデプロイに、HTTPS を終端する方法（コンテナ内の TLS
  サーバブロックのオプトイン、または外部リバースプロキシ）を記載しました。ログインパスワード・ベアラー
  トークン・投入されたデバイス認証情報が、信頼できるネットワークの外に平文で露出しないようにするため
  です。既定は LAN / プロキシ配下向けの平文 HTTP のままで、これは案内と設定のみの変更です。

## v0.1.8

**検索可能なパッシブイベントログストア、Events とダッシュボードの強化、SSRF 対策、大規模環境の高速化** —
受信した syslog メッセージと SNMP トラップを検索可能なログストアに保存できるようになり、Events 画面に
フィルタが加わり、ダッシュボードウィジェットのリサイズと高さ変更に対応し、URL 監視プローブを SSRF に対して
強化し、ノード一覧とトポロジが大規模環境で高速に読み込まれるようになりました。

### 新機能
- **全文検索対応のパッシブイベントログストア** — 受信した syslog メッセージと SNMP トラップを、非同期の
  バッチ書き込みで専用のログストア（VictoriaLogs）に保存し、Events 画面から全文検索できるようになりました。
  パッシブイベントが一時的なものではなく、保持・検索可能になります。
- **Events 画面のフィルタ** — イベントを時間範囲・イベント種別・ルールに一致したか・全文 / 正規表現検索で
  絞り込めるようになり、多数のイベントの中から目的のものを見つけやすくなりました。
- **リサイズ・高さ変更が可能なダッシュボードウィジェット** — ダッシュボードウィジェットが、ウィジェット
  ごとの段階的な高さ（編集モードの高さセレクタ付き）と、スナップ整列・隙間のないグリッド上での角ドラッグ
  リサイズに対応しました。編集は Cancel で破棄でき、ウィジェットの内容はリサイズ後のセルいっぱいに広がります。

### 改善
- **大規模環境でのノード一覧 / トポロジの高速化** — ノード一覧とトポロジ画面で、アラートエンジンが未観測の
  ノードのフォールバック状態を、ノードごとに 1 クエリではなく単一の時系列クエリで導出するようになりました。
  大規模なインベントリや、コア再起動直後（エンジンがまだ各ノードの状態を把握していないとき）に特に効果が
  あります。
- **データベース接続プールの調整可能化** — コアの PostgreSQL 接続プールサイズを環境変数
  `YAGRA_PG_MAX_CONNECTIONS` で設定できるようになり、既定値を引き上げました。数万ノード規模でスケジューラ・
  結果取り込み・API を律速していた同時実行の上限が緩和されます。
- **ノード詳細の表示高速化** — ノードの SNMP スカラー値を逐次ではなく並行して読み込むようになり、スカラー
  メトリクスの多いデバイスで Overview タブの表示が速くなりました。

### バグ修正
- **ダッシュボードチャートのちらつき** — フィルモード（高さ追従）のダッシュボードチャートで発生していた
  ResizeObserver 起因のちらつきを修正しました。
- **UI テキストサイズの一貫性** — 残っていた WebUI のテキストサイズ（未定義のスタイルトークンを参照して
  意図しないサイズで表示されていたものを含む）を共通のサイズトークンに統一し、一貫した・テーマ対応の
  サイズで表示されるようにしました。

### セキュリティ
- **URL 監視の SSRF 対策強化** — HTTP(S) 監視プローブが、初回のターゲットだけでなく**すべてのリダイレクト
  ホップ**についても、SSRF フィルタ付きリゾルバ経由でのみ名前解決・接続するようになりました。これにより、
  ループバック / リンクローカル / クラウドメタデータのブロックリストを回避できた 2 つの経路を塞ぎます。
  監視対象のエンドポイントがプローブをブロック対象アドレスに解決されるホスト名へリダイレクトする経路と、
  事前チェックを通過しつつ実際にはブロック対象アドレスへ接続される DNS リバインディングです。プライベート /
  内部レンジは引き続き許可されます（NMS は正当にそれらを監視するため）。

## v0.1.7

**ホストリソース傾向、より広い多言語対応、大規模運用とアップグレードの快適化** — System Health で
コアと全ポーラの CPU / ロード / メモリ / ディスクを時系列表示できるようになり、WebUI の日本語対応が
さらに広がり、ポーリングスケジューラと停止処理を大規模運用とクリーンな再起動に向けて強化しました。

### 新機能
- **System Health のホストリソース傾向** — System Health ページで、**コアと各ポーラ**の **CPU・ロード・
  メモリ・ディスク**を時系列で表示できるようになりました。現在値のスナップショットだけでなく、リソース
  逼迫の推移を確認できます。

### 改善
- **英語 / 日本語ローカライズの拡充** — 残りの WebUI 領域（アプリの骨格＝トップバー・サイドバー・
  パンくず、アラートのトリアージ、共通のノードピッカーと時間範囲コントロール、および残りの画面）を
  翻訳しました。日本語に切り替えたときにカバーされる範囲が大きく広がります。未翻訳のテキストは英語に
  フォールバックします。
- **ナビゲーションの再編** — サイドバーを **Monitor**（監視）と **Configure**（設定）に分類してラベルを
  分かりやすくし、サードパーティの **Integrations** はカタログにまとめました。
- **グレースフルシャットダウン** — コアとポーラが `SIGTERM` / Ctrl-C（`docker compose` の再起動や
  ローリングアップグレードなど）でクリーンに停止し、処理中の作業と処理中の API リクエストをドレインして
  から終了するようになりました。
- **大規模環境でのスケジューリング / 取り込みの高速化** — ポーリングスケジューラが各プールのワーキング
  セットを並行して構築し、通知配信を結果取り込みパスから分離しました。通知先が遅くてもメトリクス取り込み
  が止まらなくなり、数万ノード規模でのスループットが向上します。

### バグ修正
- **UI のサイズ / スタイルの一貫性** — 存在しないスタイルトークンを参照して意図より少し大きく表示されて
  いた WebUI の一部のテキストサイズとモーダルのスタイルを修正しました。

## v0.1.6

**多言語 WebUI と SNMPv3 ノードのインタフェース単位メトリクス** — インターフェース言語を
Settings ▸ Preferences から英語と日本語で即座に切り替えられるようになり、SNMPv3 で監視する
デバイスでも v2c と同じインタフェース単位のメトリクスを収集できるようになりました。

### 新機能
- **インターフェース言語の切り替え（英語 / 日本語）** — 新しい **Settings ▸ Preferences ▸ Language**
  で UI 全体の言語をその場で切り替えられます（リロード不要）。既定は英語で、選択はブラウザごとに
  記憶されます。アプリの骨格（上部ナビゲーション、サイドバー、パンくず、サインイン、Preferences と
  About 画面）と共通の値フォーマット（日時、「5分前」などの相対時刻、ノード状態・HTTP ステータスの
  ラベル、証明書有効期限の表示）が翻訳済みで、日本語リソースは切り替え時のみ遅延読み込みされます
  （既定の英語パスに追加コストなし）。残りの画面は順次翻訳中で、未翻訳のテキストは英語にフォール
  バックするため空欄になりません。
- **SNMPv3 ノードのインタフェース単位メトリクス** — SNMPv3（USM）ノードでも v2c と同様に GETBULK で
  インタフェーステーブルをウォークし、インタフェース単位のカウンタを収集します。従来 v3 ノードは
  スカラーメトリクスに限られていました。

## v0.1.5

**分散トレーシング（OpenTelemetry）** — Yagra が OpenTelemetry トレースを送出できるようになり、
1 回のポーリングを中央 core とポーラをまたいでエンドツーエンドで追跡できます。オプトインで既定は
無効（構造化ログと Prometheus メトリクスは従来どおり）なので、コレクタに向けるまでオーバーヘッドは
ゼロです。

### 新機能
- **OpenTelemetry 分散トレーシング（自己監視）** — `YAGRA_OTEL_ENDPOINT`（または標準の
  `OTEL_EXPORTER_OTLP_ENDPOINT`）に OTLP/HTTP コレクタを設定すると、core とポーラが 1 回の
  ポーリングを 1 本のトレースに繋ぐ span（core の dispatch → ポーラの poll → core の ingest）と、
  北向き API リクエストごとの span を送出します。トレースコンテキストはジョブと結果に載ってバスを
  流れるため、1 本のトレースが分散ポーラをまたぎ、ローリングアップグレード中の古いポーラとも互換
  です。大規模時は毎回トレースする代わりに `OTEL_TRACES_SAMPLER=parentbased_traceidratio`
  （+ `OTEL_TRACES_SAMPLER_ARG`）でサンプリングします。`docker compose --profile tracing up` で
  同梱の Jaeger が起動し、ローカルでトレースを閲覧できます。エンドポイント未設定なら Yagra は従来
  どおりログのみで、トレーシングのオーバーヘッドはありません。**DEPLOYMENT.ja.md ▸ 分散トレーシング
  （OpenTelemetry）** を参照。

### セキュリティ
- **推移的アドバイザリの修正** — `crossbeam-epoch` を 0.9.20 に更新し RUSTSEC-2026-0204 を解消
  （メトリクス依存内で null ポインタを debug 整形した時のみ到達する無効ポインタ参照。Yagra では
  発生しませんが予防的に適用）。

## v0.1.4

**依存関係マネジメント** — アラート抑制を駆動する依存グラフを、閲覧するだけでなく編集できるように
なりました。ノードの上流を WebUI から設定でき、依存抑制が親ダウンのイベントに反応するようになった
ため、親より先に落ちた子は単独で通知される代わりに親の下へ巻き取られます。

### 新機能
- **ノードの依存関係を WebUI から管理** — ノードの上流（親ダウン抑制と根本原因ロールアップを駆動
  するエッジ）を、ノード作成後でも設定・変更・解除できるようになりました。ノード詳細ヘッダに
  **Dependency…** アクションが追加され、新しい **Topology ▸ Dependency view** ページで各ノードの
  上流・現在のステータス・根本原因の帰属を一覧し、各エッジをその場で編集できます。自己依存と循環は
  拒否されます。Network map は既にこれらのエッジを可視化していましたが、本リリースで定義できるように
  なりました。

### バグ修正
- **順序が前後した障害でも依存抑制が漏れない** — 根本原因の帰属がイベント駆動になりました。従来は子が
  最初にダウンした瞬間にのみ親への帰属を計算していたため、親より*先*に落ちた子は巻き取られず単独で
  通知され続けていました。本リリースでは、親がダウンすると既にアクティブな下流アラートを再評価して
  親のインシデントへ巻き取り（単独通知はクローズ）、逆に親が復旧しても子がまだダウンしている場合は
  その子を単独で再通知します。

## v0.1.3

**分散ポーラプール** — 監視対象の近くに配置したポーラへポーリングを分散できるようになりました。
各ノードは **pool** 属性（サイトやリージョン）を持ち、中央サーバがポーラの死活を追跡し、プール内の
ノードを生存ポーラへ一貫ハッシュで割り当て、各ポーラに担当分（working set）をバス経由で配布します。
リモートポーラは中央の NATS バスへアウトバウンド接続する（NAT／ファイアウォール越しに強い）ため、
拠点セグメントを inbound 開放なしで監視できます。本リリースには受動イベント監視、トポロジマップ、
ノード単位イベント表示も含まれます。

### 新機能
- **分散ポーラプール（ロケーション親和割り当て＋フェイルオーバー）** — リモート拠点にポーラを配置し、
  **pool** 属性でノードを割り当てられます（既定は単一プール `default`）。中央コーディネータがポーラの
  ハートビートを監視し、プールの担当を生存ポーラへ一貫ハッシュリングで分配し（ポーラの増減で入れ替えが
  最小になります）、離脱したポーラの担当を同一プールの生存ポーラへ自動的にフェイルオーバーします。
  ポーラは割り当てを working-set スナップショットとして受け取り、以降は差分を適用し、再接続時はフル
  再同期する（再起動をまたいでポーリングの欠落なし）方式です。新しい **Settings ▸ Pollers** ページで
  各ポーラの pool・ステータス・バージョン・working-set サイズ・最終ハートビートを一覧でき、ノードは
  あるが生存ポーラがいないプールを警告し、リモートポーラの設定を生成する **Register poller** ダイアログ
  を備えます。生存ポーラのいないプールは従来の中央ポーリングにフォールバックするため、アップグレードは
  シームレスです。リモート拠点向けの `docker-compose.poller.yml` と、信頼境界を越えてバスを公開する
  ための NATS TLS ＋認証設定を同梱します。
- **受動イベント監視（syslog / SNMP トラップ / Webhook）** — 能動ポーリングに加え、syslog（RFC
  5424/3164）と SNMP v1/v2c トラップを受信し、inbound Webhook を受け付け、運用者定義のルール（部分
  一致／正規表現）で照合してアラートを生成し、発生／解消のライフサイクルを PagerDuty（Events API v2）
  と Jira Service Management（Alerts API）へ転送できます。
- **トポロジ Network map** — 依存グラフの新しい可視化ビュー。ノード同士の関係と、親障害が子へどう
  波及するかを俯瞰できます。
- **ノード単位のイベント表示** — ノード詳細ページに **Events** タブが追加され、Events ページを単一
  ノードに絞り込めます（ノードピッカーと `?node_id=` ディープリンク付き）。イベントログの取得・
  ページング・カラムは両ビューで共有されます。
- **Alerts ▸ Events のフリーテキスト検索** — 任意の検索文字列でイベントログを絞り込めます。

## v0.1.2

**読み取り専用** の Dashboard API による Cisco Meraki 監視。Meraki 機器は SNMP/ICMP ホストと同じ
HostTree に通常のノードとして現れますが、収集は **組織（Organization）単位** で行われ（1 回の
ページング付き組織一括 API 呼び出しで多数の機器をカバー）、大規模な Meraki 環境でも組織の API
レート制限に触れません。さらに、この統合が Meraki に対してできるのは *読み取り* だけです。

### 新機能
- **Cisco Meraki（Dashboard API）監視** — **Settings ▸ Integrations ▸ Cisco Meraki** から読み取り
  専用 API キーで Meraki 組織を追加できます（1 つのキーで複数組織を一括登録可能）。インポート
  ウィザードが組織のネットワークと機器を列挙し、監視対象を選択できます。取り込んだ機器は通常の
  ノードとなり、**組織 → ネットワーク** のグループツリー下に自動配置され、インベントリツリー上で
  **Meraki** バッジが付きます。収集メトリクス（死活、WAN アップリンクの損失／遅延／ステータス、
  クライアント数、トラフィック使用量）は、ノード詳細ページの新しい Cisco Meraki カードに表示され
  ます。組織単位で、収集の一時停止／再開、ティア別のポーリング間隔とレート予算の調整、監視対象
  ネットワークの編集ができます。

### セキュリティ
- **設計上の読み取り専用と多層セーフガード** — この統合が Meraki に発行するのは HTTP **GET のみ**
  で（設定の書き込みは一切行いません）、すべてのリクエストは許可リストに登録された Meraki API
  ホストに限定され、API キーが他所へ送られることはありません。キーは **保存時に暗号化** され（API
  で返却されず、ログにも出力されません）、ポーリングは組織単位の API 上限より十分低いレートに抑えて
  利用者自身のツール用の余裕を残し、Integrations ページの **Meraki ポーリング全体キルスイッチ** で
  すべての Meraki 収集を即座に停止できます。

## v0.1.1

初回リリースに続く小規模アップデート: 専用の About ページ、Pollers から分離した自己健全性ページ、
そして完全に非 root で動作する WebUI コンテナ。

### 新機能
- **Settings ▸ About** — 製品情報と稼働中のバージョンを 1 か所にまとめた新しいページ。稼働中の
  Core / API バージョン（新しい公開エンドポイント `GET /api/v1/version` 経由）、WebUI ビルド
  バージョン、リポジトリリンク、ライセンスを表示します。両方のバージョンを並べて表示することで、
  ローリングアップグレード中の core/web のずれが一目で分かります。

### 改善
- **System Health が独立したページに** — Yagra の自己健全性（ポーリングループのカウンタ、バックエンド
  サービスの到達性、データ網羅率）を Settings ▸ Pollers から専用の Settings ▸ System Health ページに
  移動しました。Pollers の項目は、将来の分散ポーラ設定用のプレースホルダとして予約されています。

### セキュリティ
- **WebUI コンテナが完全に非 root で動作** — web イメージを `nginx-unprivileged` ベースにし、非 root
  ユーザーで動作するようにしました。そのためサーバは 80 ではなくコンテナポート **8080** で待ち受け
  ます。同梱の Docker Compose ファイルはこれに合わせて設定済みのため、標準的なデプロイには影響しません
  （WebUI は既定でホストポート 3000 のまま）。web コンテナのポート 80 を参照していた独自のリバース
  プロキシやオーケストレーションは更新してください。

## v0.1.0

Yagra の初回リリース — ネットワークデバイスやサーバを ICMP / SNMP / API コールで監視する
ネットワーク監視システム（NMS）です。数万ノード規模と分散ポーリングを最初から見据えて設計され、
Rust バックエンドと React WebUI を Docker コンテナとして提供します。

### 新機能
- **監視・ポーリング** — ICMP 死活、SNMP v2c / v3（USM 認証・暗号）のスカラ／テーブル収集
  （マルチインデックステーブルを含む）、TLS 証明書の有効期限追跡付き URL / HTTP(S) エンドポイント
  監視。生カウンタを保存し、レートはクエリ時に算出（ラップ／リセット対応）。グローバルおよび
  Device Profile 単位のポーリング間隔を設定でき、間隔のジッタとデバイス単位のレート制限／
  バックプレッシャを備えます。
- **探索・分類** — IP レンジ／リストに対するネットワーク探索（保存済み資格情報のピッカー付き）、
  sysObjectID / sysDescr による自動デバイス分類で該当する Device Profile を適用、保存済み資格情報を
  参照で試行する Credential Finder（デバイス単位でレート制限、試行値はログに残さない）。
- **デバイスプロファイル・収集** — ロール × ネットワーク OS のプロファイル分類体系、編集可能な
  Collection Templates・Classification Rules・厳選した MIB/OID カタログ（いずれも検索可能）。主要
  ベンダー（Cisco、Huawei — USG ファイアウォールセッションや RAM を含む、Meraki MX/MS、A10）と
  標準のホスト／インタフェース MIB を標準収録。
- **アラート** — 明示的な状態機械に、ドウェル時間ヒステリシス、フラッピング検出、ルート原因への
  ロールアップを伴う依存抑制（親ダウン ⇒ 子を抑制）、メンテナンスウィンドウ、ミュート、
  dedup・グルーピングを備えます。追記専用のアラート履歴はノード／チェック名を人間可読で表示し、
  「What」（メトリクス＋条件）列を持ち、PagerDuty / Jira Service Management など外部ツールからの
  確認応答を読み取り専用で反映します（ADR-015）。メール・Webhook 通知チャネルはリトライ付き。
- **ダッシュボード・可視化** — 管理者がカスタマイズできる Shared Dashboard と、ユーザーごとの複数
  ボード対応 My Dashboard を、ウィジェットライブラリから構築: フリート健全性タイムライン、Top-N
  CPU／メモリ／インタフェース、トラフィック差分、合計スループット、使用率ヒートマップ、地理マップ、
  依存／ルート原因トポロジ、データ網羅率ゲージ、アラート履歴集計、ポーラ／収集の健全性。分割表示の
  ノード詳細を持つノードツリー、インタフェース行内のスパークライン、共有レンジコントロール
  （1h–7d ＋カスタム）と設定帯域の基準線付き時系列グラフ。
- **レポート** — カスタマイズ可能なレポートセクションを HTML / CSV / PDF で出力し、定義の保存と
  スケジュール実行に対応（Dashboard ▸ Reports）。
- **トラブルシュート** — VictoriaMetrics 上で非同期の分析ジョブ（例: 異常検知）を実行する分析
  ツールカタログ。単一ノードまたはグループ（再帰的）にスコープできます。
- **管理** — ロールベースアクセス制御（viewer / operator / admin）とグループスコープの可視性、
  ユーザー管理、すべての設定変更とログインを記録する不変の監査ログ、Yagra 自己健全性監視
  （Settings ▸ Pollers と `GET /api/v1/system-health`）。
- **WebUI** — 単一の型付き REST クライアント上の React + TypeScript SPA。SSE によるライブ更新
  （ベアラー認証）、ライト／ダークテーマ、数万ノードを見据えた仮想化・サーバページングのリスト。
- **デプロイ** — 単一ノードの Docker Compose スタック（Yagra-core / Yagra-poller / Yagra-web /
  PostgreSQL / Redis / NATS / VictoriaMetrics）。非 root イメージで、raw-socket ICMP 用の
  `CAP_NET_RAW` はポーラのみに付与。expand-contract のデータベースマイグレーションでアップグレード時の
  データを保全します。

### セキュリティ
- 監視用の資格情報（SNMP コミュニティ、SNMPv3 USM 資格情報、API トークン）はエンベロープ暗号で
  保存時に暗号化します — AES-256-GCM のデータ鍵を、マウントしたシークレットファイルから読み込む
  鍵暗号化鍵（KEK）でラップします。シークレットはログ出力・API 応答・メトリクス／トレースラベルに
  決して含めません。
- 状態を変更するすべての API エンドポイントは認証と RBAC を強制し、監査ログに記録します。
- URL モニターと通知 Webhook からの外向き HTTP は SSRF を防止します: ループバック、リンクローカル、
  クラウドメタデータ宛先を拒否します（IPv4 マップ IPv6 形式や HTTP リダイレクト経由を含む）。一方で、
  正当なプライベート／内部レンジは監視可能なままです。
- URL モニターの TLS 証明書検証は既定で有効で、無効化はモニター単位の明示的なオプトインです。
