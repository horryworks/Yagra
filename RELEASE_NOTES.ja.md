# リリースノート

<!-- ## Unreleased は、出荷済みだがまだバージョンの付いていない挙動変更の置き場です。
     Yagra は main への push ごとにテストサーバへデプロイされるため、変更はリリースのタグ付けより
     ずっと前に稼働に入ります。**変更を出荷するのと同じコミットで**ここに項目を足してください —
     レスポンスの形、ステータスコード、既定値、クエリの意味、削除した挙動など、運用者や API
     クライアントが気づきうるものすべてです。リリース時に `/docs` がこの節を新しい `## v<x>` に
     畳み込み、空の Unreleased を残します。空であるのが平常状態で、節自体が無い場合は誰かが
     この仕組みを消したということです。 -->

## Unreleased

### 破壊的変更
- **オペレーターが監視を動かせるようになりました。**`ManageConfig` は約 100 本のエンドポイントを
  まとめて守っており、「ノードを 1 台足す」も「TLS 証明書を差し替える」も同じ 1 つでした。それが
  管理者限定だったため、オペレーターはノードを追加することも、しきい値を編集することも、探索を
  実行することも、デバイスプロファイルを変えることもできませんでした。これを 2 つに割りました。
  **「監視の管理」（従来の `manage_config`）と「認証情報の管理」はオペレーター以上**が持ちます。
  新設した**「デプロイの管理」（`manage_system`）は管理者限定**で、デプロイそのものを変えるもの・
  データが箱の外へ出るものを担当します — 通知の配信とルーティング、転送、TLS 証明書、
  アップグレード、保持期間、AI プロバイダ、設定バンドル、サポートバンドル、ポーラーの削除。
  - **オペレーター権限でできることが広がります。**アップグレード前に、オペレーターのアカウントを
    見直してください。閲覧者と管理者は変わりません。
  - `設定 ▸ ロールと権限`に新しい行列が出ます。サーバーから導出しているので、**API が実際に
    強制しているものと必ず一致**します。
  - OpenAPI 文書の `403` の説明を両方向に修正しました。オペレーターが到達できるようになった
    エンドポイントに「管理者未満」と書いてあったもの、`ManageSystem` が必要になったのに
    `ManageConfig` と名乗っていたものの両方があります。

### 新機能
- **インターフェースのグラフを大きくしました。大きさは自分で決められます。**`ノード ▸ <ノード> ▸
  Interfaces` のスループットとエラーのグラフは高さ 132px 固定で、製品内で 2 番目に小さく、傾向を
  読み取るには小さすぎました（issue #65）。通常の画面で**およそ 2 倍の高さで開く**ようになり、
  グラフの入った領域の**上端にドラッグできる帯**が付きました。上へ引けばグラフが高くなり、下へ
  引けばインターフェース一覧が広くなります。帯はキーボードでも操作でき（フォーカスして `↑` /
  `↓`）、ダブルクリックで既定の高さに戻ります。上の一覧は必ず数行残るので、グラフが画面を占有する
  ことはありません。スマートフォンでの表示は変わりません。グラフは元から縦に並び、領域はスクロール
  します。
- **WebUI の設定が、別のマシンにも付いてくるようになりました。**グラフの高さを、設定した
  ブラウザだけでなく**アカウントに紐づけてサーバーに保存**します。別のマシンからサインインしても、
  別のブラウザでも、デプロイが `http://…:3000` から `https://…` へ移ったあとでも、同じ高さで
  開きます。裏側は新設した 2 本のエンドポイント `GET` と `PUT /api/v1/preferences` で、
  **アカウントごとに 1 個の不透明な JSON** を保持します。サーバーは保存するだけで中身を読まないので、
  **次の設定を足すときに API の変更もマイグレーションも要りません**。設定は完全にアカウント単位です
  — API トークンは拒否し（`403 session_required`。トークンは人を名指ししません）、公開ダッシュ
  ボードのデプロイでは匿名の閲覧者も拒否します。共有された 1 行は「全員の設定」になってしまうため
  です。この機能より前のコアは `404` を返し、WebUI は何も言わずにブラウザ側の値を使い続けます。

### 改善
- **押せないボタンを描くのをやめました。**WebUI の書き込み操作 — すべての「+ 追加」「編集」
  「削除」「保存」— は、表示するかどうかを「**サインインしているか**」で決めていました。
  「**その操作をしてよいか**」ではありません。そのため Viewer に「+ 期間を追加」「+ ミュートを
  追加」が見え、Operator には管理者専用の「+ 追加」が、一覧を誰でも読める画面すべてで見えて
  いました。サーバーはすべて拒否していたので起きてはいけないことは起きていませんが、運用者は
  押してみるまで分かりませんでした。今後は**その操作自体が要求する権限**（API が確かめるのと
  同じもの）を見て表示を決め、権限が無ければ描きません。パネル全体が操作そのものである画面
  （設定バンドルのエクスポート／インポート、監視の既定値の 3 フォーム）では、サインイン済みの
  利用者に「サインインしてください」と言う代わりに、必要な権限の名前を出します。
- **拒否のときに必要な権限の名前を出します。**これまでは「この画面を見る権限がありません」と
  だけ表示していましたが、8 つの権限のどれが必要かを添えるようにしました。文言はサーバーの
  カタログから取っています — `設定 ▸ ロールと権限`が表示しているものと同じです。権限による拒否を
  「利用できません」や読み込みエラーとして表示していた 3 画面（`設定 ▸ AI 分析`、
  `設定 ▸ API トークン`、`設定 ▸ 監査ログ`）もこの表示に揃えました。
- **共有ダッシュボードの「カスタマイズ」は、無効化ではなく非表示にしました。**変更できない
  アカウントには理由をツールチップで出していましたが、タッチ端末はツールチップを表示しません。
  スマートフォンでは「壊れているボタン」に見えていました。
- `設定 ▸ ポーラー ▸ ポーラーを登録`に「設定の管理」権限が必要になりました。同じ画面の他の操作に
  揃えたものです。これまではサインインしていれば誰でも開けました。

### バグ修正
- **画面を開いただけで、フリート全体の再解決が走らなくなりました。**`/api/v1` への書き込みが
  成功すると「監視設定が変わった」という印が立ち、これが 4 つのバックグラウンド処理にキャッシュの
  作り直しを指示します。設定を一切変えない 7 本の経路がこの印を立てていました。最も影響が大きかった
  のは `POST /api/v1/node-names` — ノード名を表示する表が**描画のたびに**呼ぶ、ID を名前に直す
  問い合わせです。つまりページを開く／再読み込みするだけで、全ノード走査、ノードごとのポーリング
  仕様の再構築（認証情報の復号を含む）、アラート設定の再読込、ポーラー担当数の数え直しが走り、
  5 分以内にトポロジの再導出まで走っていました。**作り直した結果は常に正しいため、失敗もログも
  出ません。**無駄な CPU としてのみ現れ、しかも大規模なフリートでしか目に見えませんでした。
  除外したのは次の 7 本です。
  - `POST /api/v1/node-names` — ID を表示名に直すだけ。POST なのは ID の配列がクエリ文字列に
    収まらないからです。
  - `PUT /api/v1/dashboard` と `PUT /api/v1/shared-dashboard` — ウィジェットの配置は表示状態です。
    追加・移動・サイズ変更・設定変更のたびに保存されるので、1 回の編集作業で上記が何十回も
    繰り返されていました。
  - `POST /api/v1/event-rules/test`、`POST /api/v1/llm/test`、
    `POST /api/v1/settings/ldap/test`、`POST /api/v1/meraki/orgs/discover` — 「保存する前に試す」
    ボタン群です。フォームを詰めている人が繰り返し押します。
    `POST /api/v1/notification-channels/preview` は**まさにこの理由で既に除外済み**でしたが、
    この 4 本は列から漏れていました。

  **同じ間違いが戻らないよう検査を追加しました。**書き込みメソッドで登録された経路のハンドラが
  読み取り権限しか要求しておらず、かつ除外リストに無ければビルドが落ちます（`node-names` が
  まさにこの形でした）。ただし上の設定テスト 4 本は捕まえられません — 何も書かないテストでも
  「監視の管理」権限は正当に必要だからです。あの 4 本は**動いているデプロイを実測して**
  見つけました。
- **ノードツリーの右クリックメニューがオペレーターに戻りました。**前のリリースで、メニュー全体を
  「一番きつい項目が要求する権限」で閉じてしまっていたため、メンテナンス期間を開いたりノードを
  ミュートしたりできるアカウント — その 2 つはメニューの中で管理作業ではない項目です — に
  メニューが 1 つも出ませんでした。各項目が自分の要求する権限を見るようにし、**1 つでも出せる
  項目があればメニューを開く**ようにしました。管理者では出ていたので一貫して見え、意図的な仕様に
  見えていたものです。
- **権限の無い画面が「0 件」と言うのをやめ、権限が無いことを言うようにしました。**Viewer で
  サインインして`ノード ▸ 認証情報とシークレット`を開くと「**認証情報はまだありません**」と
  表示されていましたが、そのデプロイには実際には 2 件ありました。一覧の取得が `403` で拒否され、
  画面には「このデプロイに管理機能が無い」場合の分岐はあっても「あなたには見せられない」場合の
  分岐が無く、拒否がそのまま空のテーブルとして描かれていたためです。**失敗が成功の形をして
  出てくる**ので、テストもログも画面を見た目も、本当に 0 件の場合と区別できませんでした。
  同じコードを 16 画面が共有していて、拒否を扱えていたのは 1 つ（ユーザー）だけです。16 画面
  すべてを 1 つの共有処理に通し、**新しい画面が同じことを書いたらビルドが落ちる**ようにしました。
  Viewer でも押せて送信時に初めて失敗していた「+ 認証情報を追加」ボタンも同時に消えます —
  塞がれた画面はツールバーごと描かなくなったためです。
- **v0.2.9 のリリースノートの訂正。**「認証情報を`ノード`へ移したので、Viewer には**権限が無い
  項目として見える**ようになる」と書きましたが、これは*そうあるべきだった挙動*の説明でした。
  実際に起きていたのは上記の「空リスト」です。移動がこの不具合を作ったわけではなく（何リリースも
  前から存在していました）、設定の奥からノードの 2 番目へ引き出した、というのが正確です。

## v0.2.9 — メニューが各画面の役割を名乗り、受動監視に専用のタブができる

このリリースの変更は、すべて WebUI のナビゲーションと文言です。バックエンドは動いていません —
API エンドポイントの形は変わらず、データベースのマイグレーションも走らず、v0.2.8 のポーラーは
そのまま動きます。**画面 4 つのアドレスが変わりますが、旧アドレスはクエリ文字列ごとリダイレクト
します**ので、ブックマークもリンクもそのまま使えます。

### 改善
- **受動監視に、専用の上部タブ「Events（イベント）」を作りました。**受信と閲覧は`アラート`の下、
  中継だけが`設定 ▸ 転送`の下にあり、この一連の流れを 1 つとして名指すものがメニューに
  ありませんでした。画面 4 つが移動しますが、**旧アドレスはすべてリダイレクトし、クエリ文字列も
  保持します** — ブックマークした `/alerts/events?node_id=…` は、これまでどおりそのノードの
  イベントを開きます:

  | 以前 | これから |
  |---|---|
  | `アラート ▸ イベント` — `/alerts/events` | `イベント ▸ イベント` — `/events` |
  | `アラート ▸ イベントソース` — `/alerts/event-sources` | `イベント ▸ Webhook 送信元` — `/events/webhooks` |
  | `設定 ▸ 転送` — `/settings/forwarding` | `イベント ▸ 転送` — `/events/forwarding` |
  | `設定 ▸ 認証情報とシークレット` — `/settings/credentials` | `ノード ▸ 監視の組み立て ▸ 認証情報とシークレット` — `/nodes/credentials` |

  `イベントのアラートルール`は**意図的に「アラート」の下に残しています**。アラートを作るもの
  だからで、ルール 2 画面をタブ 2 つに割ってしまうと、今回直している問題を繰り返すことになります。
- **`イベントソース`を「Webhook 送信元」に改称しました。**扱えるのが Webhook 送信元だけだからです。
  説明文には「syslog と SNMP トラップの受信口」と書かれていましたが、この画面が持っていたのは
  ずっと Webhook の送信元だけでした。その説明が本当に答えていた問い —「syslog はどこへ送るのか」
  — には、イベント画面の側で答えるようにしました（下記）。
- **認証情報を「ノード」へ移しました。機器に入るための鍵であって、サインインの話ではないからです。**
  従来は`設定 ▸ アクセス`にあり、ユーザー・ロール・認証（＝**人**が Yagra にサインインする話）と
  同じ棚に並んでいました。⚠️ この画面には引き続き **ManageCredentials** 権限が必要なので、
  Viewer アカウントでは「ノード」タブの中に権限のない項目が 1 つ見えるようになります（以前は
  設定の奥にあって目立ちませんでした）。
- **メニュー項目 4 つを、中身が分かる名前に変えました。**`アラート ▸ アラートルール`は
  **メトリクスのアラートルール**に、`アラート ▸ イベントルール`は**イベントのアラートルール**に
  なりました。どちらもアラートを作るものですが、旧称は一方を出力で、もう一方を入力で名付けて
  いたため、何のためのルールなのかがどちらからも読み取れませんでした。`設定 ▸ システムヘルス`は
  **Yagra ヘルス**に、`設定 ▸ システム設定`は**監視の既定値**になりました。隣り合う 2 項目が
  どちらも「システム」で始まりながら、「Yagra 自身は大丈夫か」と「フリートをどう監視するか」
  という別々の問いに答えていたためです。URL は変わっていないので、ブックマークはそのまま使えます。
- **`ノード ▸ 監視設定`を「監視の組み立て」に改称し、項目を作る順に並べ替えました。**
  MIB リポジトリ → メトリクスセット → デバイスプロファイル → 分類ルールの順です。以前は
  デバイスプロファイルが先頭にありながら、その説明が「メトリクスセットをここに取り付けます」—
  つまり 2 つ下の項目を先に作れ — と言っていました。
- **イベント画面に、どこで受信待ちしているかを表示するようにしました。**タイトルの下の 1 行に、
  バインド済みの syslog・SNMP トラップの宛先と、それを持っているポーラーが出ます。「syslog を
  送ったのに何も見えない」に、それを尋ねる画面の上で答えられます。受信口が 1 つも無いときは、
  有効にするための環境変数名を代わりに表示します。新しく収集しているデータはありません —
  `設定 ▸ ポーラー`が既に表示していたものと同じです。
- **アクティブアラートの ACK フィルタに、ACK がどこから来るのかを書きました。**ACK は
  オンコールツール（PagerDuty / JSM）から届くもので、Yagra が自分で付けることはありません。
  従来はページタイトル下のカッコ書きにしか書かれておらず、ACK 操作を探しに来た人が見る場所では
  ありませんでした。フィルタ側で説明するようになったので、タイトルのカッコ書きは外しました。
- **共有ダッシュボード・マイダッシュボード・環境設定にも、他の画面と同じ 1 行の説明**を付けました。
  その変更が誰に影響するかを書いています。
- **上部バーの検索ボックスの表示を「ノードを検索…」に変えました。**元々ノードしか検索しませんが、
  入力を始めるまでそれが分かりませんでした。
- **`アラート ▸ 通知ルーティング`を「通知の配信」に改称し、ルール 2 画面の下へ移しました。**
  ネットワーク監視製品で「ルーティング」と言えばパケットの経路のことで、実際 `監視の既定値` の
  探索走査には「ルーティング隣接 (OSPF / BGP / 経路)」があります。1 つの語に 2 つの意味を
  持たせていました。**設定**グループの並びも作業の順になりました — 何が発報するかを決め
  （**メトリクスのアラートルール**と**イベントのアラートルール**。アラートが生まれる 2 通りの
  道なので隣に並べます）、次に誰に届くかを決め、最後にいつ黙らせるかを決めます。
- **`設定 ▸ バージョン情報`を「個人設定」グループから「システム」グループの末尾へ移しました。**
  アカウントではなくデプロイについての情報だからです。
- **サイドバーを折りたたんだとき、2 文字の略号ではなく実際の項目名を表示するようにしました。**
  従来は 52px 幅に `Ms`・`Mb`・`Cl`・`Pr` が並ぶだけで、すでにメニューを覚えている人にしか
  読めませんでした。つまり、それを必要としない人にしか読めなかったということです。幅は 120px に
  なり、ラベルは 2 行まで折り返します。そのぶん折りたたみで空く幅は 168px から 100px に減ります。
  グループ見出しは折りたたみ時に消えますが、**項目**が略号になることはもうありません。
- **メニュー項目すべてに 1 行の説明を付けました。**従来は 42 項目中 21 項目に説明がなく、
  しかも説明が無い側に、互いに紛らわしい組み合わせが含まれていました。
- **`トラブルシュート ▸ 診断ツール`の 15 種類を、メトリクス・受動イベント・トラフィックフロー・
  ストア横断の 4 群に分けて表示するようにしました。**従来はカードが 1 面に平積みで、端から端まで
  読まないと目的のものが見つかりませんでした。

## v0.2.8 — フィルタ行は、求められるまで出てこない

### 改善
- **列フィルタ行を、求められるまで表示しないようにしました。**一覧の上のツールバーにある
  **「フィルタ」**を押すと出て、もう一度押すと消えます。開いた／閉じたは画面をまたいで、また
  再読み込みをしても覚えています。この行は v0.2.7 ですべての一覧に行き渡り、そのぶん絞り込みを
  使っていない画面でも常に帯を占めていました。**絞り込みが効いている間は行は出たままで、ボタン
  では閉じられません** — 行が減っているのに、その原因の操作部が画面に無い状態は、節約できる
  高さより悪いためです。したがって、絞り込みの付いた共有リンクを開くと、それを作った行が
  最初から見えています。戻すにはボタンの隣の**「すべての絞り込みを解除」**を使ってください。
  スマートフォンでは何も変わりません。「フィルタ」ボタンは今まで通り同じ下シートを開きます。
- **「フィルタ」と「すべての絞り込みを解除」のボタンの高さを、隣の操作部に揃えました。**どちらも
  スマートフォン用のタップ領域（44px）のままで、同じツールバーに並ぶ 30px のピッカーや検索
  ボックスより 1.5 倍高くなっていました。スマートフォンではどちらもタップ領域を保ちます。そこでは
  このボタンが絞り込む唯一の手段だからです。また、絞り込みが効いていて行を閉じられない間は
  「フィルタ」ボタンが淡く表示されるようになりました。以前はカーソルの形が変わるだけで、
  反応していないように見えていました。
- **ノード ▸ ディスカバリの「Seen on network」で、既定の表示のまま「すべての絞り込みを解除 (1)」が
  出ることがなくなりました。**この表は意図的に未監視のエンドポイントだけに絞られた状態で始まり
  ますが、ボタンはその既定値を「操作者が設定した絞り込み」として数えていました。そのため押しても
  何も変わりませんでした。実際に何かを変えたときだけ出るようになりました。
- **ノード ▸ デバイスプロファイルで、スマートフォンの「フィルタ」シートにも、デスクトップの
  フィルタ行と同じ選択肢の件数が出るようになりました。**「ポーリング間隔」の *継承* /
  *上書き* に、シートでは数字が付いていませんでした。デスクトップの行には付いており、2 つの面に
  別々の件数が渡されていたためです。今はどちらも同じ 1 か所を読みます。
- **アラート ▸ アラートルールの「Dwell」列を「連続検出回数」に改めました。***dwell* はレーダーや
  通信の用語で、ネットワークの現場でこの値を指す言葉ではありません。実際に表しているのは
  「アラートを出すまでに、境界を超えた読み取りが連続して必要な回数」なので、列見出し・ルール追加
  ダイアログの入力欄・説明文をその言い方に揃えました。API と設定バンドルの `dwell_samples` は
  変わりません。
- **アラート ▸ 履歴とアラート ▸ アラートルールで、`!` `~` `\` で始まる検索語を含む保存済みリンクの
  書き方が変わりました。**この 2 画面（および設定 ▸ 監査ログ、トラブルシュート ▸ すべての検出）が、
  ほかの一覧と同じ 1 つのフィルタ表現を使うようになりました。そこでは先頭の `!` が「除外」、
  先頭の `~` が「正規表現」を意味します。したがって、その文字で始まる語にはバックスラッシュが
  付きます（`!core` は `?node_q=%5C%21core`）。**変更前に保存したリンクも開けます**が、その形の語は
  残りの部分の検索として読まれます（`?node_q=!core` は `core` を検索します）。ほかの保存済みリンクに
  影響はなく、絞り込みの動作そのものは変わりません。

### バグ修正
- **5 つの画面が、スマートフォンでもデスクトップ用の列フィルタ行を描いていました。** 本来それを
  置き換えるはずの「フィルタ」ボタンと同時に表示されていました。ノード詳細の **Interfaces**
  タブではさらに、その行が一覧の途中に貼り付いていました。列見出しの下端に貼り付く設定で、
  モバイル表示では見出しが隠れるためです。結果としてインターフェースの行がその上の隙間を
  通り抜けてスクロールしていました。**Collection** タブ、**ノード ▸ ディスカバリ**の 2 つの表、
  **監視 ▸ プロファイル**でも同じ行が出ており、操作部が読めないほど細いか、絞り込む列とずれた
  位置にありました。5 つとも、ほかの画面と同じく「フィルタ」シートだけを出すようになりました。
  デスクトップで絞り込めてスマートフォンで絞り込めない画面はありません。
- **インターフェースを選ぶと、フィルタ行の下に隠れることがありました。** Interfaces タブは選んだ
  行を固定ヘッダから 32px 空けていましたが、v0.2.6 でヘッダの下にフィルタ行が入って以降、その
  32px は全体の高さではなくなっていました。

## v0.2.7 — すべての画面がその列で絞り込め、イベント検索は何に当たったかを示す

### 破壊的変更
- **イベント画面の絞り込みをツールバーから列見出し直下のフィルタ行へ移しました。**ツールバーは
  操作行になります。各列が自分の条件を持ちます。「種別」と「結果」は件数付きの複数選択、
  「メッセージ」と「送信元」は部分一致・正規表現（メッセージのみ）・除外を選べる条件、「日時」は
  従来のプリセットで、開始／終了の指定は「期間を指定…」の下に入りました。送信元とメッセージの
  **どちらか**に当てていた単一の検索窓は UI から無くなりました。どの列を指すのかを選んでください。
  API の `q` と `regex` は変更なく、MCP の `search_events` ツールでは引き続き `search` です。
- **イベントの「すべてのイベント／ルールに一致／一致なし」の選択を「結果」列の複数選択に
  置き換えました。**ルールが一致したかどうかだけでなく、ルールが**何をしたか**を選べます。
  `GET /api/v1/events` と `GET /api/v1/events/stats` の `matched=true|false` は変更ありません。
  同じ絞り込みは `action=fired,refreshed,cleared,suppressed` にあたります。
- **イベントの絞り込みはすべて URL に入ります。**絞り込んだ画面をそのまま共有できます。既定値の
  絞り込みはキー自体を持たないため、`/alerts/events` だけの URL は常に既定の画面になります。
- **レポートの「保存済み」は、レポート一覧から選ぶ方式から名前で絞り込む方式になりました。**
  実行結果は実行した時点のレポート名を保持し、列に出るのもその名前です。ID の一覧に今の名前を
  貼って並べると、**改名前に実行した行を黙って落とす**ことになります。改名したレポートの履歴は
  どちらの名前でも探せます。
- **API トークンの検索窓 1 つが「名前」と「所有者」の 2 つに分かれました。**「所有者が alice」で
  絞ったときに、alice という**名前の**トークンまで出ることはなくなります。ノード詳細 ▸ Neighbors
  の検索窓 1 つも、リンクの両端ごとに 3 つへ分かれました。
- **アラート履歴・監査ログ・アラートルール・保存済みの所見の 4 画面で、列の絞り込みが複数選択に
  なりました。** これまでは単一選択でした。`GET /alerts/history`（`severity`・`state`）、
  `GET /audit`（`action`・`status`）、`GET /thresholds`（`scope_level`・`direction`）、
  `GET /analysis/findings`（`tool`・`severity`）が、`GET /events` と同じくカンマ区切りの集合を
  受け取ります。単一値の動作は従来どおりなので、保存済みのリンクや既存のクライアントはそのまま
  動きます。未知のトークンは黙って対象を広げず 400 を返し、1 つの集合は最大 32 個までです。
  MCP の `get_alert_history` / `get_audit` / `search_analysis_findings` も同じ引数を取ります。
- **フロー系エンドポイントの絞り込みが複数値を取るようになり、解釈できない値は無視ではなく 400 に
  なりました。** `/flow/*` と `/nodes/{id}/flow/*` の全 12 本で、`proto`・`port`・`peer`・`asn` が
  カンマ区切りの集合（最大 8 個）を受け取ります（`proto=6,17`・`port=80,443`）。
  ⚠️ **アップグレード前に確かめるのは拒否のほうです。** `port=not-a-port` のような要求は、これまで
  **絞り込まれていない上位 N** を返していました — 誰も聞いていない質問への答えです。今後は
  `invalid_filter` を返します。壊れた絞り込みが黙って捨てられることに頼っていたクライアントは
  エラーを受け取るようになりますが、それが狙いです。そのクライアントは求めた行を一度も
  受け取っていませんでした。MCP の `top_flows` / `flow_fanout` は変更なしで、値は 1 つずつです。
- **`GET /api/v1/audit` の `action`・`status` と `GET /api/v1/thresholds` の
  `scope_level`・`direction` が、OpenAPI 文書上は列挙型ではなく文字列になりました。** 複数値を
  運ぶために必要な変更です。受け付ける語彙は変わっておらず、未知の値は今までどおり拒否します。
  変わったのは拒否の返り方だけで、パラメータの平文拒否ではなく通常のエラー本体になります。
- **メンテナンス期間・ミュート・分類ルール・ポーラー・認証情報の検索窓 1 つが、列ごとの絞り込みに
  置き換わりました。同じものの置き換えではありません。** これらの検索窓はどれも複数の項目を同時に
  見ていて、どれを指しているのか言えませんでした — ポーラーでは `0.2` が「v0.2.4 で動いている
  ポーラー」と「`site-0.2` という名前のプールのポーラー」の両方に当たっていました。これからは
  指したい列で絞ってください。認証情報の検索窓は名前と認証情報 ID を一緒に見ていましたが、
  こちらは 2 つの列に分かれました。
- **同じ置き換えが残りの画面にも行き渡りました**: アクティブアラート、Troubleshoot ▸ 実行、
  ユーザー、ノードツリー、ノード詳細の Interfaces と Collection、ディスカバリの 2 つの表、
  メトリクスセット、デバイスプロファイル。どの画面でも、複数の項目を同時に見ていた検索窓 1 つが
  列ごとの操作に変わりました。列見出しを持たない一覧（アクティブアラート・実行・ユーザー・
  ノードツリー）では、同じ操作に名前を付けて一覧の上に並べています。**いずれも 1 つしか選べな
  かったものが複数選べるようになりました。**
- **`GET /api/v1/nodes` の `state`・`kind`・`pool` がカンマ区切りの集合を取るようになりました。**
  MCP の `list_nodes` も同じです。単一値の指定は今までどおり動きます。
  `state=warning,critical,unreachable` は「正常でないものすべて」で、これまではツリーを 3 回
  見る必要がありました。未知の `state`・`kind` トークンは黙って広げず 400 で拒否します。
  ⚠️ **`pool` だけは照合しません** — プール名は運用者が付けるものなので、知らない名前は
  エラーではなく単に「一致なし」になります。
- **`GET /api/v1/nodes` の `state`・`kind` が、OpenAPI 文書上は列挙型ではなく文字列に
  なりました。** 監査ログの `action`・`status` と同じ理由（複数値を運ぶために必要）で、
  受け付ける語彙は変わっていません。
- **ノード詳細 ▸ Collection の「届いているものだけ」チェックボックスが、3 値の状態フィルタに
  なりました。** チェックボックスでは「届いていないものを隠す」しか言えず、「設定済みなのに
  届いていないものだけ見せて」——収集セットが止まったときに知りたいこと——が言えませんでした。
  「収集中」と「未設定」を両方選ぶと、以前のチェックボックスと同じ意味になります。
- **ディスカバリの観測された端点にある「未監視のものだけ」チェックボックスが 2 値の
  フィルタになりました。** 「すでに取り込んだ端点だけ」をそのまま指定できます。この表は
  これまでどおり「未監視」で開きます。
- **スキャン結果の「応答したものだけ」チェックボックスは、代わりを置かずに削除しました。**
  そもそも表示どおりの意味ではありませんでした — 掃引したアドレスは **ICMP に答えたか、
  SNMP で機種情報を返したか**のどちらかで候補になるので、「応答なし」は実際には
  **「SNMP には答えたが ping には答えない」**、つまり ICMP を遮断している機器のことでした。
  実在はしますがごく稀で、フィルタの幅に収まる正直なラベルが付けられません。機種情報の有無は
  Identity 列がすでに分けており、各行の `ping` バッジも残っています。
- **デバイスプロファイルは、検索窓から役割で引くことができなくなりました。** 役割は行を束ねる
  見出しであって列ではないので、表の上に専用の操作を置いています。検索窓と違い、
  こちらは複数の役割を同時に選べます。

### 新機能
- **アラート履歴をノード名・メトリクス名・確認の有無で絞り込めるようになりました。**
  `GET /api/v1/alerts/history` と MCP の `get_alert_history` ツールが `node_q`（ノードの現在の
  名前の部分一致。ノードを 1 つだけ指す `node_id` とは別物です）、`metric`（メトリクス名の
  部分一致）、`acked`（`true` / `false`）を受け取ります。「今週発生して誰も確認していないものは
  どれか」が、目で追わずに絞り込み 1 つで出せます。
  ⚠️ 履歴が大きいデプロイでは、**絞り込みが効く語ほど遅くなります**。索引は並び順とページ数を
  支え続けるので、1 ページ分がそろうまで索引を辿ります。100 万行に 1 件しかないメトリクスでは
  索引を最後まで歩くことになり、よくあるメトリクスならすぐに止まります。
- **保存済みの所見をノード名と所見の内容で絞り込めるようになりました。**
  `GET /api/v1/analysis/findings` と MCP の `search_analysis_findings` ツールが `node_q` と `q`
  を受け取ります。`q` はメトリクス名**または**所見の種別に当たります — What 列が並べて描いている
  2 つです。フリート全体の所見はノードを持たないので、`node_q` には決して一致しません。
- **保存済みの所見をスコアで絞り込めるようになりました。**
  `GET /api/v1/analysis/findings` と MCP の `search_analysis_findings` ツールが `min_score` と
  `max_score` を受け取ります。**どちらも境界を含み、片側だけの指定もできます** — 「スコア 60
  以上」は範囲ではなく下限 1 つです。WebUI の絞り込み行にとって初めての数値フィルタなので、
  所見の他の列より 1 増分遅れて入りました。
- 手書きの表だった一覧がすべて仮想スクロールになり、列の絞り込み行が付きました。対象はメンテナンス
  期間・ミュート・MIB リポジトリ・分類ルール・イベントソース・イベントルール・通知ルーティング
  （2 つの表）・認証情報・ポーラー・メトリクスセットです。**2 つの画面は意図的に手書きの表のまま
  にしました**: 行が役割ごとの見出しで束ねられているデバイスプロファイルと、展開した行の中にある
  メトリクス編集欄（仮想スクロールの行の中に仮想スクロールの一覧を入れることになるため）です。
- **Troubleshoot のレポート 3 つに列の絞り込み行が付きました。** ルール未整備（rule gap）は
  ソース種別・シグネチャ・件数・検出元で絞れます。スキャン検知は 7 列すべてに絞り込みが付き、
  「宛先を 500 以上触った送信元」を 1 つの操作で出せます。認証試行は送信元アドレス・重大度・
  失敗回数のフィルタバーが付きました。重大度は多選択になったので「重大と警告」を選べるように
  なり、`情報` も選べるようになりました。残る 12 のレポートのチップは**意図的にそのまま**です —
  あれは行の属性ではなく診断の切り口（`慢性` と `断続` など）を選ぶもので、共通のフィルタに
  畳むと意味が変わってしまいます。
- `GET /api/v1/events` と `GET /api/v1/events/stats` に任意の絞り込みを 5 つ追加し、MCP の
  `search_events` ツールも同じ 5 つを受け取ります。`action` と `severity`（`kind` と同じく
  カンマ区切りの集合）、メッセージだけを対象にする `msg` + `msg_regex` + `msg_not`、イベントの
  送信元 IP または送信元ノード名を対象にする `src` + `src_not` です。集合に未知のトークンが
  含まれる場合は黙って検索を広げず 400 を返します。1 つの集合は最大 32 個までです。
- `kind` が複数値を受け取れるようになりました（`kind=syslog,trap`）。単一値の動作は従来どおりです。
- `GET /api/v1/system-health` に **`search_semantics`**（`prefix` / `substring`）を追加しました。
  このデプロイで検索語がどう一致するかを表します。VictoriaLogs 構成では語の先頭から一致するため
  `POLICY` では `POLICYPERMIT` が見つかりますが、`PERMIT` では見つかりません。イベント画面は
  それを絞り込みの隣に表示し、0 件のときも「このフィルタに一致するものがありません」ではなく
  その状況を名指しします。

### 改善
- **Troubleshoot の「保存済み分析結果」を「すべての検出結果」に変えました。** 旧名は存在しない
  操作を約束していました。検出結果は分析が完了した瞬間に書き込まれるので、保存する手順も押す
  ボタンもありません。この画面には全実行の検出結果が、その実行が保持期間で削除されるまで
  出ます（Settings ▸ 保持期間の「診断」、既定 90 日）。変えたのは表示名だけで、URL・
  `GET /api/v1/analysis/findings`・スキーマ名 `SavedFinding` はそのままです。
- **認証情報の種別フィルタが、表にあるすべての種別を選べるようになりました。** これまでは 3 種類
  しか並んでおらず、`http_auth` の認証情報や、運用者ではなく連携機能が作る Meraki API キーは、
  一覧に出ているのに絞り込めませんでした。
- 表の作り直し後も、無効な通知チャネルとルーティングルールは薄く表示されます。
- **ノードツリーを状態・種別・プールで絞ると、フォルダも絞られ、自動で開くようになりました。**
  この 3 つはサーバ側で効くためツリーからは見えず、Critical を選んでも**該当ノードを持たない
  フォルダまで全部残り**、畳んだフォルダは一致行を隠したままでした。ツリーに「絞り込み中」を
  伝えていたのは検索ボックスだけだったためです。
- **絞り込み中は、フォルダのバーと件数が画面に出ている行を表すようになりました。**
  1 行しか無いのに「DNS 3」と出ていては、どちらが答えなのか読み手が考えることになります。
  ブラウズ時は従来どおりサーバの集計です — まだ開いていないフォルダが本当の規模を示せるのは
  そちらだからです。
- **メトリクス編集欄の追加フォームを、メトリクス一覧の下へ移しました。**「新しいメトリクス」の
  見出しが付きます。一覧の上に入力が並ぶ形は、今や他のすべての画面でフィルタ行の見た目です。
  そのためフィルタと読まれ、メトリクス名を打っても下の一覧は何も変わりませんでした。代わりに
  フィルタを足すことはしていません — メトリクスセットは最大 11 個・平均 3〜4 個で、3 行の表に
  フィルタは釣り合わないためです。
- 除外は部分一致と正規表現の両方で使えます。出荷前に実イベント 670 万件で計測しており、除外の
  コストは除外しない形と同じでした（両方のストア、両方のモードで）。
- **ログストア構成では、検索語が語の先頭から一致するようになりました。** `POLICY` で
  `POLICYPERMIT` が見つかります（これまでは語全体が一致する必要がありました）。出荷前に実イベント
  6,695,066 件で計測しています。語の先頭はストアの索引で解けるため、語全体を指定した場合とほぼ
  同じコストです（24 時間で 0.12 秒に対し 0.06 秒）。一方、語の**途中**に当てる形は期間内を
  すべて走査するため約 15 倍かかります。だからそちらは「正規表現」スイッチの側に残しています。
- **検索語が 1 件も見つからなかったときは、語の途中まで含めてもう一度だけ探し直し、そうしたことを
  画面に表示します。** 重いほうの検索は、軽いほうが行き止まりだった場合にだけ実行されます。
- **メッセージのうち絞り込みに一致した部分を強調表示します。** 一覧・全文パネル・モバイルカードの
  すべてで有効です。除外条件のときは何も強調しません（画面に出ている行は「一致しなかった行」なので）。
- **メッセージの全文をパネルで開けるようになりました。** マウスを置くと開き、クリックすると固定されて
  文字を選択・コピーできます。これまでのブラウザ標準のツールチップは、表示が遅く、コピーできず、
  いちばん必要な長い行ほど途中で切られていました。
- **「すべての絞り込みを解除」がデスクトップにも付きました。** 操作行に、解除する件数つきで出ます。
  何かで絞り込んでいるときだけ現れ、イベント画面ではノードの選択も一緒に解除します。
- イベントの 0 件表示を 3 つに分けました。期間内に無い場合、フィルタに一致しない場合、そして
  **語の先頭が**一致しない場合です。既定の期間が絞り込む側であるため、「イベントがありません」は
  そもそも正しい文ではありませんでした。
- **さらに 12 画面の絞り込みを列のフィルタ行へ移しました。** API トークン、転送、依存関係、
  ノード詳細 ▸ Neighbors、Troubleshoot ▸ スケジュール、レポートの 3 つの表、アラート履歴、
  監査ログ、アラートルール、保存済みの所見です。どの画面にも「すべての絞り込みを解除」と
  モバイル用のシートが付き、多くの画面には今まで無かった条件が増えました。トークンの最終利用日、
  依存関係一覧のノードの状態、保存済みレポートの実行契機などです。
- **1 つの列に 2 つのことを詰めていた表 2 つを、2 列に分けました。** 転送の「宛先」は
  アドレスと**宛先種別**を積んでいたため、種別の絞り込みに置き場がありませんでした。有効・無効も
  名前の脇のバッジから独立した列になりました。
- **依存関係の「全ノード / 上流あり / 抑制中」の 3 択が 2 つの絞り込みに分かれました。** もともと
  別々の列についての質問が 1 つの操作に同居していたもので、両方を同時に指定できるようになります。
- 一覧をすべてブラウザに持っている画面では、選択肢の件数が「**そちらに切り替えたら何件になるか**」
  を表します。その列自身の絞り込みは除いて数えており、表計算ソフトのオートフィルタと同じ読み方です。

### バグ修正
- **表示領域より広い表が、収まらない列を隠さず横スクロールするようになりました。** 設定 ▸ ポーラー
  ／転送／API トークンの 3 画面は、幅 1280px の画面が与えられる幅より多くの列幅を宣言しています
  （API トークンでは 354px 超過）。はみ出した分は切り落とす箱の外に描かれていたため、操作列は
  存在するのに到達できず、スクロールバーも出ないのでそのことが分かりませんでした。同じはみ出しが
  1 列目を自分のパディングの幅まで押し潰しており、名前が表示されず、絞り込みボタンが 14px 幅で
  読めなかったのもこれが原因です。もともと収まっている表は変わらず、スクロールバーも出ません。
- **通知チャネルの種別の表記が、どこでも同じになりました。** アラート ▸ ルーティングの種別列の
  絞り込みは生のトークン（`pagerduty`／`jsm`）を表示し、その上のダイアログは製品名を表示して
  いました。2 つが別々のリストだったためです。
- **列の絞り込みが、開いた直後から入力を受け付けるようになりました。** これまではクリックが 2 回
  必要でした。小窓は開くものの、その中の入力欄をもう一度クリックしないと文字が入りません。小窓は
  位置を測る 1 フレームのあいだ隠れた状態で描かれ、隠れた要素にはフォーカスを当てられないため、
  コードが要求していたフォーカスが黙って捨てられていました。今は小窓の入力欄にカーソルが入ります。
  テキストの絞り込みなら語の欄、フローの絞り込みなら値の欄、数値の範囲なら下限の欄、選択肢が多い
  列なら検索欄です。打つ場所が無い小窓（期間のプリセット、選択肢の少ない列）は、勝手に選択肢を
  選ばずフィルタ欄にフォーカスを残します。モバイルの絞り込みシートは従来どおりです。全列を一度に
  表示するため、キーボードを渡すべき欄がありません。

## v0.2.6 — 機器のすべてのチェックが再び回り、22 の一覧が絞り込める

**⚠️ v0.2.3 / v0.2.4 / v0.2.5 をお使いの場合は、できるだけ早くアップグレードしてください。** この
3 つのリリースは、1 台の機器につき**チェックを 1 本しかポーリングしません**。SNMP ノードでは
システムスカラーだけが届き続け、インターフェース walk・ベンダーの機器状態表・トポロジ walk、さらに
は ICMP までが毎周期捨てられます — **SNMP の収集が黙って止まり**、しかも何も知らせません。生き残る
チェックが**そのまま死活監視のチェック**だからです。なぜ気づけないのか、そしてなぜ**規模が小さい
ほど**重く出るのかは、バグ修正の 1 件目に書きました。設定を変える必要はありません。修正はポーラー
側にあり、アップグレードがそのまま対処になります。

残りは一覧の絞り込みです。22 の画面にフィルタが 1 つもありませんでした。インシデント対応で最初に
開く 2 画面（Alerts ▸ Active と Alerts ▸ History）も、そして「一致が出ないこと」が利便性ではなく
正しさの問題になる画面 — Settings ▸ Audit — も含みます。Audit は取得済みの行だけを絞っていたため、
それより古い一致を黙って隠していました。

### 破壊的変更
- **イベントログの既定の期間が「全期間」から「過去 24 時間」になりました。** 開始/終了の指定は、
  新しい期間ドロップダウンの「期間を指定…」の下に移動しています。これは下の検索の変更を支払うための
  変更です。期間を区切らない検索では、大文字小文字を区別しない検索は区別する検索の約 10 倍
  （テスト環境の 670 万件で 9.4 秒）かかりますが、24 時間に区切れば約 1.1 倍で済みます。「全期間」は
  ドロップダウンから 1 クリックで選べます。API は変更ありません — `GET /api/v1/events` の
  `start`・`end` は今までどおり任意で既定は無制限なので、期間を送っていない API クライアントの
  挙動は変わりません。
- `GET /api/v1/system/upgrade`（および `get_system_health(section="upgrade")`）に
  **`updater.installed`** が増えました。この環境にアップグレード機構がそもそもあるかどうかを表します。
  `updater.present` より先に読んでください。`present` は「アップデータが報告したか」だけを意味する
  ようになり、`installed` が true のときにしか意味を持ちません。
- 機構を持たない環境へのアップグレード要求は、`upgrade_unavailable` ではなく
  **`upgrade_unsupported`**（503）を返すようになりました。この画面の 503 は 3 種類に分かれます:
  `upgrade_unsupported`（機構が無い）、`upgrade_disabled`（機構はあるがオフ）、
  `upgrade_unavailable`（機構はあるがアップデータが応答しない）。機構を持たない環境への
  イメージアーカイブのアップロードも、`upgrade_unavailable` から `upgrade_unsupported` に変わります。

### 新機能
- **Nodes ▸ All nodes を状態・種別・ポーリングプールで絞り込めるようになりました。** ツリーで検索
  できたのは名前とアドレスだけだったので、「どの URL モニタが落ちているか」「tokyo プールは実際に
  何を監視しているか」に、ノードを並べているその画面が答えられませんでした。3 つの絞り込みは URL に
  載るので共有できます。`GET /api/v1/nodes` が `state`・`kind`・`pool` を受け取り、MCP の
  `list_nodes` も同じ 3 つを受け取ります。意味について 2 点補足します。`pool` は**実効プール**です
  — ノード自身が指定していればそれ、なければ最も近い祖先フォルダの指定、それも無ければ `default`。
  格納された列だけで絞ると `default` に一致するノードが 1 つも見つからず、フリートの大半は実際そこに
  属しているためです。そして 3 つとも SQL の `WHERE` には書けません。状態はアラートエンジンの中に
  あり、種別はどの補助テーブルに行があるかから導出され、プールは継承されるからです。他の画面が使う
  のと同じ解決器で、最大 5,000 件の候補走査に対して適用し、その上限に達した場合はレスポンスの新しい
  `truncated` で知らせます。件数が少ない答えが「これで全部か」を自分で言うようになりました。
- **Reports ▸ 保存済みレポートを絞り込めるようになりました。** レポート・実行状態・自由文で絞れます。
  `GET /api/v1/reports/runs` は API クライアント向けに `definition_id`・`state`・`since` を受け取り、
  MCP の `get_report_runs` も同じ 3 つを受け取ります。画面自体はブラウザ側で絞ります — この一覧は
  SSE で届くので、絞り込んで取得しても次の進捗フレームで元に戻ってしまうためです。レポートでの
  絞り込みは名前ではなく定義の ID を使うので、レポートを改名しても過去の実行履歴が切り離されません。
- **Settings ▸ API トークンを列でソートできるようになりました。** ヘッダをクリックでソート、もう
  一度クリックで逆順。状態の列は五十音順ではなく深刻度順に並ぶので、使えないトークンが一方の端に
  まとまります。有効期限の無いトークンはどちらの向きでも末尾に置かれ、逆順にした途端に画面の先頭を
  埋めることはありません。共有のテーブルコンポーネントにこの操作子を足しましたが、**行の並べ替えは
  意図的に行いません** — そうしないと、大きな一覧をページ送りしている画面が、たまたま読み込み済みの
  ページだけを並べ替えて「これが順序です」と示してしまうためです。
- **Alerts ▸ ルールをデータベース側で絞り込めるようになりました。** この表はノード数とともに増える
  唯一の設定テーブルで（ノード単位の上書きはノード × メトリクスの数だけ増えます）、だからこそ以前から
  500 件で打ち切り「M 件中 N 件」と表示していました。フィルタは 1 つもなく、しかもブラウザ側で絞ると
  その先頭 500 件だけを見ることになります。「cpu_util のルールを見せて」が新しい 500 件だけを調べて
  報告する、という状態です。`GET /api/v1/thresholds` が `q`（メトリクスの部分一致）・`scope_level`・
  `direction` を受け取り、`total` は絞り込みに一致した件数を数えるので「M 件中 N 件」が正しいまま
  になります。絞り込みは URL に載るので共有できます。
- **Troubleshoot ▸ 実行を絞り込めるようになりました。** 分析・実行状態・自由文（対象と要約）で
  絞れます。`GET /api/v1/analysis/jobs` は API クライアント向けに `tool`・`state`・`since` を
  受け取ります。画面自体はブラウザ側で絞ります — この一覧は SSE で届くので、絞り込んで取得しても
  次の進捗フレームで元に戻ってしまうためです。
- **さらに 13 画面を絞り込めるようになりました。** Alerts ▸ メンテナンスウィンドウとミュート、
  Alerts ▸ ルーティングの 2 つの表、Settings ▸ 転送・API トークン・ポーラー、Nodes ▸ ディスカバリの
  2 つの表、Troubleshoot ▸ スケジュール、レポート画面のテンプレートとスケジュールのタブ、ノード詳細の
  インターフェース・隣接ノード・収集の各タブ、そしてウィジェットカタログです。それぞれに検索ボックスと、
  その画面で実際に聞かれる 1〜2 個のフィルタ（状態・種別・有効無効・プロトコル・プール）を付けました。
  いずれもブラウザ側で絞りますが、これは正当です — どの一覧も、ノード数ではなく運用者が設定した数で
  件数が決まるからです。2 点だけ補足します。ディスカバリのエンドポイント表は、取り込み済みのものを
  以前からずっと無条件に隠していて、そのことが画面上のどこにも書かれていませんでした。これを操作子に
  したので、同僚が取り込んだために消えたのか、見えなくなったのかを区別できます。もう 1 つ、ウィジェット
  カタログの検索は内部の識別子ではなくカードに表示されている語を照合するので、日本語でも同じように
  使えます。
- **Events の検索が大文字小文字を区別しなくなりました。** ログストア（ADR-024）を使う構成では
  プレーンな検索語が大文字小文字を区別して照合されていたため、`SSH` で `ssh` が見つかりませんでした。
  一方ログストアを使わない構成では PostgreSQL の `ILIKE` がそもそも区別しないので見つかりました。
  同じ検索が、どちらのストアで動いているかによって別の答えを返していたことになります。プレーンでも
  正規表現でも、両方とも区別しないようになりました。差は 1 つだけ残していて、これは意図的です —
  ログストアは単語単位で照合し、PostgreSQL は部分一致します。単語の途中から始まる部分一致は転置
  インデックスでは全ブロック走査でしか答えられず、約 300 倍かかって VictoriaLogs の 30 秒上限に
  達するためです。検索ボックスの正規表現トグルがその逃げ道で、こちらはどちらのストアでも単語の
  内側に届きます。
- **Alerts ▸ Active を絞り込めるようになりました。** 障害時に最初に開く振り分け用の画面なのに、
  フィルタが 1 つもありませんでした。大規模障害では数千行が並び、読む手段はスクロールだけでした。
  重大度・ノードの状態・外部のオンコールツールで ACK 済みかどうか・自由文（ノード名、ノードの ID、
  発生したメトリクス）で絞り込めます。絞り込みは URL に載るため、リロードしても消えず、同じ障害を
  見ている相手にそのまま貼れます。History や Audit と違いこちらはブラウザ側で絞りますが、これは
  意図した選択です。アクティブなアラートは SSE ですべてブラウザに載っているので、一致した行が
  ページ境界の向こうに隠れることがありません。ダッシュボードのアラートウィジェットは変わりません。
- **Alerts ▸ History を絞り込めるようになりました。** この画面にはフィルタが一切なく、増える一方の
  追記専用ログをスクロールで読む以外に手段がありませんでした。重大度・状態・発生/解消・ノードまたは
  フォルダグループ・期間で絞り込めます。いずれもデータベース側で適用されるので、画面に読み込み済みの
  ページではなく**ログ全体**に効きます。絞り込みは URL に載るため、リロードしても消えず、リンクとして
  共有でき、特定ノードの履歴を直接指すこともできます。`GET /api/v1/alerts/history` は `severity`・
  `state`・`resolved`・`node_id`・`group_id`・`since`・`until` を受け取り、MCP の
  `get_alert_history` も同じ経路で同じ組を受け取ります。自由文検索は**意図的に入れていません** —
  自由文を当てられる列はメトリクス名だけで索引が無く、検索するとページごとに全表走査になるためです。

### 改善
- **Settings ▸ System health が、ホストを 1 台ずつではなく並べて比較できるようになりました。**
  ホストリソースは core と全ポーラーのドロップダウンだったため、あるポーラーが忙しいかどうかを
  core や同じプールの他のポーラーと比べるには、選び直して覚えておくしかありませんでした。今後は
  core で 1 セクション、ポーラープールごとに 1 セクションに分かれ、セクション内の全ホストが同じ
  CPU・ロード・メモリ・ディスクのグラフに重なって描かれます。1 台しかいないセクションはロードの
  1m/5m/15m をそのまま維持し、2 台以上では色がホストを表すため 1 分平均だけになります。この場合の
  カード見出しは、どのホストにも当てはまらない値ではなく、最も高い値とそのホスト名を表示します。
  ディスクのカードはセクション内のマウントの和集合になるため、ポーラーを選ぶと core の `metrics` や
  `database` が見えなくなる、ということがなくなりました。セクションは折りたためます。折りたたんだ
  セクションも各ホストの現在の CPU・メモリ・ディスクは表示しますが、履歴は取得しません。これが
  ポーラーの多い環境での負荷の上限になります。
- **URL 監視・DNS 監視の設定そのものを Edit node で変更できるようになりました。** 監視先の URL、
  期待するステータス、DNS のレコード種別、リゾルバの変更は、これまで Overview タブの健全性カードに
  出る ⋮ メニューを見つける必要があり、そのカードが描かれている時しか辿れませんでした。今後は
  ヘッダの Edit node にプロファイルやプールと並んで出ます。⋮ メニューには「監視設定を削除」が
  残ります。監視の内容だけ保存できてノードの設定が保存できなかった場合、ダイアログは閉じずに
  どちらが通ったかを表示します。どちらも全面置換なので、もう一度保存しても安全です。
- **インベントリツリーの右クリックメニューから Edit node を開けるようになりました。** これまでは
  対象の行を選択したうえで、詳細ペインのヘッダにあるボタンを探す必要がありました。ノード ▸
  すべてのノードでは、ノード行のコンテキストメニューの「開く」の直下に「ノードを編集…」が並びます。
  開くのは同じダイアログで、選択は動かないので、詳細ペインの表示はそのままです。
- **ダイアログ内の横並びの項目がスマートフォンで縦に積まれるようになりました。** モーダル内の
  ペア（DNS のリゾルバとポート、URL 監視のステータス範囲、認証情報の種別と名前）が 390px 幅でも
  2 列のままで、それぞれ画面半分に潰れていました。モーダル外のフォームと同じ挙動になります。
- **Troubleshoot ▸ Tools が分析ジョブの一覧を再掲しなくなりました。** そこに出ていたパネルは
  Troubleshoot ▸ Analysis runs と同じもので、同じジョブ一覧を同じコンポーネントで、絞り込みも
  件数制限もなく描いていました。診断ツールの一覧をページの下へ押し下げるだけだったので外します。
  ツールのグリッドは上部の統計の直下に来ます。実行中のジョブ数はこれまでどおりページ上部の
  「実行中」が示し、一覧は Analysis runs で見られます。
- **Settings ▸ Upgrade が、そこからはアップグレードできない環境にアップグレードを持ちかけなく
  なりました。** WebUI からのアップグレードには、core と並べて `yagra-updater` サイドカーを動かす
  コンテナ構成（`docker-compose.deploy.yml`）が必要です。それ以外の導入方法（ネイティブ導入、
  サイドカーを含まない構成）にはこの機構がありませんが、画面はそれを言わず、リリース一覧も適用
  ボタンも ON/OFF スイッチも出したうえで、押すと 503 を返していました。今後は「この環境はここから
  アップグレードできません」と表示してコマンドラインを案内し、これらの操作は一切出しません。
  稼働中のバージョン・適用済みスキーマ・ダウングレード可否はこれまでどおり、どの環境でも出ます。
  - 機構は**ある**のにアップデータが見つからない／停止している状態とは別物です。そちらは異常
    なので、これまでどおりはっきり異常として表示します。
- **Yagra の導入手順を、公開イメージを使う方法へ改めました。リポジトリの clone も不要になりました。**
  README・デプロイガイド・ウェブサイトのいずれも、最初に案内していたのは `docker compose up --build`
  — `docker-compose.yml` をソースからビルドする構成でした。そしてそれは、アップデータのサイドカーも
  永続 KEK も持たない、まさにその構成です。つまり手順書の第一歩が、自分自身をアップグレードできず、
  再起動で保存済みの機器資格情報を失う環境へ新規ユーザーを着地させていました。しかも上記の変更以降、
  Upgrade 画面はそのことを表示するのに、理由を説明するものが周囲に何もない状態でした。今後の推奨は
  `docker-compose.deploy.yml` です — `curl` で取得し、`docker compose up -d` で起動します。
  チェックアウトもビルドも不要です: 参照する変数はすべて既定値を持ち、Docker ソケット以外の
  バインドマウントもありません。初回起動より前に決めておく価値があるのは `POSTGRES_PASSWORD` だけで、
  これは Postgres が初期化時にデータボリュームへ焼き込むためです。
  - ソースからのビルドは変更なく、これまでどおり完全に文書化しています。位置づけを、Yagra を開発
    する人・監査する人・独自ビルドを作る人に宛てたものへ改め、2 つの制約を別の節ではなく手順の
    すぐ隣に明記しました。
  - `DEPLOYMENT.md` の 2 つの Docker 構成は記号が入れ替わりました。**A** がビルド済みイメージ、
    **B** がソースからのビルドです。ページ内アンカーも一緒に移動しました。`C`/`D`/`E` は変更ありません。
- **インベントリツリーの検索がグループ名に一致したとき、そのグループの中身を表示するようになりました。**
  ノード ▸ 全ノードで検索語がフォルダに一致すると、そのフォルダの行だけが出て中身は何も出ませんでした。
  検索はサーバー側でノードの名前とアドレスに対して走るため、一致したフォルダのメンバーはそもそも
  応答に含まれず、「メンバーがいる」と示すヘルスバーの隣で行が空になっていました。名前が一致した
  グループは配下のサブグループとメンバーノードを、ノード自身が一致するかどうかに関わらずすべて
  読み込みます。ノード名だけに一致する検索の挙動はこれまでと同じです。一度に開ける数を超える
  グループが一致した場合は、一部を空のまま見せるのではなく、その旨を表示します。

### バグ修正
- **チェックを 2 つ以上持つ機器が、そのうち 1 つしかポーリングされていませんでした。無言で、アラート
  も上がりません。v0.2.3・v0.2.4・v0.2.5 が該当します。アップグレードしてください。** 一般的な SNMP
  ノードでは、システム系のスカラー値だけが届き続ける一方で、インターフェースの walk、ベンダー固有の
  ヘルス表、トポロジ walk、さらには ICMP までが毎周期捨てられていました。外からは何も異常に見えませ
  ん — 生き残ったスカラーのチェックが**そのまま死活監視のチェックでもある**ため、ノードは `ok` のま
  まで、欠測としても記録されず、見える症状はグラフが伸びなくなることだけでした。URL・DNS・ICMP 単独
  の監視はチェックが 1 つなので影響を受けません。3 つの条件が重なっていました。ポーラーのローカルスケ
  ジューラは 500ms の 1 tick で due になったものをまとめてワーカーへ流します。ワーカーの機器ごとのガ
  ード（遅い機器にポーリングを積み上げないためのもの）は 1 つの宛先アドレスにつき 1 本だけ通し、残り
  を**後回しにせず破棄**します。そして破棄されたチェックのタイマーも 1 周期分進むため、同じノードの
  2 つのチェックが一度同じ tick に入ると、以後ずっとそこに留まります。v0.2.3 が、新しく引き受けた
  チェックを配置する窓を「ポーリング間隔」から「チェック数 ÷ 毎秒 200」へ狭めたことで、この重なりが
  既定になりました — 小規模なデプロイでは約 70ms で、1 tick の中にすっぽり収まります。向きに注意して
  ください: **デプロイが小さいほど窓が狭くなるため、最初に導入した環境ほど強く踏み、大規模なフリー
  トはまったく影響を受けません。** 修正として、ノードのチェックはそれぞれが独自のオフセットを引くの
  をやめ、並び順に沿って 1 秒ずつずらして配置します。これは重複ポーリング防止が本来求めているもの
  （散らすのは*ノード*どうし）であると同時に、偶然ではなく算術で保証される分離です。60 秒 / 300 秒 /
  3600 秒という互いに割り切れるティアの組み合わせ — 一度重なれば永久に重なり続ける組み合わせ — でも
  成立します。v0.2.3 の引き継ぎ改善はそのままです。`YAGRA_ADOPT_RATE_PER_SEC=0` は v0.2.3 以前のスケ
  ジューリングに戻す脱出ハッチとして残りますが、この問題の回避に使う必要はもうありません。
- **CSV に書き出したレポートが、表計算ソフトの数式を製品の外へ持ち出しうる状態でした。** 書き出し側
  は RFC 4180 の引用符付けだけを行っていましたが、それでは防げません — 表計算ソフトは引用符を外して
  から中身を評価します。レポートの表はデバイス由来の文字列（ノード名・インターフェースの説明・
  `sysDescr`）で組まれるため、デバイス（あるいはノード名を設定できる人）が `=HYPERLINK(…)` を仕込め
  ば、書き出しを開いた人の手元で実行されえました。WebUI 側は以前まったく同じ見落としの代償を払って
  中和を入れていましたが、バックエンドには中和しない 2 本目のエンコーダがありました。エンコーダを
  複製するとはこういうことです。今は 1 本だけで、WebUI 側と一致していることをテストが固定します。
  出力は、すべてのフィールドが引用符で囲まれること、数式として解釈されうる値の先頭にアポストロフィ
  が 1 つ付くこと以外は変わりません。
- **Troubleshoot の分析完了通知が、成功をすべて「失敗」と伝えていました。** 「通知する」の判定が
  完了状態を `succeeded` と比較していましたが、これは**レポート実行**側の語で、分析実行は `done` です。
  比較が成立することは一度もなく、正常に終わった実行はすべて「分析が失敗しました」になっていました。
  `AnalysisJob.state` は両側とも閉じた集合になったので、この比較はコンパイルエラーになります。あわせて
  API は、書き込まれることのない状態を指定した絞り込みを、空リストではなくエラーとして返します。
- **ノード詳細の収集タブが、URL / DNS モニタでは流れているメトリクスをすべて隠していました。**
  「届いているものだけ」のトグルが「届いている」を 1 つの状態と読んでいましたが、この状態は 2 つの
  事実を掛け合わせたものです。一方の値は**収集設定があり、かつ届いている**を意味し、もう一方は
  **収集設定は無いが届いている**を意味します。到達性・`http_up`・`dns_up`・隣接数・監視した JSON から
  抽出した値は、すべて後者です。URL / DNS モニタには収集セット自体が無いため、絞り込むためのトグルが
  一覧を空にしていました。
- **ノード名が生の UUID のまま表示されることがあり、出るかどうかは読み込みごとの運任せでした。**
  ノードを参照する一覧（アクティブアラート・アラート履歴・イベント・保存済みの所見・ポーラー）は、
  ID をまとめて 1 回のリクエストで名前に解決します。そのリクエストを投げていたのはリゾルバを持つ
  コンポーネントの effect でしたが、ID を集めるのは**セル**のレンダーです。仮想化された一覧は最初の
  レンダーでは行を 1 つも描かず、採寸を終えてから**自分だけ**再レンダーします。つまり ID は所有側が
  参加していないレンダーで集まり、effect は走らず、リクエストは投げられませんでした。救っていたのは
  無関係な取得がわずかに遅れて返り所有側を再レンダーすることで、だから同じ画面が 1 回目は名前、
  2 回目は UUID になっていました。バッチは参照そのものから予約されるようになり、どのコンポーネントが
  再レンダーしたかに依存しません。名前が永久に来なくなる経路も 2 つ塞ぎました。リクエストが失敗した
  ID を「問い合わせ済み」のまま放置しない（通信が一度こけるとリロードまで UUID のままでした。再試行は
  回数を区切ってあり、ループにはなりません）ことと、エンドポイントの上限を超えるバッチを黙って先頭
  1000 件に切り詰めず分割することです。
- **ポーラープールのアラートが Flapping watchlist で壊れた参照のように表示され、同じバッチの他の行の
  名前まで巻き添えにしていました。** Yagra 自身の監視範囲についてのアラートにはノードがなく、ノード ID
  の位置に `pool:<名前>` が入ります。ウィジェットはこれをプール名として出さず解決できない ID として
  描いたうえ、ノード名解決のエンドポイントへそのまま送っていました。UUID として解釈できないので
  **リクエスト全体が失敗**し、同じバッチに乗った他の行も名前を失っていました。振り分け用の一覧と履歴の
  表は元から正しく分岐していて、間違っていたのはこのウィジェットだけです。3 か所とも 1 つの実装を
  共有するようにしました。
- **Alerts ▸ History がスクロール中に行を飛ばしていました。しかも大規模障害のときほど多く飛ばして
  いました。** ページングのキーセットカーソルが最後の行の `recorded_at` だけでした。この列の既定値は
  PostgreSQL の `now()`＝**トランザクション時刻**で、Yagra はアラートの遷移をフラッシュ単位の 1 本の
  多行 INSERT で書きます。つまり同一フラッシュの全行が同じ `recorded_at` を持ちます。ページの境界が
  フラッシュの内側に落ちると、次の要求は「その時刻より厳密に古い行」を求め、そのフラッシュの残りを
  黙って捨てていました。障害が大きいほどフラッシュも大きく、失われる行も増えます。
  `GET /api/v1/alerts/history` は各行の `id` を返し、`before` と並べて `before_id` を受け取るように
  なりました。2 つ揃ってカーソルです。`before` だけを送った場合は従来どおり「その時刻より厳密に古い」
  を意味するので、古いクライアントが壊れることはありません。マイグレーション 0082 が `recorded_at` の
  インデックスを `(recorded_at, id)` に置き換えます。
- **MCP の `get_alert_history` はそもそもページングできず、説明も誤ったフィールドを指していました。**
  `before` というカーソルを広告しながら、それを組み立てられる値を 1 つも返していませんでした。さらに
  説明は「`at` を使え」と書いていましたが、`at` はアラートが発生した時刻で、カーソルが比較する
  `recorded_at` とは別の時計です。各行が `cursor_at` と `cursor_id` を返すようになり、ツールは
  `before_id` を受け取り、説明もどのフィールドを使うか明記するようになりました。
- **Settings ▸ Audit が、条件に一致する古いエントリを隠していました。** 検索ボックスと操作 /
  ステータス / 期間のフィルタはブラウザ側で「すでに読み込んだページ」だけを絞り込んでいたため、
  「過去 30 日・DELETE のみ」を選んでも実際に検査されるのは直近 100 件で、それより古い一致は
  黙って落ちていました。エクスポートも同じ部分集合を出力していました。完全性が目的のログでこれは
  機能不足ではなく正しさの問題です。フィルタはデータベース側に移り、`GET /api/v1/audit` が
  既存の `limit` / `before` に加えて `q`・`action`・`status`・`since`・`until` を受け取ります。
  MCP の `get_audit` ツールも同じ経路で同じパラメータを受け取ります。ツールバーの操作子は
  変わりません。エクスポートは従来どおり「読み込み済みの行」を出力し、その旨を表示するように
  なりました。
- **LDAP と OIDC のサインインが、監査ログでサインインとして扱われていませんでした。** ディレクトリ
  経由のサインインはローカルの `auth.login` と並んで `auth.login.ldap`（および
  `auth.login.ldap_unavailable` / `auth.login.ldap_conflict`）として記録されますが、監査画面は
  完全一致で判定していました。そのため該当エントリは「サインイン」ではなく生の `auth.login.ldap`
  と表示され、「サインイン」フィルタにも一度も当たりませんでした。UI と新しいサーバー側フィルタの
  両方で、すべてのサインイン方式を前置詞で判定するようになりました。
- **MCP ツール経由の操作を絞り込めませんでした。** `/mcp` は REST の監査ミドルウェアを通らないため、
  AI クライアントからの確認応答やポーリング実行は `mcp.<tool> …` という形で記録されます。これは
  HTTP メソッドの固定リストでは表現できない形でした。操作フィルタに「MCP ツール」を追加しました。
- **SNMPv3 の認証情報を、UI からノードに割り当てられませんでした。** ノードの追加と Edit node の
  一覧に `snmp_v2c` しか出ていなかったため、v3 の認証情報は保存でき、ディスカバリの認証情報
  ファインダーで機器に一致させることまでできるのに、いま認証が通ったそのノードに割り当てられ
  ませんでした（ポーラー側は以前から v3 を復号して使っています）。両方のピッカーが v2c と v3 の
  両方を出すようになり、ディスカバリと同じ挙動になります。出るのはこの 2 種別だけです。HTTP や
  Meraki の認証情報をノードの SNMP 認証情報として割り当てると、その秘密がコミュニティ文字列と
  して機器に送られてしまうためです。
- **URL 監視・DNS 監視の Edit node に、使いようのない SNMP 認証情報の欄が出ていました。** この
  ダイアログはノードの種別を一度も読まず、すべての種別に同じ 5 項目（デバイスプロファイル・SNMP
  認証情報・メーカー・モデル・ポーラープール）を出していました。URL 監視と DNS 監視は HTTP／DNS の
  ジョブを 1 本だけ実行し、SNMP ウォークは一切行いません。そこに割り当てた認証情報は保存はされる
  ものの読まれることはなく、その認証情報の「使用中」件数だけを増やして削除を妨げていました。今後は
  種別ごとに実際に意味のある項目だけを出します。SNMP 認証情報は通常のデバイスのみ、メーカー／
  モデルはデバイスが報告する種別のみ、プロファイルの候補は種別に合うカテゴリのみです（すでに
  割り当て済みのプロファイルは候補に残すので、編集して黙って付け替わることはありません）。
- **検索語を入れたままノードを編集すると、ツリーが空になりました。** ノード ▸ すべてのノードでは、
  ノードの追加・編集・移動・削除、およびツリー内のドラッグのたびにインベントリを再読み込みします。
  このときツリーは、グループごとのメンバーとサーバー側の検索結果という 2 つのキャッシュを両方とも
  破棄していましたが、取り直していたのは前者だけでした。そのため一致していたノードが消え、検索語を
  打ち直すまで戻らず、再読み込みの失敗ではなく「この検索に一致するものはない」という画面に見えて
  いました。今後はインベントリと一緒に検索も再実行します。新しい結果が届くまで直前の一致を表示した
  ままにするので、ツリーが一瞬空になることもありません。

## v0.2.5 — ツリーが「なぜこの行は黙っているのか」に答え、グループの窓から 1 台だけ外せる

抑止は All nodes から**設定する**のは簡単で、そこから読むことも解除することもできませんでした。行に
🔧 や 🔕 が付いているだけで、どのウィンドウがそれを付けたのか、その行自身のものなのか上位のフォルダ
グループから継承しているのか、いつ終わるのかは何も言いません。このリリースで、両方のマーカーがその
3 つに答えるパネルを開くようになり、抑止の出どころに応じた操作をちょうど 1 つだけ提示します —
グループのウィンドウから、兄弟に触れずに 1 ノードだけを外すことも含めて。

残りは、同じ機能が実画面に出会った結果です。下のバグ修正 3 件のうち 2 件はテストではなくテスト
サーバで触って見つかったもので、うち 1 件 — ホバーすると既にポインタの下にあるボタンが動く — は
今回の作業よりずっと前からツリーにあったもので、マーカーが押せるようになって初めて危険になりました。

### 新機能
- **抑止を、設定したのと同じツリーから解除できるようになりました。抑止中のグループの中の
  1 ノードだけを外すこともできます。** All nodes のメンテナンス（🔧）とミュート（🔕）のマーカーが
  ボタンになりました。クリックすると、その行を黙らせているものをパネルが列挙します —
  ウィンドウの名前、それが行自身のものか上位のフォルダグループ由来か、そしていつ終わるかです。
  これまでツリーは 1 クリックで抑止を掛けられる一方、**なぜ黙っているのかを一切答えられず**、
  外すには Alerts ▸ Maintenance windows や Alerts ▸ Mutes で名前を頼りに行を探す必要がありました。
  同じパネルは右クリックメニューからも開けます。
  操作は抑止の出どころによって変わり、ラベルもそのとおりに書き分けています。
  - 行自身を名指ししているウィンドウは、削除ではなく**今すぐ終了**します。`ends_at` を現在時刻に
    動かすので、実施した保守の記録として Maintenance 画面に残り、既存の「終了したものを一括削除」で
    掃けます。行自身を名指ししているミュートは従来どおり解除（削除）です。
  - **継承しているだけ**のウィンドウやミュートは、終了させると配下の兄弟すべてを解除してしまうため、
    ノードには代わりに「このノードをメンテナンス対象から外す」（ミュートなら「このノードをミュート
    対象から外す」）を出します。そのノードだけが通常のアラートに戻り、グループの残りは抑止された
    ままです。除外は、元になった抑止が終わると自動的に切れます — 期限を計算するのはサーバーで、
    ブラウザは期限を送りません。さらに、抑止が予定より早く止まったとき（ウィンドウの早期終了・無効化・
    削除、ミュートの解除）はその都度期限を計算し直すので、除外が元になった理由より長生きすることは
    なく、**次のウィンドウからそのノードが黙って抜け落ちることもありません**。除外中かどうかは
    マーカー自体が示します。抑止が**効いている**行は塗りつぶしのチップ（メンテナンスは青、ミュートは
    黄）、そこから外れている行は色が抜けて破線の枠だけになります。主たる信号は「塗りか枠か」で、これは
    グレースケールでも判別できます。色相は 2 種類のどちらかを言うだけの役割です。ミュートのグリフは
    斜線が取れて、鳴っているベルに戻ります。パネルは「何の対象から外れていて、いつまでか」を示して、
    操作は元に戻すことだけになります。
  - 上位グループのウィンドウで覆われている**グループ**は、原因を読み取り専用で表示し、どのグループ側で
    解除すればよいかを示します。子の行から上位のウィンドウを終了できてしまうと、そこからは見えない
    範囲まで抑止が解けてしまうためです。
  新しいエンドポイント: `POST /api/v1/maintenance-windows/{id}/end`（ManageMaintenance。アクティブな
  ウィンドウ以外は `404`。これは未開始のウィンドウに開始より前の終了時刻が入るのも防いでいます）、
  `PUT /api/v1/nodes/{node_id}/maintenance-exemption`（ManageMaintenance）と
  `PUT /api/v1/nodes/{node_id}/mute-exemption`（AckAlerts、いずれも `{"exempt": bool}`。継承した抑止が
  無い場合は `400 not_suppressed`）、`GET /api/v1/suppression-exemptions`（View）。MCP の
  `list_suppressions` ツールに `exemptions` 配列が増えます — 除外中のノードは黙っていないので、
  ウィンドウだけを見た読み手は逆の結論に達してしまいます。

### 改善
- **サポートバンドルのダウンロード中に、API の他の処理が止まらなくなりました。** この要求の最後の
  区間は、ローテートされたログファイルを最大 24 MB ディスクから読み、その同じバイト列に対して
  秘匿検査と gzip を掛けます。すべて同期処理で、しかもこれまではすべてリクエスト処理スレッド上で
  走っていました。これをブロッキングプールへ移しました。バンドルの中身は変わりません。変わるのは
  「バンドルを作っている間もデプロイが応答し続ける」ことで、バンドルを取るのは既に何かが起きて
  いる時なので、そこが効きます。

### バグ修正
- **アップグレード失敗時に表示される復旧手順が、実際には何も直していませんでした。** v0.2.4 は、
  compose ラベルが古いデプロイを最初の段階で停止させ、復旧コマンドを名指しするようにしました。
  そのコマンドが誤りでした。`docker compose up -d` が作り直すのは定義が変わったコンテナだけで、
  汚染された apply の後にそれに該当するのは core・web・poller です。アップデータは、自分が直前に
  インストールした構成どおりの定義なので作り直されません。ところがラベルを読むのは**アップデータ
  自身**なので、このコマンドが唯一触れないコンテナが、唯一ラベルを読まれるコンテナでした。緑の行を
  4 つ表示して何も変えない——失敗するより悪い挙動です。メッセージは
  `docker compose -p yagra -f docker-compose.deploy.yml up -d --force-recreate yagra-updater`
  を名指しし、素の形では直らない理由を述べ、そして「問題そのものであるディレクトリから実行せよ」と
  言わなくなりました。
  **これは v0.2.3 以前の全デプロイに影響し、回避できません。** アップグレードを実行するのは
  **置き換えられる側**のリリースのアップデータなので、v0.2.4 の修正は、その修正自身を届ける
  アップグレードには適用できません。しかもその修正はアップデータのサービス定義そのものを変えるため、
  不正なラベルを刻む作り直しは「起こりうる」ではなく「必ず起こる」ものでした。したがって v0.2.4 へ
  移行した後の最初のアップグレードは、最初の段階で停止します。上記コマンドをホストで一度実行すれば、
  以後のアップグレードは影響を受けません。その間もリスクはありません。イメージの取得もコンテナの
  停止も始まる前に停止するためです。
- **All nodes で行にカーソルを載せると、その行にすでにあるボタンの位置が動いていました。**
  ツリーは行ごとの操作（グループなら ＋ / ✎ / 🗑、ノードなら ↗）をホバーで表示しますが、表示は
  それらを行に**挿入**する形だったため、左側にあるもの — メンテナンス / ミュートのマーカー、件数、
  ヘルスバー — がカーソルの到着と同時に約 80px 横へずれていました。以前からの挙動ですが、マーカーが
  ボタンになったことで危険になりました。グループの 🔧 を狙うと、クリックが着地する前に同じ位置へ
  **グループの削除**が来るためです。マーカーをアクションの**後ろ**に描くようにしました。flex 行の
  最後の子は、左側で何が起きても右パディングにぴったり収まります。したがってマーカーが狙っている
  カーソルの下から動くことはなく、そのためにどの行も幅を明け渡さずに済みます。
- **All nodes が、すでに終わった抑止のマーカーを表示し続けていました。** ツリーがメンテナンス
  ウィンドウとミュートの一覧を読むのはページを開いたときと、そのページで操作を行った後だけで、
  その間に更新する仕組みがありませんでした。そのためタブを開いたままにしていると、すでに通常どおり
  アラートを上げている行に 🔧 や 🔕 を描き続け、リロードするまで直りませんでした。行の判定は、
  一覧を取得した時点で真だったフラグではなく**時計**に対して行うようにし、さらに次の
  ウィンドウ／ミュートが切れる瞬間にだけ取り直すようにしました。その間のポーリングはありません。

## v0.2.4 — 2 回目のアップグレードが通り、その前に取るバックアップにメトリクスが入る

バグ修正 2 件。いずれも v0.2.2 で入った自己アップグレード経路のもので、見つかり方も同じ——実機の
デプロイを 3 回以上アップグレードしたことです。どちらも何かを壊してはおらず、そこが問題の大半でも
あります。片方は**違う段階**を名指して派手に失敗し、もう片方は成功しながら静かに不完全なバックアップ
を書いていました。**v0.2.2 / v0.2.3 を使っていて WebUI からアップグレードしたことがある方は両方とも
お読みください**。前者は復旧コマンドを 1 つ挙げており、後者は「すでに手元にあるアップグレード前
バックアップにメトリクスが入っていない」ことを意味します。

### バグ修正
- **WebUI から一度アップグレードしたデプロイは、二度目のアップグレードができませんでした。**
  アップデータはデプロイのディレクトリを、自分のコンテナが持つ compose ラベルから読み取ります。
  一方アップグレードは、使い捨てコンテナの中から `docker compose up -d` を実行してスタックを
  作り直すため、作り直した全コンテナに刻まれるのは**その使い捨てコンテナ側のディレクトリ**で、
  ホストのそれではありませんでした。作り直しの対象にアップデータ自身が含まれた時点で、次の
  アップグレードはホストに存在しないディレクトリをマウントし、Docker がそれを空で作り、実行は
  最初の段階で `the pre-upgrade backup failed` と報告して停止していました。段階もコンポーネントも
  実態と違う報告です。アップグレード用コンテナはホストと同じパスでデプロイのディレクトリを見る
  ようになり、構成が見つからない場合はその旨と復旧コマンドを名指しで報告します。壊れたものは
  ありません。イメージの取得もコンテナの停止も始まる前に落ちていました。
  **すでにこの状態のデプロイ**（Settings ▸ Upgrade が 1 秒以内にバックアップ段階で失敗する）は、
  このデプロイの `docker-compose.deploy.yml` が置かれているディレクトリで、ホスト上から一度
  `docker compose -p yagra -f docker-compose.deploy.yml up -d --force-recreate yagra-updater`
  を実行してください。ラベルが刻み直され、以後は WebUI から実行できます。（公開後に訂正: 当初は
  素の `up -d` を挙げていましたが、それではアップデータの定義が変わっていないため作り直されず、
  何も直りません。ラベルを読むのはアップデータ自身なので、作り直すべきはそのコンテナです。）
- **アップグレード前のバックアップにメトリクスが入っておらず、そのことがどこにも出ていません
  でした。** `yagra-backup.sh` は VictoriaMetrics のスナップショットをスタック内部から HTTP で
  要求しますが、その要求は `docker exec core` 経由で行っていました。core のランタイムイメージには
  `wget` も `curl` も入っていないため、呼び出しは何も返さず、スクリプトは「応答にスナップショット名
  が無い」分岐に入ります。この分岐は意図的に致命的ではなく（メトリクスストアの無い環境でも設定の
  バックアップは要る）、出力に 1 行報告しますが、アップデータはアップグレード用コンテナの出力を
  捨てます。結果として、この経路で取られたバックアップはデータベースと KEK、空の `vm/`
  ディレクトリ、そして `"yagra_version": "unknown"` を持っていました。2 つの呼び出しは HTTP
  クライアントを持つコンテナ経由になり、manifest は `metrics_snapshot`（名前、または `null`。
  省略ではなく明示）を持ち、末尾のサマリーはメトリクスを含まないバックアップをその旨の文章で
  報告します。

## v0.2.3 — アップグレードが全拠点に届き、ポーラーは 1 台ずつ入れ替わり、引き継ぎでサンプルが欠けなくなる

### 新機能
- **遠隔拠点のポーラーも、デプロイをアップグレードするのと同じボタンで上げられるようになりました。**
  これまで Settings ▸ Upgrade が入れ替えていたのは core・WebUI・同居ポーラー、つまり 1 つの compose
  プロジェクトに載っているものだけで、監視対象拠点のポーラーは 1 台も入れ替わらず、しかもそのことが
  画面のどこにも出ませんでした。`docker-compose.poller.yml` に `yagra-poller-updater`
  サイドカーを足すと（コメントアウト同梱・外すまで無効）、その拠点は「自分を入れ替えられる」と申告し、
  core がインストールしたのと同じリリースを受け取ります。入れ替えは**1 プール 1 台ずつ**なので、
  2 台以上のプールは監視を止めずに上がります。ポーラー自身は Docker ソケットに触れません。命令を
  検証して共有ボリュームに書き、サイドカーが実行します（中央での core と同じ形）。イメージの取得は
  停止の**前**に行うため、1 台構成の拠点で止まるのはコンテナの再作成のあいだだけです。
- **Upgrade 画面が、どのポーラーが一緒に上がり、どれが残るかを表示します。** 計画と確認ダイアログの
  両方が、現在の版のまま残るポーラーを名指しします。部分的なアップグレードは、後から気づくものでは
  なく選ぶものになりました。`GET /api/v1/system/upgrade` に `pollers`（`with_core` / `manual`）を
  追加しています。アップデーターが自分の compose プロジェクトのポーラーを報告するまでは空配列では
  なく `null` です。「取り残しは無い」と「誰も確認していない」は別の答えだからです。
- **終了したメンテナンスウィンドウを、まとめて削除できるようになりました。** 一覧は誰かが削除する
  までウィンドウを持ち続けるため、定例作業を登録している運用者にとっては、ほとんどが履歴の画面に
  なっていました。アップグレードのたびに 1 件、しかも先頭に増えてもいました。Alerts ▸ Maintenance
  windows の件数の横に「終了した N 件を削除」ボタンが付き、その裏で
  `DELETE /api/v1/maintenance-windows?status=ended`（manage-maintenance）を呼びます。終了したか
  どうかはサーバーの時刻が判断し、対象行は呼び出し元のグループスコープが決めます。ブラウザは ID の
  一覧を送りません。実行中・予定のウィンドウが消えることはなく、スコープの付いたアカウントは自分に
  見える分だけを削除します。応答は削除件数を返します。`status` は必須で `ended` のみを受け付けます。
  パラメータなしの `DELETE` は「全件削除」とは解釈せず、拒否します。
- **Settings ▸ Upgrade がリリース一覧を最後に取得した時刻を表示し、取り直すボタンを持つように
  なりました。** アップデータがレジストリを見に行くのは既定で 24 時間ごと
  （`YAGRA_UPGRADE_CHECK_SECS`）です。つまり、リリースが公開された直後こそ一覧が確実に古い時間帯で
  あり、そこが人がこの画面を開く時間帯でもありました。`POST /api/v1/system/upgrade/check`
  （manage-configuration）で今すぐ取り直せます。何もインストールせず、引数も持ちません。問い合わせ先は
  従来どおりホスト側で固定されており、機構がオフのときは拒否されるため、外向き通信をしないデプロイは
  今までどおり何もしません。

### 改善
- **ポーラーが他のポーラーからノードを引き継いだとき、最大 1 周期待たずにすぐポールするように
  なりました。** ポーラーがプールから抜けたとき（再起動・障害・ローリングアップグレード・縮小）、
  引き継いだ側は移ってきたノードを新規ノードと同じ扱いにし、最初のポールをポーリング間隔いっぱいに
  散らしていました。前の担当はすでに止まっているので、そのノードの連続サンプルは最悪で**間隔の 2 倍**
  離れます（既定 30 秒なら 1 分）。しかも何も報告されませんでした。プールには生存ポーラーが居るので
  カバレッジのアラートは鳴らず、`monitoring_gaps` が記録するのは core とポーラーの疎通であって
  ポール漏れではないからです。引き継いだ分は「持続可能なレートで 1 周するのに要る時間」に散らすように
  なりました（`YAGRA_ADOPT_RATE_PER_SEC`、既定 200 spec/秒、間隔で頭打ち）。50 ノードの引き継ぎは
  1 秒未満で終わり、フリート全体のコールドスタートは従来と同一の挙動のままです。`0` で従来動作。
- **復帰したポーラーに、担当ノードがすぐ戻るようになりました。** 抜けるポーラーは以前から即座に
  再割当を起こしていましたが、戻るポーラーは次のスイープまで（最大でフリート最小のポーリング間隔）
  待たされ、そのあいだ担当は代わりに引き受けた側に留まっていました。両方向とも即時になりました。
- **停止するポーラーが、始めた仕事を終えてから終了するようになりました。** 実行中のプローブが結果を
  返すのを最大 5 秒待ってから終了します（従来はランタイムごと破棄して結果を捨てていました）。あわせて
  最後の「離脱します」ハートビートがプロセスの外に出たことを確認します。このビーコンは core が
  3 回分のハートビート欠落を待たずに再割当するための合図ですが、従来はプロセス終了と競走して
  失われることがありました。
- **`GET /api/v1/pollers` が各ポーラーの `caps` と `listeners` を返すようになりました。** Settings ▸
  Pollers は core と版が違うポーラーと、自分を入れ替えられるポーラーに印を付けます。リスナ一覧は拠点を
  再起動する前に見る価値があります。syslog・トラップ・フロー送出にはバッファも待機系も無いため、
  能動ポーリングと違い、ポーラーが落ちているあいだに届いたものは回復できません。
- **ポーラーを 2 リリース以上取り残すアップグレードを、押せないようにしました。** バスが保証する版差は
  1 リリース（N/N-1）までです。ADR-050 はそう書いていたのに機構がどこにも無く、危険と書いた跳躍が
  他と同じように押せていました。該当するリリースは理由付きの無効ボタンとして**一覧に残ります** —
  探している版が「無い」ように見えるのは「あるが押せない」より悪いためです。数えるのはその操作で
  上がらないポーラーだけなので、全ポーラーが一緒に上がるデプロイが止められることはありません。
- **アップグレードの実行中に、何をしているかが画面に出るようになりました。** 進捗バーとフェーズ名
  （バックアップ中・イメージ取得中・コンテナ入れ替え中・検証中）が出ます。これまでは、アップグレードに
  かかる約 65 秒のあいだ画面が何も変わりませんでした。あわせて、**コンテナ入れ替えによる接続断**と
  **画面が固まっただけ**を区別して表示します（従来は見分けがつきませんでした）。
- **Upgrade 画面の文字量を減らしました。** 各説明文を 1 文に圧縮し、3 つは削除しました。リリース行の
  ボタンは「アップグレード」「ダウングレード」の 1 語になりました（バージョン番号は同じ行に出ているため、
  文で言い直す必要がありません）。

### バグ修正
- **無効化したまま期限が過ぎたメンテナンスウィンドウが、いつまでも「無効」と表示されていました。**
  状態列が時刻より先に有効/無効を見ていたため、オフにしたあと期間が過ぎたウィンドウが「終了」に
  ならず、これから有効化されるものと見分けが付きませんでした。期限が過ぎたものは「終了」と表示
  します。新しい一括削除が数える対象もこれと同じです。サーバーがアクティブと報告しているウィンドウ
  は、ブラウザの時刻が何を言っても「終了」とは表示しません。
- **アップグレード中、Upgrade 画面が最初から最後まで何も表示しませんでした。** 確認ダイアログを閉じた
  あと、手で再読み込みするまで進捗も状態変化も出ませんでした。ポーリングをサーバ側の実行状態から
  張っていたためです。アップデータはリクエストを 5 秒ごとにしか見に行かず、そこからコンテナを起動する
  ので、最初の 5〜10 秒はサーバがまだ**前回の実行**を「完了」として報告しています。結果、ポーリングは
  一度も開始されませんでした。リクエストが受理された時点から開始するようになりました。同じ時間帯には
  2 つ目のリリースをクリックできてしまい、バックエンド側の重複チェックもそれを見られませんでした。
  どちらも塞いであります。
- **アップグレード完了後もフリート全体が 15 分間沈黙し続ける問題を修正しました。** アップグレードの
  適用時は、再起動が自分自身についてアラートを上げないよう、デプロイ全体のメンテナンス窓を開きます。
  この窓には上限時刻があるだけで**閉じる処理が無かった**ため、65 秒で終わったアップグレードの後も
  残り約 14 分、全ノードが `maintenance` のまま＝本物の障害も見えない状態が続いていました。復帰した
  core が、実行結果が出た時点で窓を閉じるようになりました。上限時刻は「結果を一度も報告しない実行」
  に対する保険として残ります。
- **完了したアップグレードが監査ログに記録されるようになりました（これまで一度も記録されていません）。**
  core は自分が監視している実行の `compose` ステップで作り直されるため、実行がまだ `running` の状態で
  復帰し、一度だけ確認して二度と見に行かず、updater はその数秒後に結果を書いていました。監査証跡には
  「誰かがアップグレードを要求した」だけが残り、それが成功したかは残りませんでした。core が結果を待つ
  ようになりました。記録は `upgrade succeeded -> v0.2.2 (…)` の形で、ボタンを押した本人に帰属します。
  失敗した実行も成功した実行と同じように記録されます。
- **アップグレード画面が、戻せないバージョンについて誤った理由を表示していました。** 該当のマイグレー
  ションがスキーマを狭めたと述べていましたが、実在する唯一の下限についてそれは事実ではありません。
  0.2.2 より前のリリースが新しいデータベースで起動できないのは、それを許容する仕組みが 0.2.2 で初めて
  出荷されたからであって、何かが狭められたからではありません。下限に記録された理由自体はその直後に
  正しく表示されており、導入部の一文と矛盾していました。画面が独自に原因を断定するのをやめました。

## v0.2.2 — Yagra が自分で次のバージョンを導入し、どこまで戻せるかを自分で答え、レジストリ無しでもそれができる

### 新機能
- **Settings ▸ Upgrade — 動いているビルドと、どこまで戻せるかを Yagra 自身が答えられるようになりました**
  （ADR-050）。`GET /api/v1/system/upgrade`（manage-configuration）が、稼働中バイナリのコミットと
  ビルド種別、適用済みマイグレーション数、そして**互換下限**（このデータベースを動かせる最も古い版）を
  返します。これまでコミットとビルド種別はサポートバンドルの中（3 権限が必要）でしか読めず、
  「CI が緑でも、これは本当に自分がビルドしたバイナリか」という問いを安く確かめる手段がありませんでした。
  同じ内容は MCP の `get_system_health(section="upgrade")` からも読めます。
- **WebUI からアップグレードできるようになりました。** Settings ▸ Upgrade に移行できるリリースが
  並び、バックアップ → pull → 対象イメージから取り出した構成の導入 → 再作成 → 検証までを新しい
  `yagra-updater` コンテナが実行します。**core は Docker ソケットを持ちません** —
  `ipasn-updater` が唯一の外向き通信を握っているのと同じ形です。
  **これが通常のアップグレード手段になりました** — `/release` はどこにもデプロイしなくなり、
  各環境は運用者がボタンを押したときに新しい版を受け取ります。
  - **既定で有効です。画面上のスイッチでオフにできます。** オフにすると、アップグレードを要求できず、
    アップデータはレジストリへの問い合わせも停止します（コンテナ自体は残ります）。設定はデータベースに
    保存されるのでアップグレードをまたいで残ります。compose からサービスを削除する方法は残りません —
    各バージョンが自分の構成を導入するためです。
  - ⚠️ **Yagra の Admin ロールからホスト root へ至る経路が新たに生まれます。** 歯止めは、
    イメージの取得元がホスト側の環境変数で固定されていること（Admin が入れられるのは
    我々が公開したタグだけ）、命令の集合が固定であること、そして**共有ボリュームには実行されるものを
    一切置かないこと**です。アップグレードの要求には manage-configuration **と**
    manage-credentials の両方が必要で、MCP には出していません。
  - **リリースを探しに行く先は専用のホスト設定です** — `YAGRA_UPGRADE_REPO`（既定は GHCR の公開
    リポジトリ）。「動いているコンテナを取ってきた場所」とは意図的に別にしてあるので、イメージを
    private ミラーから取得している環境でも、本物のリリース一覧が見えます。
  - レジストリの確認は 1 日 1 回です。
  - 各リリースには「上げる」「戻す」のどちらかが明示されます。現在のデータベースを読めない版は
    隠さず、理由を添えて選べない状態で表示します。0.2.2 より前のリリースはどの環境でもこれに
    該当します — 新しい版が移行したデータベースに対して起動できるようにする緩和が今回初めて
    入るためで、それより古い版へはバックアップからの復元でしか戻れません。
- **レジストリに一切到達できない環境でのアップグレード。**
  `POST /api/v1/system/upgrade/bundle` がリリース 3 イメージの `docker save` アーカイブを受け取り、
  共有ボリュームへストリームして導入します。バックアップも、対象イメージから取り出した compose も、
  導入後の provenance 検証も通常経路と同一です。変わるのは手順 2 だけで、`docker pull` の代わりに
  `docker load` を行い、そのアーカイブが本当に操作者の名乗ったリリースを含むかを照合します。
  サイドカー本体とは**別のオプトイン**（`YAGRA_UPGRADE_ALLOW_BUNDLE=1`）が必要です。compose が
  名指ししていないイメージをホストへ持ち込める唯一の経路だからで、レジストリに到達できる環境では
  閉じたままにしてください。

### 改善
- **互換ウィンドウの範囲内なら、ダウングレードできるようになりました。** 新しい `schema_compat`
  テーブルにより、スキーマを狭めるマイグレーションが「以降はこの版以上でしか動かない」と宣言できます。
  何も書かなければ可逆 — 既存 77 本すべてがそうです。core は、認識できない適用済みマイグレーションが
  **すべて自分より新しい場合に限り**、新しい core が移行した DB に対して起動します。
  途中に欠けがある・別系統の履歴といった場合は従来どおり起動を拒否します。
  **戻しても 1 行も失われません** — 新しい版が追加した列はそのまま残り（読まれないだけ）、
  再び上げれば見えるようになります。
  ⚠️ これは「戻る**先**」の版に入っている必要があるため、実際に窓が開くのは次のリリースからです。
- **`yagra-core migrations`** が、そのバイナリに埋め込まれたマイグレーション一覧を JSON で出力します。
  DB も設定も不要なので、**対象イメージの中で先に実行して**、何も触る前にアップグレード計画を
  立てられます。
- メンテナンスウィンドウにフリート全体を対象とする `system` スコープを追加しました。アップグレード
  経路が、期限を区切って自己監視を黙らせるために使います。追加ダイアログには意図的に出していません —
  アップグレード以外がフリート全体を黙らせられるべきではないからです。

## v0.2.1 — インベントリがノードの種別を示し、埋まらないタブは消え、＋ からノードを追加できる

### 破壊的変更
- **ノード一覧が「そのノードが何であるか」を返すようになりました。** `GET /api/v1/nodes` と
  `GET /api/v1/nodes/by-group` は、2 値の `source` フィールドを `kind` に置き換えます。値は
  `GET /api/v1/nodes/{id}` が既に返していた `meraki` / `url` / `dns` / `device` と同じで、
  スケジューラも参照する単一の関数が解決します。一覧の行と、そこから開く詳細ページが食い違うことは
  もうありません。`source` は非推奨扱いにせず削除しました。ツリーのバッジ 1 か所のために足された
  非可逆な射影であり、その後に追加された監視種別も、今後追加される種別も、すべて `"device"` と
  名乗ってしまうためです。API クライアント向けの対応: `source == "meraki"` は `kind == "meraki"`、
  `"device"` だったものは `device` / `url` / `dns` のいずれかになります。MCP の `list_nodes` と
  `get_node_status` も同じ `kind` を返すので、AI クライアントはメトリクスを問い合わせる前に
  URL 監視とスイッチを区別できます。

### 改善
- **ノード詳細のタブが、その種別で中身を持ちうるものだけになりました。** URL 監視と DNS 監視は
  Overview と Collection、Meraki デバイスはこれに Events が加わります。これらの種別から
  Interfaces・Neighbors・Flow が消えるのは「今たまたま空だから」ではなく、**構造的に埋まらない**
  からです。URL / DNS 監視には HTTP または DNS のチェックが 1 本だけ配られ、ICMP も SNMP も
  打ちません。Meraki デバイスは Dashboard API 経由で、ノード単位のジョブ自体が出ません。到達
  できなくなるものはありません — 受動イベントは **アラート ▸ イベント** でノードを絞れば従来どおり
  見られますし、これらの種別が NetFlow のエクスポーターになることはありません。表示しないタブへの
  ブックマークは Overview に着地し、**アドレスバーも書き換わります**（どのタブも選択されていない
  状態で本文だけが描かれることがなくなりました）。
- **監視ノードの概要が、そのノードではないものの説明をやめました。** DNS 監視の概要は、これまで
  空の「ICMP RTT · 直近 30 分」グラフで始まり、メーカー・モデル・SNMP 認証情報・稼働時間が
  4 行続けて「—」でした。いずれもその種別が受けない SNMP ウォークへの答えです。グラフは実際に
  ICMP を打つ種別にだけ表示し、facts は値を持ちうる行だけを並べ（DNS 監視にはリゾルバ行が加わり
  ます）、名前の下の行は `0.0.0.0 · 不明なデバイス` ではなく `wg.example.net · A` と読めるように
  なりました。「最終確認」も種別ごとに実際に出しているメトリクスから取るようにしたので、監視ノード
  にも表示されます。
- **URL / DNS / Meraki のノードがインベントリ上で見分けられるようになりました。** ノードツリー、
  グループのメンバー一覧、詳細ページのタイトル横に短い `URL` / `DNS` / `Meraki` バッジが付き、
  ホバーで種別の正式名が出ます。通常のデバイスは無印のままなので、バッジは「これは別物として読む」
  という意味になります。従来は Meraki だけが区別でき、URL 監視はスイッチと見分けられませんでした。
- **Collection タブの `0` バッジを出さなくしました。** 収集セットが 1 つも紐づかないノード —
  つまりすべての URL / DNS 監視 — で `0` が出ており、不具合のように読めていました。バッジ自体を
  出さない挙動に変え、Interfaces タブの既存ルールに揃えました。
- **インベントリの ＋ からノードを追加できるようになりました。** ＋ は「ノードを追加 / グループを
  追加」の 2 択メニューになり、どちらもツリーで選択中の対象に対して働きます。グループ（またはその
  中のノード）を選んでいれば「「Tokyo」にノードを追加…」「「Tokyo」にサブグループを追加…」と表示し、
  未選択ならトップレベルに追加します。従来ノードの追加はツリーの右クリックからしか辿れず、
  デスクトップでは見つけにくく、`contextmenu` が発火しないタッチ端末では不可能でした —
  スマートフォンからはグループは作れてもノードは作れない状態でした。グループ行の ＋ も同じ 2 択に
  なり、こちらはその行のグループが対象になります。
- **「ノードを追加」ダイアログにグループ欄を追加しました。** 開いた場所に応じて初期選択され、
  保存前に変更できます。従来は「トップレベルに追加します」という変更できない注記だけでした。
  あわせて、グループを選択した状態でそのペインから追加したノードがトップレベルに落ちる不具合も
  解消しています。
- **「ノードを編集」をノード一覧のノードペインにも置きました。** 詳細ページ専用だったため、
  プロファイル・認証情報・メーカー / モデル・ポーラープールを一覧側から変えるには、まず
  「詳細を開く」を押す必要がありました。これは編集ではなく画面遷移としか読めず、編集の入口が
  事実上隠れていました。「移動…」の隣に並び、開くダイアログは詳細ページと同じものです。

### 不具合修正
- **DNS 監視の解決履歴で、状態バッジが隣の列に重ならないようになりました。** 「解決」列の幅が
  140px しかない一方、`No such name (NXDOMAIN)` と、それより長い「名前が存在しない（NXDOMAIN）」は
  その 1.5 倍ほどを必要とし、状態バッジは意図的に縮まない設計です。そのため、ブラウザの幅に
  かかわらず列からはみ出していました。列幅を両言語の最長ラベルに合わせ、それでも収まらない場合は
  バッジ側を省略記号で切り詰めます（全文はホバーで表示）。

## v0.2.0 — 最初の公開リリース: 機器が報告するメトリクスをすべて可視化、本文を読む URL 監視、IdP を知っている SSO 設定

**これは Yagra の最初の公開リリースです。** v0.1.23 までは非公開リポジトリでの開発で、一度も公開して
いません。以下に残してあるそれらのノートは「動かせたリリースの記録」ではなく、ここまでどう作られてきた
かの記録です。今回初めて触る方にとっては v0.2.0 が出発点で、アップグレード作業も、アップグレード元と
なる公開済みバージョンも存在しません。`1.0.0` ではなくマイナーバージョンを上げたのは意図的です。変わった
のはソースとイメージが公開されたことであって、API が動かなくなったということではありません。

デプロイの前に読んでいただきたいことが 3 つあります。

- **Yagra はオープンベータです。** 動作するスタックではありますが、このノートに書かれていることの大半は
  まだ他者の本番ネットワークで検証されていません。既存の監視を置き換えるのではなく、併走させてください。
  機能一覧は「作られているもの」であり、「他所で大規模に実証されたもの」ではありません。
- **いちばん役に立つのはバグ報告です。** 報告先は
  [GitHub Issues](https://github.com/horryworks/Yagra/issues) で、ここが**このプロジェクトの唯一の連絡窓口**
  です（質問や商用ライセンスの相談も含みます）。問い合わせ用のメールアドレスはありません。例外は脆弱性で、
  公開 Issue ではなく[非公開の脆弱性報告](https://github.com/horryworks/Yagra/security/advisories/new)
  をお使いください。プルリクエストはまだ受け付けていません — [CONTRIBUTING.md](CONTRIBUTING.md)
  を参照してください。
- **`docker-compose.yml` ではなく [DEPLOYMENT.md](DEPLOYMENT.md) から始めてください。** 単一ノード用の
  compose ファイルは評価用です — データベースのパスワードは既定値、鍵暗号鍵（KEK）は揮発性（そのため
  保存した機器の資格情報は再起動で失われます）、証明書は自己署名です。実運用で何を変えるべきかは
  DEPLOYMENT.md に、セキュリティ上の要点は [SECURITY.md](SECURITY.md) にあります。

### 新機能
- **URL 監視がエンドポイントの応答時間を記録するようになりました。** 新しい `http_response_time_ms`
  ゲージとして、ノードの概要に表示され、他のメトリクスと同じように閾値を設定できます。これまで
  URL 監視は「上がっているか」は言えても「遅いか」を一切言えませんでした — プローブは以前から
  計測していて、ただ捨てていただけです。2 点ご注意ください:
  - これは応答**ヘッダ**が返るまでの時間で、本文の受信完了までではありません（プローブは本文を
    読みません）。
  - **応答が無かったときは記録しません。** タイムアウトを記録すると障害中ずっと「遅い応答」が
    平坦に並び、レイテンシ閾値が `http_up` と同じ 1 つの障害で二重に発報してしまいます。
  - 既定の閾値は種まきしていません — 応答時間は環境差が大きすぎて既定値が正しくなりようがない
    ためです。プロファイル／グループ／ノード単位で設定してください。
- **URL 監視が応答本文をキーワードで照合できるようになりました。** キーワードが本文に「含まれること」
  または「含まれないこと」を条件にできます。後者は、`200` を返しながら本文には障害を示している
  エンドポイント — 死活監視では構造的に見えない障害 — を捕まえるためのものです。ノードの URL 監視
  ダイアログから設定でき、`PUT /api/v1/nodes/{id}/url-check` の任意フィールド `body_match`
  （`pattern` / `mode` / `max_bytes`）としても送れます。MCP の `get_config` にも出ます。報告するもの:
  - `http_body_match` — 条件を満たせば `1`、満たさなければ `0`。組込 URL プロファイルに
    `below 0.5` の critical 閾値を種まきしてあるので、ルールを設定すればそれだけで発報します。
    既存の監視には影響しません（ルールを持つ監視だけがこのメトリクスを出します）。
  - `http_body_truncated` — 診断用。本文が読み取り上限を超えたときに `1`。
  - **本文が読み取り上限を超えた場合は「満たさない」と報告し、決して「満たす」とは報告しません。**
    黙って切り詰めると、「含まれないこと」のルールが、切れた先に障害メッセージがあるページに対して
    「正常」と嘘をつくことになります。正当なキーワードが上限の先にある場合は `max_bytes`
    （既定 65536、最大 1048576）を上げてください。
  - 照合は正規表現ではなく、**大文字小文字を区別する**単純な文字列一致です。
  - `GET` / `POST` のみ。`HEAD` の監視に本文ルールを付けると拒否されます
    （`body_match_needs_body`）— HEAD は本文を返さないため、条件が永久に満たされないからです。
  - **ローリングアップグレード時:** 未更新のポーラーには本文照合つきの監視を配りません。抑止した数は
    `yagra_specs_withheld_total{cap="http-body"}` に記録されます。古いポーラーはルールを落として
    本文を読まないまま `http_up = 1` を返す — まさにそのルールが守ろうとしていた障害に対して
    緑を表示する — ため、計算していない結果を報告するくらいならチェックを止めます。`http-auth`
    と同じ仕組みです。
- **URL 監視が JSON 応答本文から数値を記録できるようになりました。** キュー長・レプリケーション遅延・
  ワーカー数などを、オペレーターが付けた名前のメトリクスとして記録します。URL 監視ダイアログから
  1 監視あたり 8 個まで設定でき、`PUT /api/v1/nodes/{id}/url-check` の `json_extract` 配列
  （`metric` と `path`）としても送れます。取り出した値はノードの概要に表示され、他のメトリクスと
  同じようにしきい値を設定できます。
  - 場所の指定は**ドット区切りで、ちょうど 1 か所を指します** — `data.queue.depth`、
    `items.0.value`（`items[0].value` も同じ意味で受け付けます）。JSONPath ではないのは意図的で、
    複数の値を選べる式にすると、誰も求めていない集約規則が必要になるためです。
  - 数値・真偽値（`true` は 1）・数値を引用符で囲んだ文字列（`"42"`）を記録します。**それ以外は
    その回に何も記録しません（0 にはしません）** — 0 は「値が本当に 0 だった」と区別できないためです。
    本文が JSON として不正な場合や読み取り上限で切れた場合も同様です。
  - メトリクス名は TSDB の名前として妥当である必要があり、**この監視自身が報告する名前
    （`http_up` / `http_status_code` / `http_response_time_ms` / `ssl_cert_days_to_expiry` /
    `http_body_match` / `http_body_truncated`）は使えません** — ノード自身の死活系列を上書きして
    しまうためです。同じ名前に 2 つのルールを割り当てることもできません。
  - キーワード照合と違い、取り出しは古いポーラーから**差し止めません**。落としても記録が無くなる
    （系列が見当たらないという形で見える）だけで誤った値にはならず、差し止めると `http_up` を含めて
    監視全体が止まってしまうためです。
- **`body_max_bytes` はルールではなく監視の設定になりました。** 本文は 1 つ、読み取りも 1 回、
  上限も 1 つで、キーワード照合と値の取り出しが共有します。既定 65536、範囲 1024〜1048576。
- 本文の機能を設定しても `http_response_time_ms` は応答**ヘッダ**までの時間のままです。どの監視でも
  同じ意味を保つためです。

- **収集しているメトリクスがすべて画面から見えるようになりました。** これまでノードの「収集」タブに
  出ていたのはスカラー種別の現在値だけで、グラフもありませんでした。ベンダー固有のテーブル列を
  自分で追加した運用者は、収集が成功していても数値を一度も見られない状態でした。収集タブが全件を
  一覧し、どのメトリクスもその場でグラフ化します。API は
  `GET /api/v1/nodes/{id}/metrics`、MCP ツールは `list_node_metrics` です。各項目は次の 3 つを示します。
  - `status` — `ok`（設定済みでデータあり）／`no_data`（設定済みだが届いていない）／
    `unconfigured`（収集項目は無いがデータがある）。最後のものは異常ではありません。到達性、
    URL 監視と DNS 監視、隣接数、監視対象の JSON 応答から取り出した値は収集セットではなく
    チェック側が生成するため、収集セットを見ている画面からは原理的に見えませんでした。
    その結果 `snmp_neighbor_count` を初めてグラフにできます。
  - `dimension` — `none`／`interface`（インターフェース別に読んでください）／`entity`（テーブルの
    行ごとに 1 系列）。行の識別子は収集の時点で失われるため、`entity` はノード全体の最大値として
    表示し、そうであることを明示します。出せない行別の内訳があるかのように見せないためです。
  - `metric_kind` — ゲージかカウンタか。グラフの描き方が決まります。
- **カウンタをレートとしてグラフ化できるようになりました。**
  `GET /api/v1/nodes/{id}/metrics/{metric}/range` に `rate=true` を渡すと、保存された値ではなく
  毎秒のレートを返します。これまでノード単位のカウンタを描く方法がありませんでした。保存された値は
  積算値そのもので、`agg=max` を掛けると通信量のように見える右肩上がりの線になるためです。
  `rate` と `agg` は併用できません（`rate_with_agg`）。行ごとのカウンタにノード単位のレートは
  定義できないためです。
- **ダッシュボードに「メトリクスグラフ」ウィジェットが増えました。** ノードを選び、その機器が実際に
  報告しているメトリクスの一覧から 1 つ選ぶと、直近 6 時間を描きます。ほかのカードはカタログが
  あらかじめ決めた問いに答えるものですが、これは「あなたの機器にあってこちらの機器には無い」
  メトリクス — ベンダー固有の温度、監視対象の JSON から取り出した値 — のためのものです。いくつでも
  追加できます。知っておくとよい挙動が 2 つあります。
  - **正直に描けるものだけを候補に出します。** インターフェース単位のメトリクスは一覧に出ません。
    ノード詳細の「インターフェース」タブがポートごとに描いており、8 ポートを 1 本の線にまとめるのは
    別の問いに答えることになるためです。カウンタは毎秒のレートとして描き、保存された値のままでは
    描きません。
  - **選んだメトリクスが無くなったときは、そう表示します。** ノードがそのメトリクスを報告しなくなった
    場合、空のグラフではなくメトリクス名を挙げて知らせます。
- **ダッシュボードに「メトリクス別の上位ノード」ウィジェットが増えました。** 全ノードを、入力した
  メトリクス名で順位付けします。既存の RTT / CPU / メモリの上位カードはカタログが選んだ 1 つの
  メトリクスを扱いますが、こちらは任意のメトリクスを、現在値または直近 1 時間のピークで並べます。
  どのカードも扱わないベンダー固有のメトリクスでも「どの機器が一番悪いか」を出せます。知っておくと
  よい挙動が 3 つあります。
  - **メトリクスは一覧から選ぶのではなく入力します。** 全ノードのメトリクス名を列挙する仕組みは
    意図的に用意していません。大規模なフリートで唯一持ちこたえられない問い合わせだからです。入力欄
    には、このブラウザーで開いたノードで見たメトリクスが候補として出ます。それ以外の名前も入力
    できます。
  - **カウンタは順位付けせずに断ります。** カウンタの保存値を並べると、負荷ではなく稼働時間の長さが
    上位に並びます。通信量やエラーはインターフェースの上位カードを、カウンタの推移はノード詳細の
    グラフ（毎秒のレート）をお使いください。
  - **結果が空のときは、空になりうる理由も出します。** 「報告しているノードが無い」のか「名前が
    一致していない」のかはブラウザー側では区別できないため、どちらもありうると明示します。

- **SSO プロバイダの追加が、まず IdP を選ぶ形になりました。** Microsoft Entra ID / Okta /
  Google Workspace / その他（任意の OIDC プロバイダ）から選ぶと、その IdP に必要な項目だけを
  訊きます。これまでは 8 項目すべてが自由入力で、運用者が「自分の IdP では何が正しいか」を
  既に知っている前提になっていました。その前提は成り立たず、**初期表示のスコープに含まれていた
  `groups` を Entra ID は拒否するため、Entra ではサインイン画面にすら到達できませんでした**。
  かといって `groups` を一律に外すのも誤りで、Okta はまさにそれでグループ情報を渡します。
  IdP ごとに次のようになります。
  - **Entra ID** はディレクトリ (テナント) ID だけを訊き、Issuer URL を組み立てます。要求する
    スコープは標準の OIDC スコープだけです。グループクレームはアプリ登録の Token configuration
    で有効にすること、値はグループのオブジェクト ID であることを画面に書いてあります。
  - **Okta** は org ドメインだけを訊いて Issuer URL を組み立て、org authorization server が
    そのまま扱える `groups` スコープを要求します。カスタム authorization server は Issuer が
    別なので「その他」を使う旨も書いてあります。
  - **Google Workspace** は Issuer が 1 つなので URL の入力欄がありません。**グループ →
    ロールの対応づけは意図的に無くしました。** Google は ID トークンにグループ情報を含めないため、
    設定しても永久に一致しないからです。理由を画面に示したうえで、既定ロールを必須にしています
    （未設定だと全員が拒否されるため）。
  - **「その他」** は従来のフォームそのままです。一覧に無い IdP はこちらを使います。
  - **既存のプロバイダは変わりません。** 一覧でも編集画面でも「その他」として扱われます。項目を
    1 つずつ入力して作られたものなので、それが実態どおりです。サインインの動作は何も変わらず、
    このフォームが組み立てない形の Issuer URL は、開いても保存されている値のままです。
  - API では `POST` / `PUT /api/v1/settings/oidc` が任意の `kind` を受け取り、一覧（および MCP の
    `get_config`）が返すようになりました。省略時は `generic` です。
  - ⚠️ **実際の Entra / Okta / Google テナントでの検証は行っていません。** 各 IdP が何を受け付ける
    かは、それぞれの公開ドキュメントの記載に基づいています。

### 改善
- **ノード概要のデバイスヘルスとメトリクス一覧に管理者権限が要らなくなりました。** 取得元が
  ManageConfig を要求する収集セットの API だったため、閲覧者には組み込みのメトリクス 1 本しか
  見えず、ヘルスのゲージは 1 つも出ていませんでした。読み取り権限だけで足りるメトリクス目録から
  取得するようにしました。
- **日本語 UI の文言を全面的に見直しました。** 同じ用語に 2 通りの表記が混在し、英語の文構造が
  そのまま残り、記法が画面ごとにばらついていました。ほとんどの画面で文言が変わりますが、動作は
  何も変わりません。今後の新規文言が揺れないよう、表記の規約も文書化しました。
- **スケジューラがスイープごとに全ノード分の `url_checks` を引かなくなりました。** URL 監視の
  ノード ID を DNS 監視と同じく 1 スイープに 1 回だけ先読みするようにしたので、数万台の通常機器を
  抱えるフリートが「URL チェックを持っていないこと」を確かめるためだけに毎周期 1 台 1 回の
  データベース往復を払うことがなくなりました。
- **WebUI がトポロジ / トラブルシュート / 設定の各セクションを必要になった時点で読み込むように
  なりました。** ダッシュボード・ノード・アラートといった日常的に使う画面はこれまでどおり最初の
  ダウンロードに含まれますが、この 3 セクションの奥にある約 25 画面は含まれなくなりました。
  ダッシュボードを開くだけで世界地図の輪郭・レポートのカタログ・17 個の設定画面まで取りに行く
  ことがなくなります。セクション内での画面移動は従来どおりです（最初の 1 回だけ読み込みます）。
- **フリート規模に比例する処理を毎周期行っていた 2 か所を解消しました。** 小規模な環境では見えま
  せんが、「何も変わっていない周期」の費用が 0 になるかどうかの差です。スケジューラは、各ノードの
  解決済みチェック集合（復号済みの認証情報・OID 列リスト・経路プローブ計画を含む）を「何も変わって
  いないと確認するためだけに」プールごと・スイープごとに複製しなくなりました。コーディネータは
  レジストリのロックをメンバーシップの読み取り中しか保持しなくなり、ポーラーのハートビートや
  Pollers 画面がワーキングセットの差分計算の後ろに並ぶことがなくなりました。アラート評価は、
  しきい値の解決をサンプルごとではなく 1 ポーリング結果あたり 1 メトリクスにまとめました。1 つの
  結果に同じメトリクスのサンプルが 100 個入る SNMP の広いテーブルで効きます。dwell の判定は従来
  どおりサンプルを 1 件ずつ見ています。まとめたのは検索だけです。
- **ノードツリーが状態更新のたびに再構築されなくなりました。** ステータスの点はフリート内の
  どのノードの変化でも流れてくるストリームで届きますが、画面上で何も動いていない場合でも
  そのたびにツリー全体を組み直していました。

### 不具合修正
- **ダッシュボードのサマリ・グループ別集計・ネットワークマップ・インベントリレポートが、ノード
  一覧と違う状態を表示することがなくなりました。** これら 5 つの画面は同じ規則 —— アラート
  エンジンが判断を持っていればそれ、無ければ直近の ICMP 応答があれば `ok` —— に従うはずでしたが、
  規則が 5 か所に手書きされており、そのうち 2 か所ではフォールバックが失われていました。症状が
  出るのは core の再起動直後です。最初のスイープが終わるまでの数分間、ダッシュボードは
  隣のノード一覧が `ok` と表示しているノードを `unknown` と報告し、その間に生成した PDF の
  インベントリレポートは状態の列がすべて `unknown` になっていました。規則は 1 か所にまとめました。
  **意図的に変えていない箇所**: フリートヘルスの時系列はエンジンの生の見え方のまま記録します。
  再起動直後の空白は履歴に残るべきものだからです（履歴は「その瞬間に実際に監視できていたか」を
  語るべきで、後から埋めると再起動の代償が消えます）。
- **`inventory` ティアを有効にした Cisco Meraki の組織で、ラベルの代わりに翻訳キー
  `meraki.tier.inventory` がそのまま表示されていました。** API は 4 つのティアすべてを受け付け、
  組織一覧は保存された値をそのまま表示しますが、名前が付いていたのは 3 つだけでした。収集間隔の
  ダイアログのチェックボックスは 3 つのままです。inventory は「デバイスの取り込み」から実行する
  棚卸しであって定期収集ではないため、チェックボックスを置くと行われないポーリングを約束して
  しまいます。

### セキュリティ
- **CSV エクスポートに、開いた瞬間に実行される数式を仕込めなくなりました。** 監査ログはログイン
  **失敗**時に入力されたユーザー名をそのまま記録するため、サインイン画面に到達できる人なら誰でも
  `=HYPERLINK("http://…"&A1,"Click")` のようなセルを仕込み、後でログをエクスポートして開いた
  管理者の環境で評価させることができました。トラブルシュートのエクスポートも機器由来の文字列を
  含むため同じ問題を抱えていました。RFC 4180 の引用符では防げません（表計算ソフトは引用符を外して
  から中身を評価します）。`=` `+` `-` `@` タブ・CR で始まる値の先頭にアポストロフィを付け、
  文字列として読ませるようにしました。**値全体が負の数**（`-5`、`-0.31`）だけは対象外で、従来
  どおり数値として出力します。トラブルシュートのレポートは相関係数を出力しており、これを文字列化
  すると 1 つの列が行によって文字列と数値に分かれ、見た目が変なだけでなく並べ替えが静かに壊れる
  ためです。
- **PDF レポートの生成がコンテナ内のファイルを読めなくなりました。** レポートは `wkhtmltopdf` で
  描画していますが、必要が無いのに `--enable-local-file-access` を渡していました。生成する文書は
  自己完結（スタイルもチャートの SVG もインライン、画像・リンク・`url()` なし）で、レポートが
  ファイルを読む必要は元々ありません。このフラグは、機器由来の文字列のエスケープ漏れをローカル
  ファイル読み出しに変えてしまうものなので、既定値に任せず明示的に
  `--disable-local-file-access` を渡すようにしました。
- **どの機器も到達可能と答えるテスト用トランスポートが、出荷するバイナリに含まれなくなりました。**
  テストビルドでのみ有効になるフィーチャーの下に移しました。

## v0.1.23 — 監視自身の盲点を知らせるアラート、サポートバンドル、自分で調べる RCA

### 破壊的変更
- **アラートの `node` フィールドは「主体」になり、常に UUID とは限らなくなりました。** ポーラープール
  のアラートでは `pool:<名前>` になります。影響範囲は `GET /api/v1/alerts`、
  `GET /api/v1/alerts/history`、`/api/v1/stream/alerts` のフレーム、および MCP のアラート系ツールです。
  アラートを含むレスポンスには `subject_kind`（`node` | `pool`）が、名前を持つ主体には
  `subject_name` が併せて入るようになりました — **`node` をノード ID として扱う前に `subject_kind`
  で分岐してください。**
  - 履歴行と MCP DTO では、ノード以外の主体の `node` / `node_id` は捏造した UUID ではなく `null` です。
  - `POST /api/v1/alerts/ack` は `node`（従来どおり）**または** `subject`（アラートの主体表記）の
    どちらでも受け付けます。`node` は必須ではなくなり、どちらも無い場合は `400 invalid_subject` です。
  - ノード指向の集計は影響を受けず、ノード限定のままです: `/api/v1/alerts/top-nodes` と
    `/api/v1/alerts/transitions` にプールは現れません。
- **これまで無制限に伸びていた 4 つの表が保持ポリシーを宣言しました(ADR-040)。** Troubleshoot の解析実行と
  その findings、生成された AI 根本原因レポート、監視の空白記録がスケジュールで刈られます。インタフェース
  対応表は「ノード削除まで保持」と理由付きで宣言しました。前者 3 つは導入以来まったく上限が無く —
  マイグレーション 0026 には "no auto-trim yet" とそのまま書かれており、以後スケジュール解析がその表に
  定期的に書き続けていました。
  - Settings ▸ System settings に **診断データ**の窓(既定 90 日)を追加し、解析実行と RCA レポートを
    まとめて扱います。「レポート実行」とは意図的に別にしています — 窓の**名前**が別種のデータを黙って
    支配してはならないからです。
  - 監視の空白はアラート連動の窓に従います。空白は「アラートが鳴らなかった理由」であり、それが説明する
    履歴の隣にあって初めて読めるものだからです。
  - インタフェース対応表は**年齢では刈りません**。理由はポリシー表に書いてあります: 孤立した
    `(ノード, ifIndex)` 行は古いデータではなく**陳腐化した同一性**であり、`last_seen` はインタフェース収集が
    動いている間しか進みません。年齢ベースの掃引は、収集を一時停止しただけのノードについて全インタフェースの
    名前と速度を消してしまいます。これらの行はノードと一緒に消えます。本来の修正はポーラー側の別変更です。

### 新機能
- **ポーラープールからポーラーが居なくなったことをアラートするようになりました。** ここは監視自身の
  盲点でした。アラートエンジンはポーリング結果をもとに判断するため、プールが最後の生存ポーラーを失うと
  判断材料そのものが来なくなります — スケジューラは誰も購読していないサブジェクトへジョブを流し、
  NATS はそれを捨て、ノードは *down* ではなく *unknown* に流れていきます。拠点まるごとが監視対象から
  外れているのに、どのダッシュボードも平穏に見えます。これまでこの状態は Settings ▸ Pollers を
  見ている人にしか届きませんでした。
  - 設定済みの通知チャネルへ **critical** で配信されるので、既存の `critical → PagerDuty`
    ルーティングルールがそのまま拾います。ポーラーが戻れば自動的にクローズされます。
  - **通知までに 5 分の継続が必要です** — ポーラーは終了時に自ら離脱を通知するため、通常の
    ローリング再起動でも条件は即座に成立します。誰も呼び出さないためのデバウンスです。
    `YAGRA_POOL_COVERAGE_ALERT_AFTER_SECS` で調整（既定 `300`、`0` で無効）。
  - この設定に関わらずゲージが 2 本増えます: `yagra_pools_without_live_poller`（ラベル無し。
    アラートやスケールアップの起点にするのはこちら）と `yagra_pool_nodes_without_live_poller{pool}`。
    後者は健全なプールでは系列が消えるのではなく `0` を報告します。
  - Meraki 管理ノードは Pollers 画面と同様に除外されます（core の org コレクタが直接ポーリングする
    ため、プールに依存しません）。
  - **通知だけでなく、通常のアラートとして扱われます。** Active alerts とアラート履歴に現れ、
    ライブストリームに流れ、外部インシデントツールから ack でき、通知テンプレートで描画されます。
    テンプレート変数が 2 つ増えました — `subject_kind` と、常に存在する `subject_name` — ので、
    デバイスにもプールにも正しく読めるテンプレートを 1 本で書けます。
  - **グループスコープの運用者には、自分のノードを担当しているプールが見えます。** 拠点が暗くなった
    ときにまさに見るべき人です。担当ノードが 1 つも属さないプールは、その人には見えません。
  - 意図的にやっていないことが 2 つあります: ノードの表示状態には**絶対に**丸め込まれません
    （どのノードのものでもないため）。そして mute はできません — mute はノードを名指しするものです。
- **MCP から Yagra 自身の設定を読めるようになりました — `get_config(kind=…)`。** 1 本のツールで
  28 種類の読み取りに対応します: しきい値、イベントルールとイベントソース、通知チャネルとルーティング
  ルール、プロファイルと収集テンプレート、ノードごとの収集メトリクス、分類ルール、MIB カタログ、
  ノードの URL/DNS チェック、ディスカバリ候補とスキャン、Meraki の組織/ネットワーク/ポーリング、
  転送先、レポート定義とスケジュール、そして保持期間 / 隣接収集 / LLM / ロール / OIDC / LDAP の各設定。
  これで ADR-042 の読み取り同等バックログは解消し、**WebUI で見られる読み取りは購読トランスポートを
  持たない SSE 4 本を除いてすべて `/mcp` からも見られる**ようになりました。
  - **read-only は「誰でも読める」ではありません。** 各 `kind` は対応する REST と同じ権限を要求します
    — OIDC と LDAP は `manage-users`、14 種は `manage-config`、残りは `view`。したがって Viewer は
    同じツールからロール一覧を返され、しきい値一覧は拒否されます。
  - 保存された秘密は返しません。ノードの URL チェックは資格情報が**紐づいているか否か**
    (`has_credential`) だけを返し、どれかは返しません。REST のレスポンスは変更していません。
- **LLM 根本原因分析が自分で調べに行けるようになりました。** 従来は固定の事実集合（アラート、配下ノード、
  上流の連鎖、シグナルの時系列、直近の設定変更）を渡して 1 回で答えさせる形だったため、
  **あらかじめ誰かが「入れる」と決めたものについてしか推論できません**でした。今は読み取り専用の MCP
  ツールを渡すので、インターフェースの時系列を引く、そもそもポーラーが生きていたかを確認する、
  syslog に何が出ていたかを見る、発火したしきい値を読む、といった動きを自分で行います。
  - **呼び出し元自身の可視スコープで動作**します。グループスコープ付きのオペレータの分析が、
    その人に見えないノードを読むことはありません。ツールは **view 権限のみ**の許可リストに限定され、
    書込 3 本・`run_analysis`・`run_rca`・監査ログは届きません。判定はツール単位ではなく
    **畳んだ分岐単位**で行います。
  - **何を調べたかは答えと一緒に保存**され（レポート本文の `transcript`）、両方の面で再生されます。
    証跡を一緒に保存してきたのと同じ理由です — 読み手が根拠を確認できない説明は主張にすぎません。
  - 上限はターン数（`YAGRA_RCA_MAX_TURNS`、既定 6）、壁時計（`YAGRA_RCA_TASK_BUDGET_SECS`、既定 240 秒）、
    ツール出力の総量。上限に当たった場合はリクエストを失敗させずモデルの最後の回答を返します。
    **`YAGRA_RCA_MAX_TURNS=1` で従来の単発動作に完全に戻ります** — ツールは一切提示されず、
    プロバイダに送るリクエストは以前とバイト単位で同一です。
  - ツール出力は装置由来のテキストとして扱い、同じフェンスで囲みます。「指示を無視しろ」と書かれた
    syslog 行は、装置の `sysDescr` が常にそうであったのと同じ untrusted マーカーの内側に入ります。
- **サポートバンドル（設定 ▸ システムヘルス）。** 外部から配備を診断するために必要なものを 1 つの
  ダウンロードにまとめます: 実際に動いているバイナリの素性（バージョンだけでなくイメージの
  source ref とビルドプロファイル）、システムヘルスの全セクション、許可リスト方式で収集した環境変数、
  チェックサム付きの適用済みマイグレーション、テーブルごとのサイズと接続数、アクティブアラート、
  監査ログの末尾、core の Prometheus スクレイプ、そして core 自身のローテート済みログファイル。
  API は `GET /api/v1/system/support-bundle?since_hours=N`。
  - シェルに入れず、データを気軽に持ち出せない現場を想定しているため、アーカイブは**外に出す前に
    人がレビューできる**ように作ってあります: 中身はすべて JSON かプレーンテキストで、
    `MANIFEST.json` には「何を入れたか」と**「何を意図的に入れなかったか」**が理由付きで並びます。
  - 秘匿情報の扱いは 2 段構えです。環境変数は**許可リスト**で収集します — パスワードらしい名前を
    弾く拒否リストでは、実際に出荷される `YAGRA_DATABASE_URL` の userinfo に埋まった資格情報を
    取りこぼすためです。そのうえで組み上げた全バイトを検査し、該当した場合は伏せ字にせず
    **書き出し自体を中止**します。最も強い規則はパターンではなく、core プロセスが自分の環境から
    見える**実際の秘匿値そのもの**の集合で、想定外の経路で混入した資格情報も捕まえられます。
    中止時は `500 support_bundle_redaction_failed` を返し、該当ファイルと規則は示しますが値は
    決して示しません。
  - 権限は **ManageConfig + ManageCredentials + ViewAudit の 3 つすべて**が必要です。どちらにも
    言及しないエンドポイント経由で監査ログや資格情報レポートを読む抜け道にしないためで、実質的に
    Admin 限定です。
- **core のログを stdout に加えてディスクにも書くようになりました。** `YAGRA_LOG_DIR` 配下に
  1 時間ごとの JSON Lines ファイルを `YAGRA_LOG_RETAIN_HOURS` 個（既定 48、自動削除）、
  名前付きボリューム上に保持するのでコンテナより長生きします。`docker logs` を読むにはホストの
  シェルが必要で、それこそが閉じた配備では得られないものであり、これまで panic や OOM は何も
  取り出せないまま消えていました。復旧**後**に取ったバンドルにも、落ちた側の実行が残ります。
  - `docker-compose.yml` と `docker-compose.deploy.yml` では**既定で有効**です。無効にするには
    `.env` で `YAGRA_LOG_DIR` を空にしてください。書き込みはノンブロッキングで、詰まった場合は
    ポールループを止めずに行を捨てます。ディレクトリが書けない場合は起動を失敗させず、警告を
    出して stdout のみに縮退します。
  - ポーラーも同じ方法で有効化できます（イメージ側にディレクトリはあります）が、どの compose
    ファイルもマウントしていません: バンドルが運ぶのは core のログだけです。ポーラーのログ本文を
    集めるにはバス経由になり、それは読み取りではなく新しいバスメッセージの追加になります。
    ポーラーのハートビートカウンタ、ポールループ統計、ホストリソースはすでにバンドルに含まれます。
- **`incident_correlate` がトポロジ隣接をまたいで相関するようになりました。** ノード自身と、直結する
  上流/下流の peer の信号が時間的に一致(5 分以内)したときに 1 つのインシデントとして組み立てます。
  上流 1 本の断が、無関係な単一ノード findings の羅列ではなく、下流の peer を名指した 1 件として
  読めるようになります。各 finding には裏付けとなった隣接ノードが載り、peer 由来のタイムライン項目には
  そのノード名が付きます。
  - トポロジモードに関わらず、**自動導出された接続グラフと手書きの親の両方**を使います。モードゲートが
    存在するのは、誤った導出辺が実際のアラートを**抑制**してしまい沈黙は回復不能だからで、余計な peer を
    名指すだけの診断はうるさい方向にしか転びません。したがって既定の `manual` モードのままの配備でも
    機能します。
  - finding が立つにはノード自身の信号が必要です — 隣接は裏付けであって、捏造ではありません。peer には
    finding ごとの上限があり、抑制の対象外に設定されたノードは誰かの上流としても推論されません。
  - 2 台にまたがるインシデントは各ノードに 1 件ずつ finding を生み、互いを名指します。どちらも影響を
    受けているためで、ノード単位の帰属を保つことでレポートのノード数も正直なままになります。

### 改善
- **Troubleshoot の受動イベント解析が、ログストア設定時にログストアを読むようになりました。** これまでは
  PostgreSQL を直接読んでおり、VictoriaLogs を有効にすると PostgreSQL にはアラート紐付きの行しか残らない
  (ADR-024) ため、**すでにアラートになったイベントの部分集合**について答えていました。極端だったのが
  `rule_gap` です。目的が「マッチしなかった大量イベントを見つけること」なのに、未マッチイベントは
  ログストア構成では PostgreSQL に一切書かれないため、**それを必要とするだけの syslog 量がある配備に
  限って、構造的に必ず空を返して**いました。
  - `event_storm` / `severity_shift` / `auth_probe` と `incident_correlate` のイベント経路が、部分集合では
    なく全量を数えるようになりました。同じクエリ 3 本で組まれていた MCP の `event_stats` ツールも、
    訂正後の答えを引き継ぎます。
  - `event_flap` は変更なしで、元から完全です。数えている action はすべてアラート紐付きなので、
    PostgreSQL はどちらの構成でも全件を保持します。これはコメントではなくテストで固定しました。
  - ログストアの失敗時に PostgreSQL へフォールバックはせず、解析を失敗させます。フォールバックは
    「何も告げずにまた部分集合から答える」ことであり、今回直している欠陥そのものだからです。
- **未マッチイベントを機器自身のイベントコードでまとめるようになりました。** Troubleshoot の
  **ルールギャップ**解析(および MCP の `event_stats`)は、trap OID か syslog の APP-NAME でイベントを
  まとめていました。ところが実在するネットワーク機器のかなりの割合はそのどちらも供給しません — タイム
  スタンプの書式が RFC 3164 にも RFC 5424 にも当てはまらず、データグラムが生テキストとして解析されるため
  APP-NAME が取れないのです。その結果、1 日に十数万件のイベントを送る機器について findings が **0 件**に
  なりえます。「0 件」は「未処理のイベントは無い」と読めますが、実際の意味は「測定できなかった」でした。
  今後は取り込み時にメッセージ本文からベンダー固有のコードを抽出してまとめます
  (`%%01URL/4/FILTER(l):` → `URL/4/FILTER`、`%LINEPROTO-5-UPDOWN:` → `LINEPROTO-5-UPDOWN`、
  先頭の `SNMP_TRAP_LINK_DOWN:` など)。
  - 抽出されるコードは常に**メッセージ本文の逐語の一部**です。したがって finding に出た署名をそのまま
    `substring` のイベントルールに貼ればマッチします — ルールギャップが促す行動そのものです。ルールの
    照合はメッセージ本文に対してプロセス内で行われるため、これはどの構成でも成り立ちます。
  - ⚠️ 一方、署名の**全体**をフリートテキスト検索に貼って当たることは、VictoriaLogs 構成では保証されま
    せん。LogsQL はトークン単位で照合するため、ベンダー接頭辞がコードと地続きになっている場合
    （`%%01ATK/4/…` は `ATK` ではなく `01ATK` としてトークン化される）先頭区間で当たりません。特徴的な
    末尾（`FIREWALLATCK`）で検索してください。PostgreSQL のみの構成は部分一致なので影響を受けません。
    これはプレーンな検索語について既に文書化されているバックエンド差と同じものです。
  - 優先順位は trap OID → 機器のイベントコード → APP-NAME です。APP-NAME を送れている機器の配備は
    従来どおり動きます。**両方**を送る機器はより具体的なコード側でまとまるため、**既存の
    ルールギャップ finding がより細かい複数の finding に分かれることがあります**。
  - 抽出は新しく受信したイベントにのみ適用されます。過去分の再処理は行いませんし、必要もありません —
    この解析が読むのは未マッチイベントだけで、それらは保持期間内に入れ替わるからです。
- **`GET /api/v1/mib-catalog` が `limit` を受け付け、件数上限を持つようになりました。** このクエリは
  どちらのエッジにも上限が無く、呼び出し元が設定画面だけだった間は許容できましたが、AI クライアントが
  カタログ全件を要求できるようになった時点で許容できなくなりました。`limit` は 1–2000、既定は 2000 な
  ので、カタログが 2000 件を超えていない限り既存の呼び出し元に変化はありません。
- `MANIFEST.json` は効いたサイズ上限を両方とも報告します（サイズ超過で落としたログファイル数と、
  指定期間より古いファイル数）。黙って切り詰められたログは「何も記録されていない」と読めてしまい、
  それは欠落ではなく誤答だからです。
- 秘匿検査の報告に `secret_literals_enforced` と並べて **`secret_literals_skipped_short`** を
  出すようにし、`README.txt` はこの 2 つを文章で解釈します。`enforced` が 0 という数字だけでは、
  レビュアには 3 つの意味が区別できません: この配備の環境に秘匿値が無い / 秘匿値はあるが安全に
  照合できる長さに満たなかった / 収集そのものが壊れた。1 つ目は「問題なし」で、3 つ目は
  **検査の中で最も強い規則が黙って動かなかった**という意味です。最初の実バンドルで発覚し、
  どれなのかを確定するのに配備側へシェルで入る必要がありました — サポートバンドルが無くすはずの
  作業そのものです。
  - 8 文字の下限は**下げません**。ラボの `POSTGRES_PASSWORD=yagra` のような設定では `yagra` が
    禁止語になり、アーカイブ内のあらゆるパスやテーブル名に出てくるため、全バンドルが永久に
    拒否されます。対処は「弾いた数を数えること」です。README には、検査がパターン照合だけに
    後退した場合にその旨と対処方法をはっきり書き出します。
- **グループスコープ付きの `rule_gap` / `auth_probe` が、後段ではなくストア側で絞り込むようになりました。**
  従来はフリート全体で集計してから「**代表**ノードがスコープ内か」で残す方式だったため、グループ外の
  ノードがたまたまソート順で先だっただけで、グループ内で実際に起きている signature が消えていました。
  `auth_probe` はさらに、在庫ノードに紐付かない認証失敗元をすべて隠していました — 外部からの
  プローブはまさにその形をしています。**以前より多くの行が出ますが**、それらは元々あなたのものです。
- `PUT /api/v1/settings/retention` に `diagnostic_days` が増えました。任意フィールドなので従来の
  4 フィールドの本文を送るクライアントはそのまま動きますが、その本文は全置換であるためこの窓は
  既定(90)にリセットされます。WebUI は常に全フィールドを送ります。

### バグ修正
- `db/connections.json` が接続経過秒に負値を出さなくなりました。`now()` はトランザクション開始
  時刻で、その問い合わせ自身のバックエンドの `state_change` はそれより後になるため、`active` 行が
  数ミリ秒のマイナスで返っていました。配備の事実ではなく、内側から測ったことによる副作用です。

### セキュリティ
- **IdP がグループクレームを別経路で配る場合、OIDC ログインを拒否するようになりました** — Microsoft Entra の
  「group overage」、つまりおよそ 200 を超えるグループに属するユーザーに対して、グループの**代わりに**
  `_claim_names` / `_claim_sources`(または `hasgroups`)が返る挙動です。Yagra は ID トークンからのみ
  グループを読むため、従来はグループが空だと解釈してプロバイダの `default_role` に落ちていました。
  これは失敗ではなく**黙った権限の変更**です: 管理者は既定のロールでサインインし、既定が Admin であれば
  閲覧者であるべきユーザーが管理者としてサインインしていました。どちらも運用者が気づける痕跡を
  残しませんでした。
  - クライアントへの応答は他の SSO 拒否とまったく同じ汎用 401 のままです — コールバックは、どの段階で
    失敗したかを部外者に教えない設計です。監査ログには `auth.oidc.group_overage` を**ユーザー名付きで**
    記録し(この分岐に到達するには検証済みの ID トークンが必要なので、助けてしまうプローバは存在しません)、
    core のログにクレーム名と対処方法を出します。
  - ⚠️ **このフォールバックに依存しているテナントでは、該当ユーザーがサインインできなくなります。**
    アプリ登録に「割り当て済みグループのみ」を出させるか、グループフィルタクレームを使ってください。
    LDAP のロールマッピングは変更ありません。

## v0.1.22 — 既定で HTTPS、自分で描かれるネットワークマップ、ディレクトリ認証

### 破壊的変更
- **WebUI が HTTPS になり、ポートは 443、平文リスナは廃止されました。** UI が運ぶもの — ログイン
  パスワード、ベアラートークン、暗号化される前のデバイス認証情報 — は、これまで既定で平文のまま
  ネットワークを渡っていました。v0.1.9 でその直し方とコメントアウト済みの設定ブロックを同梱しま
  したが、1 年経っても誰もコメントを外しませんでした。そこで、**何もしなくても手に入る側**を安全な
  構成にしました。
  - **`http://<host>:3000` は応答しなくなります。** `.env` に `YAGRA_WEB_PORT=3000` が残っている
    場合はポート 3000 のままで `https://<host>:3000` になります — ポートは据え置き、スキームが
    変わりました。この行を削除すると 443 に乗ります。
  - **リダイレクトは検討のうえ意図的に却下しました。** webhook 送信実装の多くはリダイレクトを追わず、
    追う実装は `POST` の `301` を `GET` に変えます。つまり
    `POST /api/v1/ingest/webhook/:source_id` は成功を返し続けたまま、イベントだけが届かなくなります。
    接続拒否のほうが「見える失敗」です。**webhook 送信元とその他の機械クライアントは**
    `https://<host>/api/v1/…`、または据え置きの平文 `http://<host>:8080/api/v1/…` に移してください。
  - **全員がサインアウトされ、保存済みの UI 状態が失われます。** `http://host:3000` と
    `https://host` は別オリジンなので、セッショントークン・ダッシュボード配置・テーマ・テーブル設定は
    引き継がれません。見合うだけの複雑さで実現できる移行手段が無いため用意していません。サインイン
    し直せば、配置は使いながら再構築されます。
  - **SSO は 2 か所を直すまで止まります。** 保存済みの OIDC リダイレクト URI は絶対 URL なので、
    新しいスキームとポートに更新する必要があり、同じ値を IdP 側でも更新する必要があります。保存値が
    現在アクセスしているアドレスと食い違うとき、Settings ▸ Auth に警告を出します。Yagra が自動で
    書き換えることはしません — IdP が認可コードを送ってよい先を、アップグレードが勝手に変えてよい
    はずがないからです。
  - **初回アクセスでブラウザが警告します。** 取り込むまで証明書は自己署名であり、使用するホスト名を
    Yagra は知り得ないので、名前も一致しないのが普通です。**Settings ▸ TLS** で正式な証明書を取り込むか、
    同じ画面で正しい名前を指定して自己署名証明書を再生成してください。
  - **`YAGRA_WEB_TLS=off`** を設定すると、コンテナは従来どおり平文 HTTP を配ります — 手前の外部
    リバースプロキシやロードバランサが既に HTTPS を終端している構成のための選択肢です。
  - **core 自身の API ポートは変更していません**（平文のまま、LAN に公開のまま）。これは見落としでは
    なく意図した順序です。信頼されていない証明書を導入するのと同じアップグレードでこのポートを閉じると、
    Prometheus の scrape も API スクリプトも一斉に落ち、原因が 2 つ重なって切り分けできません。それらの
    クライアントを、信頼できる証明書のある TLS エッジへ移し終えてから
    **`YAGRA_API_BIND=127.0.0.1`** でネットワークから外してください。現在の状態は Settings ▸ TLS に出ます。
  - **独自の compose ファイルを保守している場合、core のコンテナはユーザーだけでなくグループも
    固定になりました（どちらも `10001`）。** 証明書バンドルはそのグループ所有の `0640` で書かれ、
    web コンテナは `group_add: ["10001"]` でそのグループに入って読みます。グループをベースイメージ
    任せにすると、nginx はディレクトリを通過できてもファイル自体で拒否され、他のシグナルはすべて
    正常のまま WebUI だけが上がってきません。同梱の compose ファイルは既に対応済みです。

### 新機能
- **WebUI から自分の TLS 証明書を取り込めるようになりました。** Settings ▸ TLS は今配っている証明書
  （サブジェクト、発行者、対象の名前、有効期限、フィンガープリント）を表示し、PEM の証明書チェーンと
  秘密鍵を貼り付けかファイル選択で受け取ります。新しい証明書は数秒で有効になり、何も再起動しません。
  対応していない鍵と証明書の組、期限切れ、サブジェクト代替名の無い証明書は取り込まず、**次のハンド
  シェイクで失敗するのではなく、そのどれなのかをその場で言います**。暗号化された秘密鍵と `.pfx` は、
  変換用の `openssl` コマンドを添えて拒否します。自己署名証明書は実際に使うホスト名や IP を指定して
  再生成でき、期限が来る前に自動更新されます。
  - 秘密鍵は他の秘密と同じく保存時に封筒暗号化され、API から返ることはありません。証明書はダウンロード
    できるので、Prometheus の `ca_file`、`curl --cacert`、OS のトラストストアに渡せます。
  - **期限が近づくとシステムヘルスが知らせます。** 証明書が切れると WebUI 全体が落ち、直すための
    画面ごと使えなくなります。そこで**システムヘルス**の 1 行として表示し、そのまま MCP の
    `get_system_health` ツールからも見えます。*劣化* とするのは人が動く必要がある場合だけ —
    期限切れ、または**取り込んだ**証明書の残り 30 日以内です。自己署名証明書は自動更新されるため、
    期限が近くても問題として報告しません。同じ事実は Prometheus エンドポイントにも
    `yagra_web_tls_expires_in_days` として出るので、自前のアラートに使えます。
  - **NATS バスの証明書は対象外**です。あちらはバス自身が起動時に読みます。
- **AI クライアントが「Yagra 自身は健全か」を訊けるようになりました。** これまで MCP 面には監視
  システム自身の状態を見る手段がありませんでした。これは見た目より重要な欠落で、ポーラーが落ちて
  いることもストアに届いていないことも分からなければ、モデルは**データが無いこと**を「静かで健全」
  と読み、フリートは正常だと報告してしまいます。新規 6 ツールで解消しました（`/mcp`、既定は OFF）:
  - **`get_system_health(section=…)`** — ポーラー一覧とプール別サマリ、ポーリングループの計数、
    あるポーラーが持つノード、あるノードを持つポーラー、直近の core⇄poller 断、ストア別の到達性、
    core/poller のホストリソースとその推移、転送の配送状況、保管中の資格情報が今も復号できるか、
    稼働バージョン、有効な任意ティア。
  - **`get_report_runs`**、**`get_audit`**（誰が何を変更・確認したか）、
    **`fleet_state_history`**、**`get_dns_chain`**、そして **`run_rca`** —
    WebUI の「このインシデントを説明」と同じ、LLM によるインシデント解説。
  - **`get_fleet_summary(kind="coverage")`** で「実際に最近レポートしているノードはどれか」と、
    していないノードの監視リストが得られます。
  - **セクションごとに必要な権限が異なり、WebUI と完全に一致します**: 大半は view、転送状況は
    manage-config、資格情報の健全性は manage-credentials、監査ログは view-audit、`run_rca` は
    ack-alerts。**読み取り専用は「誰でも読める」という意味ではありません。** レポートと状態推移は
    REST と同様、グループスコープ付きトークンに対してフリート全体を見せるのではなく拒否します。
- **ネットワークマップが、手で入力した親子リンクではなく実際のネットワークを描くようになりました。**
  Yagra が機器の報告から接続グラフを導出します — CDP/LLDP の隣接情報と、同一 IP サブネットに
  インタフェースアドレスを持つノード同士です。同じサブネットに足を持つことは「隣接している」という
  事実そのものなので、リンクを 1 本も入力しなくてもマップが現れます。各辺は根拠（LLDP・CDP・
  サブネット共有）を保持し、マップ上でも凡例でも区別して表示されます。冗長経路は畳まずに保持するため、
  2 台のルータ経由で到達するサーバには 2 本のリンクが出ます。**これ単体で変わるのはマップの描画だけ**で、
  アラート抑制は明示的に切り替えるまで従来どおり手動の依存関係グラフに従います（切り替えは次の項目）。
- **新しい読み取りエンドポイント `GET /api/v1/topology/links`** が、このグラフを keyset ページで
  返します。導出が観測したもののリンクにしなかった事柄（照合できなかった隣接、複数ノードに一致した
  管理アドレス、ルータを特定できなかったセグメント）は `summary` に集計されます。グループスコープ付きの
  呼び出し元には、**両端**が可視なリンクだけが返ります。
- **ネットワークマップが「同一サブネットを共有しないリンク」も見つけるようになりました。**
  これまでリンクは「2 台が同じプレフィクスにアドレスを持つ」ことから導出しており、点対点の
  `/32`（PPPoE の `Dialer`、トンネル端点）、アンナンバードの OSPF リンク、アドレス情報を収集
  できていないセグメント越しのピアリングは、構造上どうしても見えませんでした。今回から各機器の
  **OSPF 隣接と BGP ピア**も読み、さらにルーティングテーブルには特定の宛先だけを問い合わせます。
  これらのリンクは `OSPF 隣接` / `BGP ピア` / `直結経路` という根拠つきでマップに表示されます。
  複数の経路で同じリンクが見えた場合も、リンクは 1 本のまま根拠が積み上がります。
  - **既定で有効**。他の自動走査 2 種と同じ 1 時間周期で、設定 ▸ システム設定 ▸ ネットワーク探索の
    走査から切り替えられます。ARP 走査と違い、ここで読む表の大きさは機器自身のピア数に比例する
    もので、ネットワークの規模には比例しません。
  - **ルーティングテーブルを表として歩くことはありません。** フルルートを持つルータでは数十万行に
    なるため、Yagra は宛先を 1 件ずつ問い合わせます。しかも問い合わせるのは自分自身がホスト
    アドレスを持つ機器だけで、上限は 64 宛先です。通常の機器しかないフリートでは、経路の問い合わせ
    は 1 件も発生しません。
  - **セッションが落ちていてもリンクは描かれます。** `active` の BGP セッションは「障害のある
    リンク」であり、まさにそれが調査対象です。説明するために存在するはずのトポロジが障害と歩調を
    合わせて消えるのでは本末転倒です。
  - **ループバック同士の iBGP はリンクになりません。** BGP ピアは「報告した機器が終端している
    ネットワーク上にピアのアドレスがある」ときだけ隣接とみなすため、ルートリフレクタが全クライアント
    への偽の star を獲得することはありません。この判定で除外したピア数はマップの他の診断値と並べて
    報告されます。
  - 半端に答えず明示する既知の制限: BGP4-MIB は IPv4 専用のため **IPv6 の BGP ピアは対象外**、
    OSPF の収集は OSPFv2、仮想リンク（`ospfVirtNbrTable`）は読みません。従来からの制限も 1 つ
    残ります: メンバーが 3 つ以上あり、どれがルータか特定できないセグメントは、推測でリンクを
    作らず**何も出さず**にマップの集計に計上します。
- **「監視していないがネットワーク上に居るもの」を Yagra が提示できるようになりました。**
  新しい **ARP / IPv6 近隣キャッシュ** の走査（設定 ▸ システム設定 ▸ ネットワーク探索の走査）を
  有効にすると、監視中のルータが実際に通信した相手のホストを報告します。在庫に無いアドレスは
  **ノード ▸ ディスカバリ ▸ ネットワーク上で観測された端点** に、アドレス・MAC・どの機器のどのポートで
  見えたかとともに一覧され、*監視を開始* ボタンでサブネットスキャンと同じインポート経路を通って
  ノードになります。スキャンは不要で、既存のポーリングの副産物として得られます。
  - ⚠️ **既定では無効です（意図的）。** 他の 2 つの探索走査と違い、この走査だけは「機器の規模」ではなく
    「ネットワークの規模」に比例する表を読みます（キャンパススイッチでは数千行）。そのため
    アップグレードによって勝手に走り始めることはありません。既定周期は 6 時間です。
  - 一覧は「どちらの空か」を明示します。「まだどの機器も報告していない」（＝誰も見ていない）と
    「監視外アドレスは 0 件」（＝見た上で何も無い）は別物です。また、いずれかのルータのキャッシュが
    行数上限に達した場合は、一覧が完全な答えではなく **標本** であることを表示します。
  - 発見された端点は**意図的にネットワークマップの頂点にしません**。監視外のホストは表示すべき状態を
    持たず、状態の無い箱を数千個描けば状態を持つノードが埋もれるためです。インポートすればノードになり、
    そこからは通常の導出が拾います。
  - 追加: `GET /api/v1/discovered-endpoints`（keyset ページング、観測元ノード経由でグループスコープ適用）と
    `POST /api/v1/discovered-endpoints/{id}/import`、および MCP ツール `list_discovered_endpoints`。
  - 行は最終観測から 7 日で削除され、テーブル全体にも上限があります。
- **インタフェースアドレス走査にようやく UI が付きました。** 出荷時から動作していましたが、設定カードは
  CDP/LLDP しか知りませんでした。設定 ▸ システム設定 に 3 つの探索走査（隣接情報・インタフェース
  アドレス・ARP）が並び、それぞれ個別のスイッチと周期を持ちます。
- **特定の 1 台を自動導出の抑制から完全に除外できます。** **トポロジ ▸ 依存関係** でノードの
  *抑制しない* にチェックを入れる（または `PUT /api/v1/nodes/{id}/suppression-opt-out`）と、
  導出グラフが何と言おうとそのノードのアラートは常に単独で上がります。ノード自体はグラフ上に残るため、
  その先にあるノードは引き続きそこを経由して解決されます。この設定は**抑制を減らす方向にしか効かない**
  ため障害を隠すことはなく、辺単位の承認が却下された一方でノード単位のスイッチが許容される理由でもあります。
- **比較画面に、自動導出グラフがフリートをどれだけカバーしているかを表示**するようになりました:
  上流が付くノード数、手動で除外したノード数、比較を開始してからの経過。これらは助言であってゲートでは
  ありません — ブロックする条件は依然として「配置できないポーラーがある」ことだけです。
- **自動導出グラフがアラートに何をするかを、実際に適用する前に確認できます。**
  **トポロジ ▸ 依存関係** に 3 つのモードを持つ切り替えを追加しました。既定は *手動作成のグラフ* で、
  既存のデプロイはすべてここに留まります。*比較中* はアラート動作を一切変えず、Yagra が自動導出した
  グラフと手動管理のグラフがどこで食い違うかをノード単位で表示します。あわせて重要な 2 つの数値
  — 自動導出グラフなら**新たに抑制される**アクティブアラート数（危険な方向。それぞれが上がらなく
  なるアラートです）と、抑制が解除されるアラート数 — も表示します。*自動導出のグラフ* で抑制を
  引き渡します。モードが自動で動くことはなく、アップグレードは元のモードのまま起動します。
- **依存抑制がノードごとに複数の上流を持てるようになりました。** 自動導出グラフは、ポーラーから
  1 ホップ近い隣接ノードをすべて親として与えます。冗長化されたルータ 2 台の先にあるサーバーは
  両方を親に持ち、どちらかが生きている限り自身のアラートは上がり続けます。これは `is_suppressed` の
  「**すべての**親がダウンしたときだけ抑制する」規則が本来想定していた形で、手入力の単一 `parent_id`
  では表現できませんでした。
- **誤ったリンクは回避策ではなく修正できます。** リンクに対する運用者の判断 — 存在を `pin` する、
  `hide` する、どちらの端が上流かを宣言する — を記録するエンドポイントを追加しました。これらは
  再計算のたびに導出結果より必ず優先されます。`GET`/`POST /api/v1/topology/link-overrides` および
  `DELETE /api/v1/topology/link-overrides/{id}`。pin されたリンクは、観測されなくなった導出リンクの
  ように期限切れになることはありません。
- **`GET /api/v1/topology/shadow`** が比較結果全体を返します: エッジ数、方向ごとの差分エッジ、
  影響を受けるアクティブアラート、グラフの根となるノード、配置できなかったポーラーを持つプール。
- **MCP の `get_topology` ツールに `kind` パラメータが増えました**: `dependency`（既定で、既存の
  呼び出しはすべてこの挙動のまま）、接続グラフの `links`、運用者の判断と比較結果を返す `overrides`
  と `shadow`。「ここで自動導出の抑制を有効にして安全か」という問いに、AI クライアントが運用者と
  同じデータから答えられるようになります。
- **ポーラーが自身のインターフェースアドレスを報告し、アンカーノードを設定できるようになりました。**
  自動導出グラフの向きはポーラーからの距離で決まるため、各ポーラーの位置を知る必要があります。
  通常はポーラーが報告するアドレスから判定しますが、⚠️ **コンテナ内で動作するポーラーはどの監視対象
  とも一致しないコンテナネットワークのアドレスを報告します**。これは例外ではなく通常のケースです。
  **設定 ▸ ポーラ** に *アンカーノード* 列を追加し、そのようなポーラーの実際の接続先を指定できます
  (`PUT /api/v1/pollers/{id}/anchor`)。`GET /api/v1/pollers` は `mgmt_addrs` と `anchor_node_id` を
  返すようになりました。ノードを持つプールに配置できないポーラーがある間、自動導出グラフへの切り替えは
  **拒否されます** — そのプールは根を 1 つも持たないため、画面上は機能が有効に見えたまま何も抑制
  されない状態になるからです。
- **`PUT /api/v1/settings/topology`** でモード (`manual` / `shadow` / `derived`) を設定します。
  対応する `GET` は意図的に用意していません。現在のモードは `/topology/shadow` の応答に含まれます。
- **LDAP / Active Directory のアカウントでサインインできるようになりました。**
  **設定 ▸ 認証 ▸ ディレクトリ (LDAP/AD)** でディレクトリを設定すると、通常のログインフォームから
  社内アカウントでログインできます（別ボタンも別 URL もありません）。サービスアカウントで対象者を
  検索し、見つかったエントリの DN で再バインドするため、DN の組み立てパターンを当てる必要はありません。
  所属グループは SSO プロバイダと同じマッピング機構でロールに対応し、完全な DN でも名前だけでも
  一致します。アカウントは初回サインイン時に自動作成されます。**ローカルアカウントが常に先に試される**
  ので、ディレクトリに到達できなくなっても管理者が締め出されることはありません — ローカル管理者を 1 つ
  残しておけばロールバックしても復帰できます。通信は LDAPS と StartTLS のみで、社内 CA の入力欄があり、
  証明書検証を無効化する手段は意図的に用意していません。**テスト**ボタンは段階ごとに結果を報告し、
  ユーザー名を渡すと DN・グループ・**そのユーザーが得るロール**を表示します（「拒否される」場合も含みます
  — ログイン画面ではパスワード違いと区別が付かない状態です）。ディレクトリを設定しないデプロイの挙動は
  一切変わりません。
- **SAML には、自前実装ではなくブリッジ構成で回答します。** `DEPLOYMENT.md` に、前段に Keycloak か
  Dex を SAML→OIDC ブリッジとして置く構成と、Yagra が XML 署名検証を自前で行わない理由を記載しました。

### 改善
- **Yagra-core と Yagra-poller のメモリ使用量が大幅に減り、増え続けなくなりました。** 両バイナリが
  システムのアロケータではなく mimalloc を使うようになりました。50,000 ノード構成・20 分間・
  ポーリングスループット同一の条件で実測したところ、core の常駐メモリは平均 **397 MB → 183 MB**、
  20 分間の増加量は **162 MB → 6 MB** になり、poller は増加そのものが止まりました。従来の挙動は
  リークではなく、システムのアロケータがスレッドごとのアリーナを解放せず抱え続けていたものです。
  そのため core のフットプリントは観測している限り増え続け、必要メモリを確信を持って見積もれません
  でした。従来のプロファイルでホストを用意していた場合、今後は余裕を持って収まります。
  `--no-default-features` でビルドすればシステムのアロケータに戻せます。
- **poller のアップグレード時にダウンロードする量が半分になりました。** poller イメージはバイナリを
  2 回保持していました — COPY で配置したときに 1 回、`CAP_NET_RAW` の付与がそのファイルを書き換える
  のでもう 1 回 — しかもこの 2 つの層は毎リリース変わります。配置とケーパビリティ付与を 1 層に
  まとめたので、`yagra-poller` のリリース毎のダウンロード量が 10.2 MB から 4.7 MB になりました。
  大半は重複が消えた分で、残りはバイナリ自体が小さくなった分です。イメージの挙動は同一で、
  raw ソケット ICMP にも影響はありません。
  - ⚠️ **`yagra-core` は逆に大きくなりました。公平のため記しておきます**: リリースビルドが fat な
    リンク時最適化 (LTO) を使うようになり、core のバイナリは圧縮後 11.9 MiB から 13 MiB に増えて
    います。`codegen-units = 1` の上では LTO が畳める重複コードはもう残っておらず、そこに加わるのは
    クレート境界をまたぐインライン展開 — そしてインライン展開はコードを複製します。poller の
    バイナリは逆に小さくなっており、上の「バイナリ自体が小さくなった分」はそれです。この 2 つの
    変更を合わせれば両方のイメージを引くアップグレードで約 4.4 MB の削減になりますが、core 単体で
    見れば以前より少し増えています。
- **依存関係／根本原因のダッシュボードウィジェット**が、親→子のインデントされたツリーではなく、
  各根本原因とその配下にロールアップされたアラートを影響の大きい順に一覧するようになりました。
  依存関係グラフはもうツリーではなく（1 ノードが 2 つの上流を持ちうる）、ツリーでは片方しか
  表示できないためです。
- **インタフェースアドレスの収集と変化の記録**を、CDP/LLDP 隣接と同じ形で行うようになりました
  （ノードごとの現在値 1 件 + 変化時追記の履歴）。**設定 ▸ データ保持**に
  *インタフェースアドレスの変更* として現れます。収集は既定で有効・同じ 1 時間周期で、
  `PUT /api/v1/settings/neighbors` に専用のトグルがあります
  （`l3_enabled` / `l3_interval_secs`。省略した場合はその設定を変更しません）。
- **LLDP 隣接に対向機器の管理アドレスが入るようになりました。** これが隣接を監視対象ノードに
  照合するための情報であり、L2 からマップを組めるようになった理由です。⚠️ アップグレード後の初回
  ポーリングで、**LLDP を喋るノードごとに隣接変化の履歴が 1 行だけ余分に記録されます**（記録される
  集合に実際にフィールドが増えたためです）。LLDP-MIB を実装していない機器では何も追加されません。
- **LDAP アカウントが所有する API トークンも、所有者の沈黙で失効するようになりました**（SSO 所有の
  トークンは以前からこの扱いです）。ディレクトリ側でのアカウント無効化は Yagra に通知されないため、
  所有者が来なくなることが唯一の手がかりです。従来この規則は OIDC にしか書かれておらず、無効化された
  ディレクトリアカウントのトークンが無期限に使えてしまう状態でした。`YAGRA_PAT_OIDC_IDLE_DAYS` は
  名前をそのままに、両方を対象とします。

### バグ修正
- **MCP から開始した Troubleshoot 分析が監査ログに残るようになりました。** WebUI や REST API から
  始めた同じ実行は記録されていましたが、`/mcp` 経由で開始したものは 1 行も残っていませんでした
  （監査は REST のミドルウェアで、MCP 面はそこを通らないため）。`/mcp` を有効にしている環境では、
  誰が分析を開始したかの記録が漏れていたことになります。
- **無効化された SSO アカウントでのサインインが 500 ではなく 401 を返すようになりました。** 拒否自体は
  正しく、ステータスコードだけが「Yagra が壊れた」と伝えていました。
- **SSO / ディレクトリアカウントのパスワード再設定を、理由付きで拒否するようになりました。** 従来は
  200 を返して決して使われないハッシュを書き込み、管理者に「設定できた」と伝えていました。

## v0.1.21 — 通知テンプレートと、「何がフリートを黙らせたか」を MCP から見る

### 破壊的変更
- **`YAGRA_KEK_FILE` が読めないファイルを指している場合、core は起動しなくなりました。** 従来はエラーを
  ログに出したうえで新しいランダム鍵を生成して起動していました — つまりデプロイは健全に見えるのに、保存済みの
  監視資格情報がすべて永久に復号不能になっており、次のポーリングが失敗するまで誰も気づかない状態でした。この
  静かなデータ損失経路を、パスを名指しする明示的な起動失敗（`load KEK from <path>`）に変えました。**未設定の
  場合の挙動は不変**です: `YAGRA_KEK_FILE` を設定しなければ従来どおり開発用の一時鍵なので、開発用 compose は
  バイト単位で同一です。影響を受けるのは**既に壊れていた**デプロイだけです。実際の鍵ファイルを指すか、変数を
  外してください。
- **`YAGRA_FLOW_RETENTION_DAYS` は新規デプロイの初回起動時のみのシードになりました。** 既存デプロイでは
  設定 ▸ システム設定 ▸ データ保持期間 の値が正になります。この環境変数は既存の ClickHouse ボリュームに対して
  そもそも一切効いていなかった（下記「バグ修正」参照）ため、アップグレードによる変化はありません — 既存
  デプロイは実際に効いている 30 日をそのまま維持します。

### 新機能
- **MCP ツールを 3 本追加し、この面で最大のギャップを解消**（ADR-042 増分 2）。`/mcp` はメンテナンス
  ウィンドウを**作れるのに一覧できない**状態で、AI クライアントが自分でノードを黙らせておきながら
  「フリートは正常」と報告しうる非対称がありました。
  - **`list_suppressions`** — 呼び出し元が見られるメンテナンスウィンドウとミュートを 1 回の応答で
    返します。「静か」と結論づける前にこれを確認してください。黙らされたフリートは健全なフリートと
    同じ見た目になります。
  - **`alert_trends`** — アラートの時間的な傾向。`top_nodes`（どのノードが頻繁にアラートを出すか＝
    常習犯。現在発報中の一覧では出せません）、`transitions`（直近の発報と復旧）、`calendar`（曜日×時刻
    のバケット。夜間に偏るパターンの発見用）。
  - **`search_analysis_findings`** — Troubleshoot の検出結果を全実行にまたがって検索（ノード・
    フォルダ・診断種別・深刻度・期間で絞り込み）。既に知っている 1 回の実行を読むのとは別の問いです。
  - **`list_analyses` に `kind=schedules`** を追加し定期実行の一覧を、**`list_node_groups` に
    `include_state`** を追加し各フォルダの状態集計を名前と一緒に返せるようにしました。
  - MCP は引き続き **read-only** です。変更を行うツールは 1 本も追加していません。
- **通知テンプレート**（アラート ▸ 通知ルーティング ▸ チャネル ▸ 通知テンプレートを編集、ADR-039）。
  チャネルごとに件名と本文を上書きできるようになりました。アラートの決まった変数集合に対する Jinja2
  テンプレートとして書きます。直接の動機は、組み込みの件名がノードを **UUID** で名指していたこと —
  `node 6f1c9d2a-0b3e-4a71-9c8d-2e5f7a1b4c60 is critical` — で、これを
  `{{ severity | upper }}: {{ node_name }} ({{ group }})` と書けるようになります。条件分岐も使えます
  （`{% if event == 'resolve' %}復旧{% endif %}`）— これが必ず出てくる次の要望だからです。
  - **既存チャネルの挙動はバイト単位で不変です。** テンプレート未設定は従来どおりの文面で、
    アップグレードによって変わる通知はひとつもありません。
  - **テンプレートが壊れても通知は失われません。** アラート発生時に描画できなかった場合 — フィルタの
    誤り、出力が上限超過、JSON を送るチャネルで本文が JSON でなくなった、など — その項目だけが組み込みの
    文面に戻り、アラート自体は送られます。フォールバックは項目単位なので、本文の誤りが正しく書けた件名を
    巻き添えにすることはありません。発生のたびに
    `yagra_notification_template_errors_total{reason}` を計上しログに残します。
  - **保存前にプレビューできます。** テンプレートが本番で初めて実行されるのは障害の最中なので、
    代表的なアラートに対して描画し、実際に送られる内容（JSON として妥当かどうかを含む）を表示します。
    コンパイルできないテンプレートは保存時点で拒否します — 深夜ではなく。
  - 使える変数: ノード名/ID/アドレス、グループ、プロファイル、重要度、状態、メトリクス、値、閾値、
    方向、発生時刻、フラッピング、ロールアップ時の根本原因、PagerDuty/JSM に送るのと同じ dedup キー。
    資格情報は一切含まれません。JSON 本文への埋め込みには `tojson` フィルタが必要で、エディタの
    エラーメッセージがそれを案内します。
  - 編集は Admin のみ・監査対象で、再起動なしに約 30 秒で反映されます。
  - **対象外**: `YAGRA_WEBHOOK_URL` / `YAGRA_SMTP_*` で設定した宛先は組み込みの文面のままです —
    テンプレートを紐づけるチャネルのレコードが存在しないためです。UI でチャネルとして追加すれば
    テンプレートを使えます。
- **MCP ツール面が WebUI と同じ問いに答えられるようになりました**（ADR-042）。読み取り 110
  エンドポイントに対してツールは 17 本しかなく、しかも欠けていたのはトラブルシュートで最初に当たる
  ものばかりでした。読み取り専用ツールを 6 本追加し、計 23 本になります:
  - **`get_interface_series`** — インターフェース単位の送受信スループットとエラー率の推移。
    `query_metrics` はノード単位専用のため、これまでツールが存在しませんでした。
  - **`top_metrics`** / **`top_interfaces`** — フリート横断のランキング。「今 CPU が一番高いノード」
    「一番混んでいる／エラーの多い／変動の大きいリンク」。従来は 1 ノードずつしか読めませんでした。
  - **`get_neighbors`** — ノードの CDP/LLDP 隣接と、その変化履歴（ADR-038）。
  - **`list_node_groups`** — フォルダツリー。`run_analysis(scope="group")` に渡すグループ ID を
    見つける手段でもあります。
  - **`fleet_throughput`** — 全エクスポータ合計の送受信 bps。
  - **`top_flows` の `node_id` が任意に**。省略するとフリート横断集計になります（従来ツール無し）。
    REST と同様、グループ限定トークンに対しては拒否されます — 行にエクスポータの帰属が残らないためです。
  - **MCP は read-only を堅持します。** 書き込みはアラート ack・メンテナンス窓の開始・即時ポーリングの
    3 種のままで、今回は 1 つも増えていません。Yagra 自身の設定の**読み取り**も UI と同じ権限を要求します
    — read-only は「誰でも読める」という意味ではありません。
- **CDP/LLDP による隣接情報の収集**（ノード詳細 ▸ 隣接、ADR-038）。SNMP ノードの LLDP / CDP テーブルを
  低頻度で走査し、結果を「**いつ・何が変わったか**」として記録します。タブには現在どのポートがどの機器に
  向いているかと、それが動いた時刻の一覧が出ます。**履歴は隣接が実際に変わったときだけ書き込まれます** —
  エージェント側の揺らぎ（LLDP の `TimeMark`、リモート行の番号振り直し、行順）は正規化して捨てるため、
  配線を触っていないラックは 1 行も書きません。2 つのプロトコルは 1 つのモデルに正規化されるので、両方を
  喋る機器でも表と機能ラベルは 1 種類です。
  - **既定で有効です。** アップグレード後、各 SNMP ノードに 1 時間あたり 1 回の走査が増えます。どちらの
    プロトコルも話さない機器は 1 往復で終わるため実質ゼロ、48 ポートのスイッチで数十行です。
    **設定 ▸ システム設定 ▸ 隣接情報の収集** で無効化・周期変更ができます。無効にしても記録済みの
    データは保持されます。
  - Yagra の他の機能と同じく読み取り専用で、機器の設定は一切書き換えません。隣接データは**アラートを
    上げず**、依存抑制にも**意図的に接続していません**: LLDP が教えるのは向きの無い「隣接」で、上流を
    取り違えると本物の障害を黙らせてしまうためです（ADR-015）。
  - 新しい経路: **`GET /api/v1/nodes/{id}/neighbors`** と
    **`GET /api/v1/nodes/{id}/neighbors/history`**（View・ノードスコープ）、
    **`GET`/`PUT /api/v1/settings/neighbors`**（View / ManageConfig）。
- **データ保持期間を UI から設定可能に**（設定 ▸ システム設定 ▸ データ保持期間、ADR-040）。アラート連動データ・
  未マッチイベント・レポート実行・トラフィックフローがそれぞれ独立した期間を持ち、変更は再起動なしに次回の掃引
  から適用されます（フローは ClickHouse へ即時反映）。カードには Yagra が変更**できない**行も含めてすべての
  保持対象を表示します: VictoriaMetrics と VictoriaLogs は保持期間をコンテナの起動フラグとして受け取り実行時
  API を持たないため、これらは読み取り専用で、**ストア自身の `/flags` から読み戻した値**（設定ファイルから
  写した数値ではなく、実際に効いている値）を表示します。監査ログは設計どおり「無期限に保持」と表示されます。
- **`GET /api/v1/settings/retention`**（View）と **`PUT`**（ManageConfig）で同じポリシーを取得・更新できます。
- **`GET /api/v1/credentials/health`**（ManageCredentials）は、保存済みの資格情報が読み込み済みの鍵で実際に
  復号できるかを報告します。これはデータベースの復元だけでは立てられない主張です — 行が完全に戻っていても、
  鍵が別物ということがあり得るためです。
- **設定バンドル**（設定 ▸ 設定バンドル、`GET`/`POST /api/v1/config/bundle`、ADR-040）。デプロイの監視設定 —
  プロファイル・メトリクスセット・分類ルール・グループ・ノード・閾値・URL/DNS 監視・転送先・イベントソースと
  ルール・レポート/分析スケジュール — を 1 つの JSON ファイルとして書き出し、別のデプロイに適用できます。用途は
  **移行**であってバックアップではありません: バンドルには資格情報・通知チャネル設定・受信トークン・履歴の
  いずれも含まれません。取り込みは **upsert のみ**（削除は一切行わず、置換モードもありません）で 1 トランザクション
  で実行され、`?dry_run=true` は実際の取り込みを行ってロールバックするため、レポートは適用した場合とまったく
  同じ内容です。レポートはスキップ・変更した行とその理由をすべて報告します: 必須の参照が無い行は参照を外して
  広げるのではなくスキップし、秘密を必要とする転送先や Webhook ソースは無効状態で作成され、スケジュールの次回
  実行時刻は移行先の時計で再計算されます。通知チャネルとルーティングルールは**意図的に含めていません** —
  チャネルの実体は封印された設定そのもので、既存チャネルの id に設定を付ける API が無いため、取り込んだルールは
  黙って誰にも通知しない状態になるからです。いずれかのテーブルが 10,000 行を超えると書き出しは切り詰めずに
  拒否します。その規模のデプロイにはデータベースダンプを使ってください。
- **スクリプトとして出荷されるバックアップ手順と、それが機能することを証明する手段。**
  `scripts/yagra-backup.sh` はティア1 一式（KEK を最初に、次に完全な `pg_dump`、そして VictoriaMetrics の
  スナップショット）を manifest 付きで取得します。`scripts/yagra-restore-verify.sh` はそれを使い捨てスタックへ
  復元し、`/readyz`・ノード数・監査ログ行数・**資格情報が実際に復号できること**を assert します。ADR-017 は
  破壊的マイグレーションに対してバックアップとロールバック手順を要求してきましたが、これまでリポジトリには
  `pg_dump` が 1 箇所もありませんでした。

### バグ修正
- **MCP の `top_flows` / `flow_fanout` ツールが行数上限を適用していませんでした。** REST のフロー
  エンドポイントは 1 リクエストを 1000 行に制限しますが、MCP ツールはクエリビルダを独自に複製しており、
  その過程で上限が失われていました — AI クライアントが 10 万行を要求すると、無制限の ClickHouse
  クエリがそのまま実行されます。**両サーフェスが 1 つのウィンドウ／上限ルールを共有する**ようにしたので、
  どちらから来たクエリにも上限が効きます。1000 行を超える要求は 1000 行になります。既定値（100 行）は
  変更ありません。
- **Geo マップの拠点ピンが、そのフォルダに直接入っているノードしか数えていませんでした。** ノードは通常
  ラック・フロア・盤といったサブフォルダに入るため、東京拠点に座標を打ってスイッチを *東京 ▸ 2F ▸ ラックA*
  に置いた運用者には、**常に緑で中身が空のピン**しか見えませんでした — 拠点が燃えていても何も映りません。
  **グループ座標を継承するようにしました**: 自分の座標を持たないフォルダは最も近い「座標を持つ祖先」に属し、
  ピンはそこに解決される全てを数えます。Geo マップページとダッシュボードウィジェットを同時に変更したので、
  同じ拠点が一方で amber・他方で green になることはありません。
  - **継承でピンは増えません。** 1 棟の下の 30 ラックはピン 1 本のままです — 完全に重なった 30 本は建物を
    隠すだけだからです。ピンの数は今までどおり「自分の座標を持つフォルダの数」で、変わったのは各ピンが
    何を数えるかです。
  - グループのダイアログに、親を通じて既にマップ上にあることを表示するようにしました。座標欄が空なだけで
    「この拠点はマップに載っていない」と読めてしまう状態を解消します。
  - API: `GET /api/v1/node-groups` の各行に `effective_latitude` / `effective_longitude` /
    `geo_source`（`own` / `inherited` / `unset`）/ `geo_group`（属するピンのフォルダ）が加わりました。
    `latitude` / `longitude` は従来どおり**そのフォルダ自身の座標**で、意味は変わりません。
- **`YAGRA_FLOW_RETENTION_DAYS` は既存デプロイでは何も していませんでした。** ClickHouse の TTL は
  `CREATE TABLE IF NOT EXISTS` の中でしか出力されておらず、テーブルが既に存在すると no-op になるため、既存
  ボリュームの保持期間は作成時の値のままでした — その一方で `DEPLOYMENT.md` はこの変数が効くかのように記載
  していました。保持期間の変更は `ALTER TABLE … MODIFY TTL` で適用されるようになり、宣言済みの TTL と実際に
  異なる場合にのみ発行されます（無条件に発行すると再起動のたびに全パートを再ミューテートしてしまいます）。
  **期間を短くするとそれより古いフロー行は削除される**ため、変更は新旧の値とともに `warn` でログに残ります。
- **5 つの封筒暗号ストアがそれぞれ独自に鍵を読み込んでいました。** 資格情報・通知チャネル・転送先・OIDC・
  LLM 設定が鍵ローダーを個別に呼んでいたため、（未設定時の）一時鍵経路では**それぞれ別の**ランダム鍵になって
  いました — コード自身のコメントが「1 つの鍵を共有する」と述べていたにもかかわらずです。1 つの鍵を共有する
  ようになりました。

## v0.1.20 — グループスコープによる可視制御、端から端まで

グループスコープが「型はあるが誰も参照しない」状態から、実際に機能する制御になりました。読み取り
経路が絞り込み、管理者が発行でき、API トークンと `/mcp` の双方が尊重します。あわせて Troubleshoot
に実行横断の findings 検索とスケジュール実行が加わり、Geo マップが実ページになりました。

### 破壊的変更
- **API トークンが所有アカウントとして動作するようになり、所有者を特定できないトークンは失効します。**
  これまでのトークンは独立した身分でした。`api_tokens` は自身のロールを持つだけで `users` への
  リンクが一切なく、発行したアカウントを削除・無効化・降格してもトークンには何の影響もありません
  でした。マイグレーション `0057` が `created_by` のユーザー名を手がかりに各トークンを所有者へ
  紐付けます。**発行元アカウントが既に存在しないトークンは紐付けできず、認証されなくなります**。
  一覧には「所有者なし」として表示されるので、意図して失効させてください。該当するトークンは、
  できればサービスアカウント(後述)を所有者にして再発行してください。
- **Top-N 系 4 エンドポイントの応答が、配列そのものからオブジェクトに変わりました。**
  `GET /api/v1/metrics/top`、`/metrics/interface-top`、`/metrics/interface-delta`、
  `/alerts/top-nodes` は `{"entries": [...], "partial": false}` を返します。従来ボディ全体だった
  ものが `entries` に入ります。`/metrics/interface-heatmap` は形を保ったまま同じ `partial`
  フィールドが増えます。`partial` は、そのランキングが呼び出し元アカウントに見えるグループだけを
  対象としており、本来見えるはずの行が欠けている可能性があるときに `true` になります — Top-N は
  グループを知らないメトリクスストアが順位付けするため、スコープ付きアカウントでは後段で絞り込む
  ことになり、結果が短くなりうるからです。可視範囲に制限のないアカウント（現時点では全アカウント）
  では常に `false` です。WebUI は追随済みですが、これらを読む外部クライアントは `entries` を
  読むよう変更が必要です。
- **`:latest` の意味が「開発トランク」から「最新の安定リリース」に変わりました。** これまでは
  `main` への push のたびに `ghcr.io/horryworks/yagra-*:<sha>` が公開されて `:latest` が動いていた
  ため、`docker-compose.deploy.yml` と `docker-compose.poller.yml` の既定値は開発ビルドを引いて
  いました。開発ビルドはもう一切公開されません — レジストリにあるのはリリースだけです。
  `:latest` は `-beta`/`-rc` の付かないリリースがタグ付けされたときに動き、`:<sha>` はリリース
  されたコミットにしか存在しません。`:latest` で `main` を追っていた場合、これからはリリースを
  追うことになります。別のものが必要なら明示的にタグを固定してください。なお `:latest` は
  「最も新しいバージョン」ではなく「最後に push された安定タグ」に付くので、大きなリリースの
  後に hotfix を切ると `:latest` は後退します。
- **`docker-compose.deploy.yml` に `YAGRA_IMAGE_REPO` が増えました**（既定 `ghcr.io/horryworks`）。
  公開リリースを使う限り設定不要です。未リリースのビルドを置いたプライベートレジストリを開発機
  から指すために存在します。

### 新機能
- **グループスコープ付きの API トークンと `/mcp` 接続が使えるようになりました。** 強制の代わりに
  置かれていた 2 つの拒否を解除し、v0.1.19 で書いた「読み取り経路が実際に絞り込むようになったら
  グループスコープを再び受け付ける」という約束を果たしました。`POST /api/v1/api-tokens` は
  `{"scope": {"Groups": [...]}}` を `400 unsupported_scope` で拒否せず受理し、`/mcp` は
  グループスコープ付きトークンを `403` にせず受け入れます。MCP の各ツールは呼び出し元のスコープを
  解決し、対応する REST エンドポイントと同じ規則を適用します — ノード一覧とイベント検索はクエリで
  絞り込み、ランキングや履歴は取得後に絞り込み、範囲外のノードを指定したツールは「不明な ID」と
  同一の応答を返し、`run_analysis` は保持していないスコープでは起動できません。Settings ▸ API
  トークンにスコープ選択と**可視範囲**列を追加しました。
  封じ込めのルールが 2 つ: トークンは所有者を超えられないため、**グループスコープ付きアカウントが
  所有するトークンはそのアカウントのスコープを継承**します(アカウントを絞ればトークンも即座に
  狭まり、再発行は不要)。そのようなトークンに別のスコープを与えようとすると
  `400 owner_is_scoped` で拒否されます。またトークンのスコープは実在するグループを指す必要が
  あります。トークンだけを所有者より狭くしたい場合は、その範囲に限定したサービスアカウントに
  所有させてください。
- **アカウントをノードグループ単位に制限できるようになりました。** Settings ▸ ユーザー ▸
  *可視範囲を変更*(`PUT /api/v1/users/{id}/scope`)で、選んだグループとその配下だけを見えるように
  絞り込めます。`"All"` でフリート全体に戻ります。読み取り系の絞り込み自体は本リリースの先行分で
  実装済みで、今回追加されたのは**その可視範囲を発行する手段**です。これまでスコープは、設定する
  方法が無いために何も持てない値でした。どのグループにも属さないノードは引き続き無制限アカウント
  にしか見えず、範囲外のノードは `404` を返します(不明な ID と同じ応答なので、存在確認の手段には
  なりません)。保存するとそのアカウントの現行セッションはサインアウトされます — ロール変更と同じ
  理由で、スコープはセッショントークンに焼き込まれているため、生きたトークンは古い広い範囲を保持
  し続けてしまうからです。
  ルールが 2 つ: **Admin は絞り込めません**(`409 admin_is_unscoped`)。管理操作はフリート全体に
  及ぶためで、Admin へ昇格させると保持していたスコープは解除されます。また**グループを 1 つも
  指定しないスコープは拒否されます**(`400 empty_scope`) — 保存できてしまうと、サインインには
  成功するのに在庫が空で理由がどこにも出ない、というアカウントになるためです。
  `GET /api/v1/users` と `GET /api/v1/auth/me` は `scope` を返すようになり、サインイン中の
  アカウントが制限されている場合はアカウントメニューにその旨を表示します。SSO アカウントは無制限で
  プロビジョニングされ、この画面で絞り込みます。設定した内容は以後のログインでも保持されます
  (IdP のグループからスコープを導出する仕組みは後続の増分です)。
- **分析のスケジュール実行。** Troubleshoot ▸ スケジュールから、分析を毎日／毎週／毎月の指定時刻
  (UTC)に自動実行できます。対象はフリート全体・サイト・単一ノードのいずれか。これまで分析は手動
  起動のみで、夜間の異常検知は誰かが覚えている必要がありました。エンドポイントは
  `GET/POST /api/v1/analysis/schedules` と `PUT/DELETE /api/v1/analysis/schedules/{id}`、権限は
  実行と同じ Operator 以上です。
  挙動として 2 点: 実行制御(admission control)に拒否された発火は**スキップではなく延期**され、
  スケジュールは due のまま次の tick で再試行します(混雑した一瞬のために 1 周期まるごと失われない
  ため)。また**フローストアの無いデプロイではトラフィックフロー系の分析をスケジュールできません**
  — 毎回空の実行が積み上がるだけになるためです。スケジュールの通知は既定でオフです(手動実行は
  結果を待っている人がいるため既定でオン)。
- **保存された検出結果 — 全実行を横断して分析結果を検索できます。** Troubleshoot ▸ 保存された
  検出結果(`GET /api/v1/analysis/findings`)が、すべての分析実行の検出結果を新しい順に一覧し、
  ノード/サイト・分析種別・重大度・期間で絞り込めます。これまで検出結果はそれを生成した実行を
  経由してしか辿れず、「このスイッチについて最近何か検出されているか」を知るには実行を 1 件ずつ
  開くしかありませんでした。各行はその実行のレポートへリンクします。エンドポイントはキーセット
  ページングで、最終行の `at` と `id` を `before` / `before_id` として渡すと次ページを取得します。
- **REST API が API トークンを受け付けるようになりました。** これまで `yat_…` トークンは `/mcp`
  専用で、REST API は `401` を返していたため、無人の自動化はパスワードを保持して毎回ログインする
  しかありませんでした。トークンは自身が使用できる**面(surface)**を持つようになり、`rest` を
  含むトークンはセッショントークンと同じように `/api/v1` で使えます
  (`Authorization: Bearer yat_…`)。既存のトークンは `mcp` のみを持つため、アップグレードによって
  AI クライアント向けに発行した資格情報が監視設定を変更できるものに変わることはありません。REST
  に届かせるのは、発行時の明示的な選択です。
  使用面によらず 2 つの制限があります。**ユーザー管理はできません**(自身の後継を発行できる資格情報
  は、元の失効を生き延びてしまうため)。また、サインイン中のアカウントを特定するエンドポイント
  (`GET /api/v1/auth/me`、個人ダッシュボード)は `403` を返します。
- **API トークンに所有者と有効期限が付きました。** 実効ロールは
  `min(トークンのロール, 所有者の現在のロール)` になるため、アカウントを降格するとそのトークンも
  同時に狭まります。アカウントを無効化・削除するとトークンは失効します。有効期限は任意です:
  `POST /api/v1/api-tokens` は `expires_at` を受け取り、省略すれば従来どおり無期限です(連携を
  動かすサービスアカウントには妥当な選択です)。Settings ▸ API tokens に所有者・使用面・有効期限、
  そして認証されない場合はその理由が表示されます。
- **サービスアカウント。** `POST /api/v1/users` が `kind: "service"` を受け取ります。パスワードを
  持たず、ローカルフォームでも SSO でもサインインできない機械アカウントです。API トークンを所有
  するためのもので、設定した担当者が異動しても連携が動き続け、無効化すればそのアカウントが所有する
  資格情報がまとめて停止します。同リクエストの `password` は任意になり、サービスアカウントに対して
  は**無視ではなく拒否**します。最後の管理者を守るロックアウト防止は、人間がサインインできる
  アカウントだけを数えるようになったため、サービスアカウントが唯一の管理者になることはありません。
- **`YAGRA_PAT_OIDC_IDLE_DAYS`**(既定 `30`)は、SSO でプロビジョニングされたアカウントが所有する
  API トークンが、所有者の沈黙をどれだけ生き延びるかを決めます。IdP 側でアカウントが無効化されても
  Yagra には通知されず(アカウント表は SSO ログイン**成功時**にしか更新されません)、所有者が
  サインインしなくなることが唯一の手がかりだからです。ローカル/サービスアカウントは対象外です。
- **URL 監視が資格情報を提示できるようになりました。** 新しい `http_auth` 資格情報の種別で
  Basic / Bearer / カスタムヘッダーに対応します。URL 監視に紐付けると、ポーラーがそれを提示して
  プローブします。資格情報は保存時にエンベロープ暗号化され、配信時にポールジョブへ埋め込まれます
  — SNMP 資格情報と同じ経路で、ポーラーは資格情報ストアを読みません。これまで作成できるのに
  消費する側が無かった `api_token` 種別は、Bearer トークンとして受理するようにしました。
  資格情報を提示する監視では TLS 検証が必須です（違反時は `400 credential_needs_tls`）。
- **ポーラーが停止時にノードを引き継がせます。** SIGTERM を受けたポーラーは `leaving` を立てた
  最後のハートビートを送るようになり、core は即座にそのポーラーをプールのハッシュリングから外して
  ノードを再割り当てします。従来は停止とネットワーク分断を区別できず、3 拍欠落（30 秒）を待って
  いました — しかも再起動がその窓に収まるとリングは一切変わらず、対象ノードは再起動の間ずっと
  ポーリングされませんでした。ローリングアップグレードが操作者の手を借りずに数秒で引き継ぎます。
- **監視ギャップに、失われた受動データが記録されます。** ギャップの行に、そのポーラーがバインド
  していた受動リスナー（`syslog:514`、`trap:162` など）を記録します。ポーリングしたメトリクスは
  再接続時にポーラーのバッファから埋め戻されますが、syslog・トラップ・フローエクスポートは
  埋め戻せません。イベントログの説明のつかない空白と、既知の欠損とを区別できるようになります。
  （SNMP インフォームは例外で、送信側が ACK を受け取るまで再送します。）
- **トップバーのグローバル検索が動くようになりました。** シェルの構築以来ずっと配置されていた
  ものの永久に無効で、コードには「検索エンドポイントがまだ無い」というコメントが残っていました。
  実際には `GET /api/v1/nodes/search` はノードピッカーの実装時から存在していました。ノードを
  デバウンス付きで検索し、矢印キーでの移動、`Ctrl`/`Cmd`+K と `/` でのフォーカス、Enter で
  ノードを開けます。検索対象がノードのみであることはポップオーバーに明記しています —
  アラート・イベント・グループにはサーバ側検索が無く、ノードだけの結果を全体検索のように
  見せるのは何も無いより悪いためです。モバイルではトップバー下に開く行として提供します。
- **Topology ▸ ジオマップが実際のページになりました。** これまで「Coming soon」のプレースホルダー
  でしたが、座標を持つノードグループごとにピンを打ち、そのグループの配下ノードの最悪状態で色分け
  して世界地図の上に表示します。ホイール/ドラッグ/ピンチでのパン・ズームと、ピンからそのグループ
  のノード一覧への遷移に対応します。ダッシュボードのジオマップウィジェットと同じグループ別
  ヘルス集計を読むので、同じ拠点が 2 つの画面で違う色になることはありません。地図の輪郭はアプリに
  同梱してあり、タイルサーバも外部通信も新規依存もありません — 監視コンソールは「ネットワークが
  壊れたときに開くもの」であり、インターネットを必要とする地図はまさに必要なときに真っ白だから
  です。
- **グループの座標を再び設定できます。** `PUT /api/v1/node-groups/{id}/geo` は v0.1.19 で
  「呼ばれていないエンドポイント」として削除されましたが、ダッシュボードの Geo マップ
  ウィジェットは座標を読み続けていたため、データを入れる手段が無いウィジェットが残っていました。
  エンドポイントを復活させ、グループのダイアログに緯度・経度の入力を追加しました。
- **Troubleshoot の「通知する」が実際に通知します。** 選択肢は提示され保存もされていましたが、
  消費する側がありませんでした。どの画面にいても完了通知が出て、レポートへのリンクが付きます。
  このブラウザで Yagra を開いている間だけ動作する点は、コントロール自身に明記しました。
- **アクティブアラートの行からミュートできます。** アラート ▸ アクティブ の「ミュート」が
  動作するようになりました。ノードを固定し、発火したメトリクスを事前入力した状態でミュート
  ダイアログを開くので、ノード全体ではなくトリアージ中のチェックだけを正確に対象にします。
  `POST /api/v1/mutes` が既に受け付けているロール（オペレータと管理者）にのみ表示され、
  ビューアには提示して拒否するのではなく最初から出しません。ミュートが止めるのは**通知**だけ
  で、アラート自体は一覧にも履歴にもそのまま残ります。
- **永久に無効だった「外部で開く」を削除しました。** 実装時から無効のまま出荷され、
  PagerDuty / JSM への深いリンクを約束していましたが、どう設定しても供給できないものでした
  — Yagra がこれらの連携先として保持しているのは送信先エンドポイントであって、インシデント
  ごとの URL ではないためです。ACK 済みピルが既に確認元のツール名と担当者を示しており、
  それが同じ情報の正直な形です。API の変更はありません。

### バグ修正
- **`rest` サーフェスを持つ API トークンが、ほとんどの読み取りエンドポイントで拒否されていました。**
  グループスコープで絞り込むエンドポイント（ノード一覧・フリートサマリ・アラート・イベント・
  メトリクスのランキング・トポロジ）が呼び出し元をセッションストアからしか解決しておらず、同一
  リクエストで権限ガードは通っている有効な `yat_…` トークンが `401 unauthorized` を返していました。
  本リリース内のグループスコープ強制と同時に混入したため、タグ付きバージョンには出荷されていません。
  両ガードとも「1 リクエストにつき 1 回解決された資格情報」を読むようになり、トークンでスコープ付き
  読み取りに到達できることをテストで固定しました。
- **URL 監視を編集すると資格情報の紐付けが消えていました。** フォーム自身のコメントには「これは
  replace なので全フィールドを明示送信する」と書かれていながら、資格情報だけ送っていませんでした。
  紐付けを消費する側が無い間は見えませんでしたが、上記の機能が入ると無関係な編集だけで監視が
  ログアウトするところでした。
- **ノードインベントリのフィルタが黙って最大 100 件で切れていました。** API は上限を 500 に
  クランプしてそれを最大値として文書化していた一方、クエリ側が 100 に再クランプしていたため、
  数千件に一致するフィルタでも 100 件しか表示されず、切り詰めた旨の表示も出ませんでした。
  上限を 1 つの定数 (500) に統一してエッジとクエリの双方がそれを使うようにし、フィルタ結果が
  上限に達した場合はツリーに通知を表示します。
- **アップグレード後も、ブラウザがアップグレード前の画面を使い続けることがありました。** nginx が
  SPA に対して `Cache-Control` を一切返しておらず、ブラウザはヒューリスティックな鮮度判定
  （RFC 9111 §4.2.2）にフォールバックして、キャッシュ済みの `index.html` を数時間再利用しうる
  状態でした。イメージはドキュメントルートを丸ごと置き換えるため、その古いページが指すハッシュ
  付きアセットは新しいイメージには存在せず、しかも SPA のフォールバックがその要求に
  `index.html` を返していたため、ブラウザは `text/html` として配信されたスクリプトを拒否し、
  理由の手掛かりが無いまま何も表示しませんでした。`index.html` を `no-cache`（読み込みごとに
  再検証。変更が無ければ 304 のまま）に、`/assets/` 以下のハッシュ付きアセットを 1 年間
  `immutable` に、存在しないアセットは HTML ではなく 404 を返すようにしました。

## v0.1.19

**「なぜ」を尋ね、「どのポーラーが何を見ているか」を知る。** 今回の目玉は 2 つです。**AI による
根本原因分析**は発生中のアラートから実行できます — Yagra はもともとインシデントを組み立てています
（カスケードの根本原因、メトリックの異常、受動イベント、その周辺の支配的なトラフィック）。そこに
足りなかった「最後に人間が書く一文」を言語モデルに尋ねます。プロバイダを設定するまでは無効で、
アラート経路はこの機能に一切依存しません。もう 1 つは **ノード→ポーラーの可視化**です。各ノードが
所属プールと実際に自分をポーリングしているポーラーを表示し、プールはインベントリツリーから割り当て
られます — フォルダ単位も可能で、これがツリー唯一の一括割り当て手段です。土台としては、ノースバウンド
API が**自身の OpenAPI 3.1 文書をハンドラから生成して公開**するようになり、WebUI の型もそこから生成
されます。手書きの TypeScript 3,340 行が消え、契約のドリフトという問題群が構造的に閉じました。

### 破壊的変更
- **未認証リクエストは API 全体で 503 ではなく 401 を返すようになりました。** Yagra はエンドポイントが
  必要とするサブシステムが未構成のとき 503 を返します（スケルトンモード）。約 100 のハンドラがこの
  可用性チェックを権限ガードより**先**に実行していたため、認証情報を一切持たない相手が「構成済みか
  未構成か」を判別できていました。ガード順序を API 全体で統一し、認証・認可が先、可用性チェックが後に
  なります。認証済みの呼び出しに変化はなく、未構成サブシステムへの未認証リクエストだけが 503 から
  401 に変わります。
- **`VITE_API_BASE` の意味がベースパスからオリジンに変わりました。** 従来は既定値 `/api/v1` で相対パスの
  前に付けていましたが、既定値が空になり `/api/v1` は各パスの一部になりました。影響を受けるのはこの変数を
  上書きして WebUI をビルドしている場合だけで、対応は末尾の `/api/v1` を外すことです
  （`VITE_API_BASE=https://core.example.net`、`…/api/v1` ではなく）。併せて、ライブ更新のストリームが
  自分では `/api/v1` を付けておらず、この変数を設定すると API クライアントと別のホストを指していた不整合も
  解消しました。
- **`GET /api/v1/thresholds` が封筒形式になり、件数上限が付きました。** レスポンスが
  `StoredThreshold[]` から `{ "items": [...], "total": <n>, "truncated": <bool> }` に変わり、
  サーバは 1 リクエストあたり最大 **500** 件のルールを返します（`?limit=` で減らすことはできますが
  増やすことはできません）。WebUI は同じイメージに同梱されており併せて更新済みなので、影響を受けるのは
  **外部の自動化のみ**です。また、このエンドポイントへの未認証リクエストは 503 ではなく **401** を
  返すようになりました（API 全体の方針に合わせ、サブシステムが構成済みかどうかを開示する前に認証を
  行います）。ルールの読み取りには View ではなく **ManageConfig** が必要です — 閾値の集合は「いつ誰を
  呼び出すか」の設定だからです — のでパブリックダッシュボードでも閉じたままです。
- **`POST /api/v1/api-tokens` がグループスコープを拒否するようになりました。**
  `{"scope": {"Groups": [...]}}` を送るとトークンを発行せず **400 `unsupported_scope`** を返します
  （`scope` を省略するか `"All"` を送ってください）。グループスコープはどちらの経路でも強制されて
  いませんでした — `/mcp` はグループスコープ付きトークンをそもそも拒否し、REST のエンドポイントは
  スコープを一度も参照していません — ため、発行されていたのは「最小権限に見えて、実際には使えないか
  無制限かのどちらかである」認証情報でした。WebUI はこの項目を提供していないので、影響は API
  クライアントのみです。読み取り経路が実際にスコープで絞り込むようになった時点で再び受け付けます。

- **レポートとグループの 4 エンドポイントが、API の他の部分と同じステータスコードを返すようになりました。**
  レポートの削除 3 本（`DELETE /api/v1/reports/definitions/{id}` / `/runs/{id}` / `/schedules/{id}`）は
  `200 {"ok": true}` を返していました（他の 24 本の削除はすべて `204 No Content`）。今後は
  **204**（ボディなし）です。`POST /api/v1/node-groups` は生成した ID を捨てて 204 を返す唯一の
  作成系エンドポイントでしたが、他の 20 本と同じく **201** と `{"id": "<uuid>"}` を返します。
  `POST /api/v1/reports/schedules` は `{"id": …}` を 200 で返していましたが **201** になります。
  ステータスコードを厳密に判定するクライアントや、削除応答から `ok` を読むクライアントは修正が
  必要です。WebUI は同じイメージに同梱されており、どちらも読んでいませんでした。
- **`GET /api/v1/rca/{id}` と `POST /api/v1/rca` が、レポートの中身を記述するようになりました。**
  `body` フィールドは型なしの JSON として公開されていたため、生成クライアントでは AI の回答と
  その根拠がまるごと `unknown` になっていました。回答（`summary` / `root_cause` / `dependents` /
  `next_steps` / `confidence` / `raw`）と、その根拠となったインシデント文脈のスキーマを文書に
  追加しました。**ワイヤ上のバイト列は変わりません** — 記述が欠けていただけです。型を取り込むには
  クライアントを再生成してください。

- **Troubleshoot 解析の実行が、両経路とも Operator 権限に揃いました。** これまで REST API では
  **Admin**、MCP では **Viewer** で通っており、オンコールの運用者が WebUI では弾かれるのに、
  同じ人が AI クライアント経由なら同じ解析を実行できる状態でした。どちらも「アラート応答」権限
  （Operator 以上）を要求するようになり、実行中ジョブのキャンセルも同じです。解析は設定を何も
  変えないので、従来の Admin 要求は実質レート制限の代用であり、その役割は既に admission 制御
  （同時実行数・分間レート）が担っています。過去の実行と所見の**閲覧は従来どおり Viewer に開放**
  されています。**Viewer 権限の API トークンでは `/mcp` から解析を開始できなくなります。**

- **どこからも呼ばれていなかった 4 つのエンドポイントを削除しました。** `GET /api/v1/rca/{id}`、
  `GET /api/v1/reports/definitions/{id}`、`PUT /api/v1/node-groups/{id}/geo`、
  `POST /api/v1/events/alerts/close` は到達可能でしたが、WebUI からも MCP ツール面からも、文書化
  された自動化からも呼ばれておらず、入口のない機能のためにリクエストを受け、公開契約に載り、
  テストを抱えていました。今後は `404` を返します。それぞれが返していたデータは今も取得できます
  — RCA レポートは生成元の `POST /api/v1/rca` が返し、レポート定義は
  `GET /api/v1/reports/definitions` が返します。**うち 2 つは整理ではなく機能の喪失です:**
  グループの地図座標はもう設定する手段がありません（Sites マップウィジェットは読むだけで、書き手
  がいなくなります）。またイベント由来アラートの手動クローズもできなくなり、従来どおりルールの
  TTL で解消されます。どちらかが必要であれば、UI を伴う形で復活させられますのでお知らせください。

### 新機能
- **AI による根本原因分析を、必要なときに実行できます。** Yagra はもともとインシデントの根拠を
  組み立てています — 依存抑制がカスケードを根本原因に帰属させ、インシデントタイムラインがメトリックの
  異常・受動イベント・その周辺の支配的なトラフィックを集めます。どれだけ相関を取っても出てこないのが
  「最後に人間が書く一文」です。**発生中のアラート**にボタンが追加され、まさにその根拠に基づいて言語
  モデルにその一文を尋ね、要約・推定される根本原因・それで説明が付く配下ノード・次に取るべき手順を
  返します（確信度と、モデルの生の回答も併せて保持します）。
  **設定するまでは無効です。** プロバイダ設定が無ければクライアントも認証情報も外向き通信も存在
  しません。アラート経路からこの機能を呼ぶ箇所は一切無いため、プロバイダが応答しても、タイムアウト
  しても、そもそも未設定でも、ヒステリシス・抑制・重複排除・通知の挙動は完全に同一です。プロバイダは
  **Vertex AI** / **Gemini** / **Claude** から 1 つを選び、その認証情報は他のシークレットと同じ封筒
  暗号で封印され、保存後は書き込み専用になります。レポートの生成には **Operator** が必要で（説明を
  必要とするのはポケベルを持っている人だからです）、狭い権限ではなく同時実行数・レート窓・文脈
  キャッシュで上限を掛けます。生成済みレポートの閲覧は View、プロバイダ設定は Admin です。プロンプトに
  引用される機器出力はフェンスで囲われ、各プロバイダのエンドポイントは設定項目ではなくコンパイル時
  定数なので設定画面が持ち出し経路になることはありません。そして **Yagra がネットワーク機器を設定する
  手段は依然として存在しません**。
- **各ノードが「どのポーラーがポーリングしているか」を表示し、プールをインベントリツリーから割り当て
  られるようになりました。** ノード→ポーラーの割り当ては以前から存在したものの、実質的に不可視でした
  — 「このノードはどのポーラーが見ているのか」を答えるには `redis-cli` を叩くしかなく、ノードの
  プールはどの画面も送っていない API フィールドからしか書けませんでした。ノード詳細に**プール**と
  **ポーリング元**が表示されます。後者はハッシュリングから再計算するのではなく**コアが実際に配布した
  ワーキングセット**から読む 5 状態の回答（割当済み / 保留 / レガシー fan-out / Meraki / 不明）なので、
  ノード側の答えとポーラー側のノード一覧が食い違うことはありません。**設定 ▸ ポーラー**からは各
  ポーラーの担当ノード一覧をその場で展開できます。プールはノードとフォルダの両方で編集でき、
  **インベントリツリーで右クリックするとプールのチップ列**（既存のプール＋継承＋カスタム）が出ます —
  ツリーには複数選択が無いため、これが唯一の一括割り当て手段です。ノードの実効プールは「自身 → 最も
  近い祖先フォルダ → `default`」の順に解決され、各チップはそのプールに**稼働中のポーラーがいるか**を
  示します。いないプールに割り当てると、ジョブが誰も購読していないサブジェクトへ配信され、ノードは
  黙って監視されなくなるからです。
- **URL / DNS 監視の設定を、作成後に編集・削除できるようになりました。** これまではノード追加
  ダイアログで作成し、ノード詳細で表示することはできても、URL・タイムアウト・リゾルバ・レコード
  種別を変更するにはノードごと削除して作り直すしかありませんでした（エンドポイントは最初から
  存在し、呼び出し元だけが無い状態でした）。ノードの URL/DNS ヘルスカードに ⋮ メニューが付き、
  **編集**と**監視設定を削除**が行えます。編集フォームは期待する HTTP ステータス（2xx すべて／
  コード列挙／範囲指定）を含む全項目をカバーします。監視設定を削除してもノードはインベントリに
  残り、記録済みの履歴も保持され、以後チェックされなくなるだけです。他の監視設定変更と同じ権限
  （Admin）が必要です。

- **API が自身の OpenAPI 3.1 文書を `GET /api/v1/openapi.json` で公開するようになりました。** 文書は
  ハンドラそのものから生成されます — パス・クエリパラメータ・リクエストボディ・レスポンス形状・
  エラーコードのすべて — ので、「誰かが書き残したもの」ではなく**サーバが実際に行っていること**を
  記述します。`/api/v1/version` や `/api/v1/config` と同様に認証不要です（在庫・設定・状態を一切
  含まず、全デプロイで内容が同一のため）。任意の OpenAPI クライアントジェネレータをこの URL に
  向けられます。WebUI 自身の型と API クライアントもこの同じ文書から生成されるようになり、
  **何も検査していなかった 3,340 行の手書き TypeScript** がなくなりました。

### 改善
- **グループスコープによる可視範囲制限（ADR-014）が、読み取り面の全体で強制されるようになりました。**
  ノードに紐づくデータを返すすべてのエンドポイントが、呼び出し元アカウントのフォルダグループ
  スコープで絞り込みます。SQL の一覧には索引の効く述語が入り、ランキングやメモリ内集計は取得後に
  絞り込まれ、SSE の 2 本のライブストリームはスコープ外ノードのイベントを流さず、ノード単体の
  経路は見えないノードに対して `403` ではなく **`404`** を返します（ノード ID の総当たり探索を
  許さないため）。リクエストボディでノードを指定する運用操作（確認応答・ミュート・メンテナンス
  ウィンドウ・即時ポーリング・分析の実行）も同様に検査されます。**現時点でスコープを保持できる
  アカウントは存在しない**ため、既存のデプロイの挙動は何も変わりません — これはスコープを発行
  できるようにする前に必要な、強制の側の実装です。なお絞り込みが原理的にできない 5 つの読み取り
  （保存済みレポート、フリート状態のタイムライン、合計スループット、フリート横断の `/flow/*`
  集計）は、フリート全体の数値を返す代わりに `403 scope_unsupported` で拒否します。いずれも
  集計済みで保存・計算されており、絞り込みの手がかりとなるノード単位の帰属が残っていません。
- **アクティブアラートの行が、UUID 2 つではなく「どの機器で何が壊れたか」を表示するようになりました。**
  従来は `● 550e8400-… 7c9e6679-… 2分前` と読めていました。ノードは名前に解決され（ID は他の画面と
  同様ホバー時のみ）、チェック ID は（ノードとメトリックの一方向ハッシュなので解決できる名前が
  存在しません）**そのチェックが実際に測っていたもの**に置き換わります — 例：
  `icmp_rtt_ms above 100 (was 450)`。この情報は元々すべてのアラートに保存され、アラート ▸ 履歴には
  表示されていましたが、トリアージ画面だけが出していませんでした。両画面は同じ整形関数を通るように
  なったので、食い違うことはありません。
- **設定 ▸ システムヘルスがフローストアを表示し、サーバー自身の判定を示すようになりました。**
  ページの総合判定は ClickHouse を数えていたのに行が無かったため、フローストア障害が
  「すべて到達可能」に見えていました。5 つのバックエンドストアすべてとバスを列挙し、
  「すべて到達可能」/「劣化」バッジを**サーバーの回答から**表示します（行から再計算しません）—
  次に列挙を忘れた依存があれば、目に見えて食い違います。
- **ポールループの健全性がワーキングセットの配布状況を表示します。** ワーキングセットとして配布して
  いるプール数と、ジョブ単位の配信にフォールバックしているプール数（後者は 0 でなくなると琥珀色に
  なります — そのプールに稼働中の登録ポーラーが無いという意味です）、スナップショットと差分の配布数、
  割当ミラーの書き込み数を追加しました。設定 ▸ ポーラーにも各ポーラーの初回登録日を示す
  **登録日**列が付きます。
- **アプリのアイコンを Yagra ウェブサイトと同じものに統一しました。** ブラウザタブの favicon と、
  上部バー / モバイル上部バー / サインイン画面のマークが、旧「二重輪」から**「Y に見えるトポロジの
  フォーク」**（1 つのルートノードが 2 つに分岐する形）に変わります。オレンジの朱印・配色・サイズは
  従来どおりです。タブのアイコンはキャッシュのためスーパーリロードが必要な場合があります。
- **イベント検索の絞り込みとページングが、取り込み時刻ではなく発生時刻を基準になりました。**
  PostgreSQL 経路は取り込み時刻を、VictoriaLogs 経路は既に発生時刻を使っていたため、有効なログ
  ストアによって同じ検索が違う行・違う並び順を返していました。両者とも発生時刻に統一されます。
  時刻順ログである以上避けられない副作用として、リモートポーラーが再接続してバッファした結果を
  再送すると（store-and-forward）、既にスクロールして通過したページより**後ろ**に古いイベントが
  挿入されることがあります — これは VictoriaLogs 経路の従来からの挙動と同じです。
- **フラッピング監視ウィジェットがノード名を表示するようになりました。** これまで生のノード UUID と
  チェック UUID を表示していましたが、「発生中のアラート」一覧と同じ流儀で、ノード名（UUID はホバーで
  表示）と、フラッピングしているチェックが何を測っているかを表示します。

### バグ修正
- **WebUI が知らない状態のレポート実行が「失敗」と表示されていました。** ステータスバッジの
  フォールバックが、未知の値をすべて赤（critical）で描画していたため、**実際には成功した実行が
  壊れたように見える**ことがありました。特にローリングアップグレード中、古い WebUI が新しいコアの
  書いた行を読むときに顕在化します。実行状態・トリガー・スケジュール周期を API 契約上の閉じた集合
  （`queued`/`running`/`succeeded`/`failed`/`unknown` など）にし、バッジを状態ごとのマップ
  （フォールバックなし）に置き換えました。本当に未知の状態は「不明な状態」として中立に表示します。
  **ワイヤ上の変更はありません** — 同じ文字列を、記述しただけです。
- **DNS 監視の失敗理由が、内部トークンのまま表示されていました。** ノード ▸ DNS の解決結果列に
  `nx_domain` / `serv_fail` / `depth_exceeded` など 9 種類が**翻訳されずそのまま**出ていました。
  今後は両言語で文章として表示されます（「名前が存在しない（NXDOMAIN）」など）。
- **1 つのイベントストリームに限定したアラートルールが、黙って全ストリームに広がることがありました。**
  このビルドが知らないソース種別を指定したイベントルールは「種別フィルタなし」として解釈され、
  マッチャはそれを**任意の種別**と読みます。結果として、syslog・トラップ・Webhook のすべてに対して
  発火していました。今後そうしたルールはエンジンに載せず、ログに記録して、コアが理解できるように
  なるまで待機します。
- **プールを移動したノードを、元のポーラーがポーリングし続けていました。** スケジューラはノード行
  だけからプール一覧を組み立てていたため、最後のノードが抜けたプールは一覧から消え、二度と再調整
  されませんでした。そのポーラーはコアプロセスが生きている限り古いワーキングセットをポーリングし
  続け、移動したノードをすべて二重にポーリングします。復旧にはポーラーの再起動が必要でした。ノード
  走査の前に、稼働中の全プールを一覧に必ず入れるようにしました。これまではデータベースを直接編集
  しないと踏めない経路でしたが、WebUI からプールを編集できるようになると運用者が最初に行う操作に
  なるため、同時に修正しています。
- **フィルタ付きのフロー転送先が、コレクタに必要なテンプレートデータグラムを落としていました。**
  フィルタ付きの転送先はデコード済みのフローレコードを判定しますが、NetFlow v9 のテンプレート定義
  のみを載せたデータグラムにはレコードが 1 件もありません。そのため、テンプレートをレコードなしの
  データグラムで別送するエクスポータでは、フィルタ付きコレクタが**永久に復号できないデータセット**を
  受け取り続けていました（無言で、恒久的に）。テンプレート定義があり、フローレコードが 0 件の
  データグラムはフィルタを迂回するようになります。判定基準は意図的に「レコード 0 件」ではなく
  「テンプレートがあってレコード 0 件」です — テンプレート未知のデータセットもデコード結果は 0 件に
  なりますが、そちらはコレクタに何も与えないので従来どおりフィルタが判断します。テンプレートを毎回
  同梱するエクスポータは元から影響を受けません。実機のコレクタに対する検証で発見しました。
- **1 つのノードに URL 監視と DNS 監視の両方が設定でき、DNS 側が一度も実行されませんでした。**
  「ノードはちょうど 1 種類」というガードが DNS 側の書き込みにしか入っていなかったため、DNS 監視中の
  ノードへ URL チェックを足す操作が受理・保存され、URL を先に解決するスケジューラは DNS チェックを
  永久に実行しませんでした。両方の書き込みが同じガードを通るようになり、優先順位の定義も 2 箇所から
  1 箇所になりました。
- **MCP の `get_fleet_summary` が、観測していないノード状態を省いていました。** REST 側の集計は 6 状態
  すべてをあらかじめ埋めるのに対し、MCP ツールは実際に存在した状態しか入れていませんでした。その
  ため `states["warning"]` を読む AI クライアントには、WebUI なら 0 が返る場所でキーそのものが無く、
  「警告データは存在しない」と自信を持って報告される状態でした。両経路が同じ集計を返します。併せて、
  ノードが 1 台も無いときのデータカバレッジが 0% ではなく 100% と読まれるようになり、新規インストール
  のたびに「監視の死角」ウィジェットが点灯して無視されるようになる、ということがなくなります。
- **鍵の不備や DB 到達不能が「入力が不正です」と表示されていました（OIDC 設定）。** ID プロバイダの
  保存は**あらゆる**失敗を `400 invalid_provider` として内部エラー文字列を添えて返していたため、鍵
  暗号鍵の問題や DB 書き込み失敗が入力ミスとして報告され、内部メッセージがそのままワイヤに出ていました。
  今後、入力不備はどの項目かを述べる 400、内部障害は原因をログに残す不透明な 500、サインイン中に IdP へ
  到達できない場合は 500 ではなく 502 になります。
- **正規表現によるイベント検索が、VictoriaLogs では大文字小文字を区別し、PostgreSQL では区別して
  いませんでした。** 有効なログストアによって同じ正規表現が違う行にマッチしていたため、どちらも
  区別しないように揃えました。なお**プレーン語**の検索は、VictoriaLogs ではトークン単位、PostgreSQL
  では部分一致のままです — 転置インデックスは語の先頭以外の部分一致を全ブロック走査なしには扱えず、
  実機計測で 0.22 秒に対し 30 秒でした — のでこの 1 軸だけは意図的な差異として残し、トークンの内側まで
  探したいときは検索ボックスの正規表現トグルを使ってください。
- **ポーラーの store-and-forward バッファがディスクを使えるようになりました。** コンテナイメージが
  スピル用ディレクトリを作成しておらず、`/var/lib` は非 root の実行ユーザーには書き込めないため、
  起動時に警告を 1 行出したあとメモリのみで動作していました。そのためメモリ上のリングを超える長さの
  バス断ではスピルされずに古いポーリング結果が捨てられ、断の最中にポーラーを再起動すると保持していた
  ぶんがすべて失われていました。ディレクトリを適切な所有者でイメージ内に作成し、テストサーバ構成では
  名前付きボリュームを与えたのでコンテナ再作成をまたいでスピルが残ります。
- **ノードが二重に追加される問題。** 監視追加ダイアログの送信ボタンがリクエスト中に保護されて
  いなかったため、ダブルクリックでノードが 2 つ作られていました。またダイアログが前回の失敗メッセージを
  保持していたため、失敗後にキャンセルして開き直すと空のフォームの上に古いエラーが表示されていました。
- **OpenAPI 文書が「REST API でパーソナルアクセストークンが使える」と記述していました（誤り）。**
  公開していた `bearer` スキームはセッショントークンの代替として `yat_…` を挙げていましたが、この API
  の認証エッジはセッショントークンしか受け付けないため、契約どおりに実装したクライアントは全呼び出しで
  401 になっていました。記述を実態に合わせました — パーソナルアクセストークンは `/mcp` の MCP 面を
  認証するもので、REST API には `POST /api/v1/auth/login` で得たトークンが必要です。
- **ダイアログがキーボード操作を内側に保持するようになりました。** これまで Tab を押すと開いている
  ダイアログの外（背後のページ）へ抜けてしまい、`aria-modal` が「背後は不活性」と伝えているにも
  かかわらず、キーボードやスクリーンリーダーの利用者がオーバーレイに隠れた入力を操作しうる状態でした。
  Tab / Shift+Tab はダイアログ内を巡回するようになり、トラップするのは最前面の 1 枚だけです。
- **通知ベルが機能するようになりました。** 発生中アラート件数を表示しながらクリックを無視していた
  ベルが、デスクトップ・モバイル両方の上部バーで「発生中のアラート」を開くようになりました。
- **ダークテーマで 3 色が読みづらかった問題。** パレットの 4・5・6 番目がダーク面でもライトテーマの
  値のままでした。これらはグラフだけでなく、Troubleshoot のレポート本文の大半と、受動イベント／
  キャパシティのダッシュボードウィジェットでも使われています。
- **しきい値が生カウンタを判定しなくなりました。** カウンタ系メトリクス（`if_hc_in_octets` や
  errors / discards など、収集カタログがカウンタと宣言しているもの）へのしきい値は、単調増加する
  生の累計値を境界と比較していたため、`above` ルールは一度超えると永久に発火したまま、`below`
  ルールは機器再起動によるカウンタリセットのたびに誤アラートを出していました。カウンタのサンプルは
  OK として扱われるようになり（これにより既存のラッチ済みアラートも通常の回復経路で解消されます）、
  カウンタ系メトリクスへの `POST /api/v1/thresholds` は **400 `counter_metric`** を返します。
  レート／使用率はクエリ時導出のままです（ADR-012）。しきい値はゲージに設定してください。
- **レポートの削除失敗が黙って閉じなくなりました。** レポートテンプレート／保存済みレポート／
  スケジュールの削除が失敗してもエラーを握りつぶしてダイアログを閉じ、成功したように見えていました。
  他の削除ダイアログと同じく、確認ダイアログが開いたままエラーを表示します。またレポートビルダーと
  スケジュールダイアログは保存中に Cancel を無効化します。

### セキュリティ
- **SSO アカウントを無効化しても、本人はサインインし直せていました。** ローカルパスワードのログイン
  経路は以前から無効なアカウントを拒否していましたが、SSO のコールバックは「IdP が本人だと言っている」
  から直接セッション発行へ進み、`users.enabled` を一度も読んでいませんでした。そのため SSO で
  プロビジョニングされたアカウントを無効化しても、稼働中のセッションが失効するだけで、次の SSO
  ログインが新しいセッションを発行してしまい、**IdP 側で止める手段を持たない運用者にとってこの操作は
  事実上の無効**でした。SSO 経路でも無効なアカウントを拒否し、他のコールバック失敗と同じ不透明な
  `oidc_denied` を返します。
- **API トークンで認証された変更操作が、匿名として監査記録されていました。** `audit_mw` は Bearer を
  *セッション*ストアだけで解決しており、トークンはそこに存在しません。トークンが `/mcp` 専用の間は
  この経路に到達しませんでしたが、REST を開けば到達します。Bearer はリクエストごとに一度だけ解決
  されるようになり、監査行にはアカウントと資格情報の両方(`svc-ci (token:grafana)`)が記録されます。
  トークン経由の変更も、責任を負う人物にたどれます。
- **URL 監視が IPv6 のループバック / リンクローカル宛先を受理していました。** エッジのバリデータは
  URL のホストを IP アドレスとして解釈して SSRF になりうる宛先を弾いていましたが、URL パーサが返す
  IPv6 リテラルは**角括弧付き**（`[::1]`）で、アドレスパーサはこれを拒否します。そのため IPv6 の
  宛先はすべてホスト名として扱われ、ブロックを丸ごと素通りしていました。URL 監視の作成・編集で
  `http://[::1]/` や `http://[fe80::1]/` が通っていたということです。**これは持ち出し経路ではなく、
  「何もしていなかったチェック」でした** — ポーラー側が実際のプローブ時に同じアドレスを拒否するため、
  実リクエストが飛んだことはありません。URL のホストを解釈する箇所は他に 3 つあり、そちらはいずれも
  正しく実装されていました。4 箇所とも 1 つの実装を共有します。

## v0.1.18

**受信した受動データを、そのまま次へ渡す。そして可視化する。** Yagra は syslog・SNMP トラップ・フロー
エクスポートを受信できていましたが、本リリースではそれらを**転送**できるようになりました。フィルタ付きの
ティー（tee）で SIEM やコレクタへ、バイト単位そのまま、または再構築して、UDP／TCP／TLS で送ります。さらに
長期のクエリ用に正規化された行を流し込む **BigQuery** 宛先も追加しました。あわせて、**DNS 名前解決監視**を
第一級のノード種別として、受動イベント／フローストアに対する **11 個の新しい Troubleshoot 分析**、
**15 個すべての分析に専用のレポート画面**、受動イベントとトラフィックフローの**新規ダッシュボード
ウィジェット 13 個**を追加しています。

### 新機能
- **転送（ティー）宛先** — 新しい **Settings ▸ Forwarding** 画面で、「このフィルタに一致するものはすべて
  そこへも送る」という宛先を定義します。送信はリーダーの core だけが行うため、ファイアウォールで許可する
  のはポーラーごとではなく**1 か所**の送信元だけで済み、機器ごとに 2 つ目のエクスポート先を設定する必要も
  なくなります。
  - **syslog と SNMP トラップ**は**バイト単位そのまま**中継します。解析済みフィールドとは別に元の
    データグラムを保持しているため、コレクタは機器が送ったものをそのまま受け取ります。元データが存在
    しない場合は、忠実な RFC 5424／SNMPv2c メッセージを再構築して送ります。
  - **フローエクスポート**（NetFlow v5/v9/IPFIX、sFlow）はテンプレートのデータグラムも含めてそのまま
    中継します。集約済みのフローレコードでは代用できません — バケット化・上位 N の切り捨て・5 タプルの
    畳み込みはいずれも不可逆だからです。フローのフィルタは **any-record**（1 レコードでも一致すれば
    データグラム全体を転送）判定です。レコードは再エンコードなしに取り除けないためです。
  - **syslog over TLS**（RFC 5425）は、システムのトラストストアに加え、宛先ごとの任意の CA 証明書で
    コレクタを検証します。
  - **BigQuery 宛先**は `tabledata.insertAll` で**正規化された型付きの行**（イベント 1 件＝1 行、フロー
    レコード 1 件＝1 行）を流し込みます。ライブのストリームを複製するのではなく、数か月分の履歴を
    クエリするための経路です。行は独立しているため、ここでのフローフィルタは**レコード単位で厳密**に
    効きます。テーブルは日次パーティションとクラスタリング付きで自動作成されますが、データセットは
    作成しません — データ所在地を Yagra が勝手に決めないためです。
  - 各宛先は上限付きキュー・レート制限・サーキットブレーカを備え、**黙って劣化することはありません**。
    バイト完全一致を約束した宛先に元データが渡らなければカウントされ、元バイトを供給できないポーラーは
    画面上に名指しで表示されます。
- **DNS 名前解決監視（ノード種別）** — URL を監視するのと同じ感覚で*名前*を監視できます。ノードを組み込みの
  **DNS name resolution** プロファイルに紐付けると、名前が解決できるかどうかと、dig のような再帰的な
  **CNAME チェーン**を記録します。履歴は**チェーンが実際に変化したときだけ**追記されます（TTL の
  カウントダウンやラウンドロビンの並び替えは変化として扱いません）。数値サマリ（`dns_up`、
  `dns_resolve_ms`、`dns_chain_length`、`dns_answer_count`）はグラフ化・アラート対象で、`dns_up` の既定
  しきい値が投入済みなので、新しいモニタは作成した時点でアラートが機能します。
- **受動イベント／フロー向けの新しい Troubleshoot 分析 11 種** — 受動系は `event_storm`、`event_flap`、
  `severity_shift`、`rule_gap`、`auth_probe`。フロー系は `traffic_anomaly`、`talker_shift`、
  `new_destination`、`flow_scan`。さらにメトリクス・イベント・フローを横断して読む `saturation` と
  `incident_correlate`。従来どおり分析は**読み取り**であり（機器 I/O は行いません）、同じ同時実行数・
  レート制限の下で動作します。
- **すべての Troubleshoot 分析に専用レポート。** これまで実質的なレポートがあったのはアノマリー分析だけで、
  残る 14 ツールは「coming soon」のトーストを出すだけでした。いまは 15 種すべてが、それぞれの検出結果に
  合わせた画面を持ちます — `incident_correlate` の SVG **インシデントタイムライン**（シグナルが届いた
  「順序」こそが原因を指し示すため）、`flow_scan` のスキャン形状の散布図、`saturation` のシェアメーターと
  キャパシティ文脈など。全レポートが CSV エクスポートと `?job=` ディープリンクに対応します。
- **受動イベント／トラフィックフローのダッシュボードウィジェット** — カタログに 2 セクション、**13 個**の
  ウィジェットを追加しました。イベントフィード、件数推移、種別ミックス、上位トラップ、トリアージ、
  ノイズの多い送信元、ルールカバレッジ。加えて上位トーカー、上位 AS、上位ポート、プロトコルミックス、
  会話 Sankey、トラフィックトレンド。フロー系ウィジェットはノード単位ではなく**フリート全体**（全
  エクスポータ）を読みます。
- **フロー／イベント向けの新しい MCP ツール** — `flow_fanout` と `event_stats` を追加し、`top_flows` に
  プロトコル／ポート／ピア／ASN／方向のフィルタと AS 名解決が加わりました。`run_analysis` は新しい分析
  種別をすべて受け付けます。

### 改善
- **メトリクス／ログストアのコンテナイメージを固定。** `docker-compose.yml` と
  `docker-compose.deploy.yml` が `victoria-metrics:latest`／`victoria-logs:latest` を参照していましたが、
  いずれも明示的なバージョンを指すようにしました。無関係な `docker compose pull` が履歴の下でストレージ
  エンジンを入れ替えてしまうことはなくなります。更新はバックアップを取ったうえで意図的に行ってください —
  固定済みの Rust ベースイメージと同じ方針です。
- **負荷時のストア&フォワードの退避が軽くなりました。** バス障害中に結果をバッファリングするリモート
  ポーラーが、退避 1 件ごとにファイルシステムの空き容量を問い合わせる（非同期ランタイム上での）システム
  コールを発行しなくなりました。読み取り値はキャッシュされ、書き込んだバイト数で差し引かれるため、安全
  フロアは遅れてではなく**早めに**作動します。
- **トラフィックフローの表示が見やすくなりました** — 会話のエンドポイントに最小ラベル幅を設けて長い
  アドレスでも読めるようにし、会話 Sankey は無制限に伸びずサイズ上限を持つようになり、送信元／宛先の
  AS 番号をアドレスと併記するようにしました。
- **フリート全体のフローエンドポイント。** フロー API はノード限定ではなくなりました。系列、上位トーカー、
  会話、上位ポート、プロトコル、上位 AS のいずれもフリートスコープを取れます。新しい `/events/stats`
  エンドポイントは、イベントログと同じフィルタでカテゴリ別・時系列のイベント集計を PostgreSQL または
  VictoriaLogs 経由で返すため、サマリとログの数字が常に一致します。

### 修正
- **知らないチェック種別に出会ってもポーラーが停止しなくなりました。** ワーキングセットのスナップショット
  チャンクを要素ごとにデコードするようにしたため、未知のチェック 1 件がチャンク全体を失敗させ、シーケンスに
  欠落を作り、そのポーラーを再同期ループに陥らせる（結果としてそのポーラーの*すべて*のポーリングが止まる）
  ことはなくなりました。DNS に限らず、今後追加されるあらゆるチェック種別に対する恒久的な修正です。
- **DNS チェック同士が互いを打ち消さなくなりました。** ターゲット単位の single-flight ポーリングガードは、
  DNS モニタが設計上リゾルバのターゲットを共有するため、1 サイクルにつき 1 件を残して他をすべて落として
  いました。グローバルガードを使うようにしています。
- **AS ドリルダウンが Conversation flow を壊さなくなりました。** AS で絞り込むと会話テーブルと Sankey が
  500 を返し（集約に、絞り込み対象の列と同じ名前のエイリアスを付けていたため）、他のパネルだけが更新されて
  画面に古いデータが残っていました。
- **Troubleshoot レポートのディープリンクが正しく解決されます。** 直近ジョブの取得範囲より古い `?job=` は
  何も要求されていないかのように表示され、別ツールに属する `?job=` は無関係な検出結果の上に誤ったレポートを
  描画していました。レポートシェルがジョブを直接取得し、その結果を読めるレポートへリダイレクトします。

### セキュリティ
- **転送は、そもそも漏れない作りにしています。** TLS 宛先には**証明書検証を無効化する手段がありません**。
  BigQuery 宛先には意図的に**生ペイロードの列がありません** — 中継されたデータグラムは運用者が選んだ
  コレクタに一度渡るだけですが、テーブルは残り続けます。生バイトの列があれば、syslog 本文に日常的に現れる
  資格情報が恒久的に外部でクエリ可能になってしまうためです。注意書きが読まれることに期待するのではなく、
  API が BigQuery に対する verbatim モードを拒否します。サービスアカウントの署名には定数時間の RSA 実装を
  使用しています。
- **メンテナンスされていない TLS 依存を除去。** `rustls-pemfile`（RUSTSEC-2025-0134、上流でアーカイブ済み）
  を削除し、証明書と鍵の解析は同じコードを `rustls-pki-types` から直接利用するようにしました。サプライ
  チェーンのポリシーチェックは advisories・bans・licenses・sources のすべてでクリーンです。
- **クライアント側のオープンリダイレクトの脆弱性を解消**（フロントエンドのルータ更新）、あわせてビルド
  ツールチェーンのパストラバーサルの脆弱性も解消しました。前者は利用者に影響するもの、後者はビルド時のみで
  製品には同梱されません。

## v0.1.17

**AI アシスタントから Yagra を操作。** Yagra に組み込みの **MCP（Model Context Protocol）サーバ**が
加わりました。オプトインかつ認証付きのツール面を `/mcp` に公開し、AI クライアント（Claude Code/Desktop
など、MCP 対応アシスタント）から稼働中の監視状態を照会し、自然な言葉で診断を実行できます。既定は無効で、
有効化してもほとんどが読み取り専用です。数少ない書き込みツールは権限で保護・監査され、機器の設定は一切
変更できません。

### 新機能
- **MCP サーバ（AI／自動化ツール面）** — `YAGRA_ENABLE_MCP` で API ポートの `/mcp` に Streamable HTTP の
  MCP エンドポイントを公開します。**15 個のツール**を提供します。読み取りツールはフリート概要、ノード、
  ノード状態、アクティブ／履歴アラート、メトリクス、トポロジ、トラフィックフロー、受動イベント
  （syslog／トラップ／Webhook）に対応し、オンデマンドの **Troubleshoot 分析**（アノマリー／相関／
  キャパシティ／フラップ）と、**3 個の書き込みツール**（アラートの確認、メンテナンス枠の開始、即時
  ポーリング）を備えます。ツール出力はサニタイズされ（監視資格情報がシステム外に出ることはありません）、
  **ネットワーク機器を設定・変更するツールは存在しません**。
- **API トークン** — 新しい **Settings ▸ API tokens** 画面で、MCP アシスタントのような非ブラウザ
  クライアント向けに、長寿命の `yat_` トークン（ロールとグループスコープ付き）を発行します。生のトークンは
  作成時に**一度だけ**表示され、保存されるのはハッシュのみです。発行・失効は管理者操作で、監査されます。

### 改善
- **オンデマンド分析にレート・同時実行の上限。** Troubleshoot 分析ランナーが、同時に実行する分析数
  （`YAGRA_ANALYSIS_MAX_CONCURRENT`、既定 4）と 1 分あたりの開始数（`YAGRA_ANALYSIS_RATE_PER_MIN`、
  既定 30）を制限するようになりました。飽和時は処理を積み増さずに API が `429` を返します。UI と新しい
  MCP 分析ツールの双方のコストを抑えます。

### セキュリティ
- **MCP 書き込み操作のツール単位の認可と監査。** すべての MCP 書き込みツールが呼び出し元のロール（RBAC）
  を再確認し、監査エントリを記録します。フェイルクローズ設計で、呼び出し元を認可できない場合は書き込みを
  拒否します。**Viewer** トークンは読み取り専用のままです。
- **フロントエンドのビルドツールチェーンの High 深刻度 2 件を解消。** 推移的な開発依存（`brace-expansion`、
  `js-yaml`）を更新しました。いずれもビルド時のみで製品には同梱されませんが、依存ツリーはクリーンに
  なりました。

## v0.1.16

**ライセンスとフロー取り込みの堅牢化。** Yagra は **GNU Affero General Public License v3.0
（AGPL-3.0-only）** の下で提供されるようになりました。あわせて本リリースでは、フローストアが
混雑・低速でもシステム全体に影響しないよう、トラフィックフローの取り込み経路を堅牢化しています。
新しいユーザ向け機能はありません。既定のインストールは v0.1.15 と同じ挙動のまま、新しい
ライセンスで動作します。

### ライセンス
- **AGPL-3.0-only へ再ライセンス。** すべてのソースファイルに SPDX ヘッダを付与し、リポジトリは
  `AGPL-3.0-only` で提供されます。Yagra は通常ネットワークサービスとして運用されるため、AGPL
  **§13** に注意してください: **改変版**をネットワーク越しに他者へ利用させる場合、そのソースを
  提供する必要があります。AGPL 以外の条件での利用（プロプライエタリ製品への組み込みなど）には
  **商用ライセンス**を提供できる場合があります（README を参照）。

### 改善
- **フロー取り込みが低速なフローストアから隔離されました。** ClickHouse フローライタをフローバス
  コンシューマから分離し、行を境界付きキュー経由で受け渡すようにしました。ClickHouse が低速・
  ハングしても `yagra.flows` サブスクリプションが詰まって無言でメッセージを落とすことはなくなり、
  高負荷時はフロー行を（ロス許容の設計に従い）破棄・計上して、取り込みを止めません。
- **フローのエクスポータ解決がバッチごとにノードテーブルを再読込しなくなりました。** 送信元 IP が
  登録ノードでないエクスポータ（loopback から export するルータで一般的）からのフローは、以前は
  バッチごとに全ノードテーブルを再スキャンしていました。未マップのエクスポータは最大 1 回のみ
  再試行するようになり、数万ノード規模でもフロー取り込みが軽量に保たれます。

## v0.1.15

**ネットワークを流れるトラフィックを可視化。** Yagra はネットワークの**フロー**情報（NetFlow・IPFIX・
sFlow）を収集・分析できるようになりました。これまでの死活・性能監視と並べて、ネットワークを流れる上位の
トーカー・ポート・プロトコル・AS（自律システム）を確認できます。受信した SNMP トラップも読みやすい名前に
解決され、アクティブアラート画面は大規模障害時でも快適に動作します。

### 新機能
- **トラフィックフロー監視（NetFlow v5/v9・IPFIX・sFlow v5）** — 機器のフローエクスポートをポーラ
  （NetFlow/IPFIX は `:2055`、sFlow は `:6343`）へ向けると、Yagra が収集し、エッジで集約（時間バケット
  ごとに上位 N 件）して、専用の **ClickHouse** フローストア（保持 1 か月・ロス許容）に保存します。各ノード
  に **Flow** タブが追加され、上位トーカー・上位ポート・上位プロトコル、そして会話の **Sankey** 図を表示
  します。トーカー／ポート／プロトコルをクリックして絞り込めます。フローはポーラ単位のオプトインで、
  メトリクスとは完全に独立しています。無効のままなら挙動は変わりません。
- **フローの AS（自律システム）補完** — フローの通信相手を、オフラインの IP→ASN テーブルから **AS 番号と
  名前**に解決します。**Top AS** カードとドリルダウン（AS をクリックしてそのフローを絞り込み）、AS 単位の
  会話ビューを提供します。AS を送出しないエクスポータ（多くの非 BGP 機器）でも自動的に補完します。データ
  セットは Docker 内の更新用サイドカーで最新に保たれ、稼働中のコアへ**再起動なしでホットリロード**されます。
  補完はオプトインです。
- **リモート拠点のポーラでもフローを収集** — 拠点のポーラがローカルのフローエクスポートを受信し、（認証
  付きの）バス経由でコアへ転送できます。ポーリングと同じ分散ポーラ構成でフローが機能します。
- **SNMP トラップの名前解決** — 受信した SNMP トラップ（v1/v2c）が、トラップ OID を**人間可読な名前**に
  解決し、**組み込みのトラップイベントルール**を同梱し、トラップバッジを表示します。トラップが生の OID では
  なく名前付きイベントとして届きます。

### 改善
- **大規模障害時もアクティブアラートが快適** — アクティブアラートのトリアージ一覧を仮想化（ウィンドウ表示）
  しました。数千件のアラートが同時発生しても、全件ではなく画面に見える行だけを描画するため、最も必要な
  ときに画面が重くなりません。
- **フロー補完のリフレッシュが滑らかに** — IP→ASN データセットの定期リロードでコアが一瞬止まることが
  なくなりました（大きなテーブルの解析を非同期ランタイム外で実行）。

## v0.1.14

**マルチコア構成と分散配置のためのハードニング。** セッションがコアのフェイルオーバーを越えて維持される
ようになり、各リモートポーラには自分のプールに必要な資格情報だけを渡せるようになりました — ネットワーク
越しにポーラを配置するサイトのメッセージバスをより堅牢にします。いずれもオプトインで、既定の単一ノード
構成の挙動はこれまでどおりです。

### 新機能
- **セッションがコアのフェイルオーバーを越えて維持（オプトイン）** — 高可用構成では、各 `yagra-core` に
  共有のセッション署名鍵をマウントすると（`YAGRA_SESSION_KEY_FILE`）、ログインがどのコアでも受け付けられ、
  コアの再起動やフェイルオーバーをまたいで有効になります。HA のフェイルオーバーで全員が再ログインを強い
  られることがなくなります。ログアウト、ユーザーの無効化、ロール変更、パスワードのリセットは、すべての
  コアに即時に反映されます。鍵を設定しなければ、セッションの挙動はこれまでどおり（コアごとのトークン）です。

### バグ修正
- **セキュアなリモートバスでメトリクスの後追い補完が動作** — TLS と認証を有効にした NATS バスでは、再接続
  したリモートポーラがバッファ済みメトリクスを publish する権限を持たず、store-and-forward の後追い補完
  （v0.1.13 の新機能）がセキュアなバス上で再送できていませんでした。ポーラが backfill チャネルへ publish
  できるようになり、分断期間のメトリクスが意図どおり埋め戻されます。平文の単一ノードバスには影響ありません。

### セキュリティ
- **メッセージバスのポーラ単位資格情報（オプトイン）** — NATS バスをリモートポーラへ公開する構成で、コアが
  各ポーラに対しそのポーラのプールだけにスコープした資格情報を発行できるようになりました。各ポーラは自分の
  **プール**のジョブと機器資格情報だけを受け取り、全ポーラが 1 つのバスアカウントを共有してフリート全体に
  到達できる従来の状態を解消します。単一のリモートポーラが侵害された場合に見える範囲を大幅に狭めます。バスの
  TLS＋認証設定と併せて有効化してください。既定では無効で、有効化するまで共有アカウントの挙動は変わりません。

## v0.1.13

**リモートポーラがネットワーク分断を乗り越えます。** 中央コアから切り離されたポーラは、ローカルで監視を
続け、回線が戻ったときに収集済みのメトリクスを後追いで補完します。WAN の瞬断が、履歴の「穴」ではなく
埋め戻された「ギャップ」になります。

### 新機能
- **リモートポーラの store-and-forward（既定 ON）** — ポーラがコアへの接続を失っても（WAN 障害や
  ファイアウォールの瞬断）、機器のポーリングをローカルで継続し、結果を破棄せずにバッファします。再接続時に
  まとめて再送し、コアは各メトリクスを**本来のタイムスタンプ**で取り込むため、グラフと履歴は分断期間を
  スパイクなしで埋め戻します。アラートは意図的に再送**しません** — アラート評価は「今」から再開するので、
  再接続時に解決済みの古いアラートが大量発報することはありません。バッファは有界で（メモリ上のリングに
  加え、ポーラ再起動をまたいで残るディスクスピル）、最古のデータから破棄するため、ポーラのディスクを埋め
  尽くすことはありません。上限の調整や無効化は `YAGRA_STORE_FORWARD*` 設定で行えます
  （`docker-compose.poller.yml` を参照）。
- **Pollers ページに「最近の監視ギャップ」** — 設定 ▸ ポーラーに、コアがポーラーとの通信を失っていた
  各期間（どのポーラー・どのプール・いつ・どのくらいの長さ）が一覧表示され、監視が途切れていた時間帯と、
  メトリクスが補完されたことを一目で確認できます。

## v0.1.12

**シングルサインオンとコアの高可用性。** 組織の ID プロバイダでサインインでき、コアを複数台で動かして
1台の障害では監視が止まらないようにできます。

### 新機能
- **SSO（OpenID Connect）でサインイン** — 外部 ID プロバイダ（Google Workspace / Microsoft Entra /
  Okta / Keycloak など）を接続し、ローカルアカウントに加えて組織の SSO でログインできます。設定 ▸ 認証
  でプロバイダ（issuer / クライアント ID / クライアントシークレット / リダイレクト URI / スコープ）を
  登録し、IdP のグループを Yagra のロールにマッピングします。アカウントは初回サインイン時に自動作成され、
  ロールは毎回のログインで IdP に追従します。クライアントシークレットは保存時に暗号化され再表示されません。
  プロバイダを有効化するとログイン画面に「SSO で続行」ボタンが表示されます。
- **コアの高可用性（オプトイン）** — 同一の PostgreSQL / Redis / NATS / VictoriaMetrics に対して複数の
  `yagra-core` を稼働させ、そのうち1台（PostgreSQL のアドバイザリロックで選出されたリーダー）だけが
  スケジューリング・ポーリング・アラートを担当します。残りは待機し、リーダー障害時は数秒で自動的に引き継ぎ
  ます（二重ポーリングや通知の重複はありません）。`YAGRA_ENABLE_HA` で有効化し、新しい `/readyz` レディネス
  プローブで稼働中のコアへ API/web を振り分けます。既定は OFF で、単一コア構成は従来と完全に同一です。

### パフォーマンス
- **ノード詳細を単一の共有タイマーで更新** — ノード詳細画面はこれまで1ノードにつき最大7本の独立した15秒
  更新ループを回していましたが、これを単一の共有 tick に統合しました。ブラウザタブが非表示の間はポーリングを
  停止し、復帰時に即座に更新します。

## v0.1.11

**モバイル WebUI の仕上げ（パリティ第3弾）** — 操作にマウスが必要だった残りの画面をタッチ対応にしました。
ダッシュボードのタイルは長押しで並べ替え、トポロジマップは2本指ピンチでズーム、レポートビルダーは電話幅に
収まるよう再配置します。

### 改善
- **ダッシュボード編集をタッチで操作** — カスタマイズ中、タイルの移動ハンドルを長押しすると並べ替えでき
  （通常のスワイプはボードのスクロールのまま）、編集コントロールのタップ領域も拡大しました。ボードが1列
  全幅に畳まれる電話では、縦積みレイアウトを変えられないリサイズグリップを非表示にしています。デスクトップ
  のマウス編集は従来どおりです。
- **トポロジマップを2本指ピンチでズーム** — 依存関係マップを2本指ピンチでズーム、2本指でパンできるように
  しました（従来のドラッグパン・ホイールズームも維持）。マップ上のズームコントロールもタッチ向けに拡大して
  います。
- **レポートビルダーが電話に収まる** — レポートのタブが潰れず横スクロールし、ビルダーのセクション設定は
  シート内で全幅に縦積みされ、タブとセクション並べ替えコントロールのタップ領域も拡大しました。

デスクトップの表示は変わりません。

## v0.1.10

**モバイル WebUI の仕上げ（パリティ第2弾）** — 電話レイアウトが未対応だった設定 / 管理画面を整えました。
詰まった行アクションは「⋯」メニューに集約し、ユーザー一覧はカード化、横に広いマトリクス / グリッド画面は
列が潰れずスクロールします。

### 改善
- **行アクションを電話では「⋯」メニューに集約** — ユーザー一覧と、複数アクションを持つ設定一覧（通知
  ルーティング、資格情報、監視プロファイル、イベントルール、イベントソース、分類ルール、メンテナンス枠）
  で、カードの編集 / 有効・無効 / 削除を単一のオーバーフローメニューにまとめ、カードが詰まらないように
  しました。デスクトップは従来どおりインラインのアクション行のままです。
- **電話で見やすいユーザー一覧** — 各アカウントを縦積みカードに再構成し、ロール選択とアクションを全幅の
  独立行に配置、ロール選択のタップ領域も拡大しました。
- **ロール / ディスカバリ画面が電話でスクロール** — ロール×権限マトリクスとディスカバリのインポート
  グリッドを、電話幅では先頭列を固定したまま横スクロールできるようにし、列が潰れて操作不能になるのを
  解消しました。
- **トラブルシュートを電話に最適化** — 分析の起動ドロワーを全幅にし、実行コントロールのタッチターゲット
  を小画面で拡大しました。

デスクトップの表示は変わりません。

## v0.1.9

**WebUI のスマートフォン対応、数万ノード規模へのスケール、そして一連のセキュリティ強化** — WebUI が
スマートフォン画面に対応し、インベントリ / トポロジ / ダッシュボードが大規模環境でも快適に動作し
（フリート全体をブラウザに読み込むのではなくサーバ側で検索）、セッション・ログイン・TLS の案内・
メトリクス取り込みが強化されました。

### 新機能
- **WebUI のスマートフォン / モバイル対応** — WebUI がスマートフォンサイズの画面に対応しました。ナビ
  ゲーションドロワーとボトムシートを備えたモバイルアプリシェル、カード形式のテーブル・フォーム・ノード
  詳細、小さな画面でもノード詳細に到達できるペイン切り替え、より見やすいイベント / インターフェース
  表示、モバイル / デスクトップレイアウトを強制する Preferences のトグルを追加しました。デスクトップの
  表示は変わりません。

### 改善
- **数万ノード規模へのスケール** — インベントリツリーを仮想化し、グループのメンバーを遅延読み込み
  （開いていて表示中のグループのみ取得）するようにし、トポロジをサーバ側でページングし、グループごと・
  フリート全体の状態集計をサーバ側で計算するようにしました。ライブなノード状態は、ページ全体の再取得
  ではなくストリームで差分的に届きます。大規模なインベントリでも快適に動作します。
- **インベントリ全体を読み込まないフリート全体検索** — ノード一覧のフィルタと Troubleshoot のスコープ
  ピッカーがサーバ側で検索（デバウンス・上限付き）するようになり、ノードピッカーも同じ入力補完を使う
  ため、ノードを探すのにブラウザがフリート全体を保持する必要がなくなりました。
- **フリート全体で生 ID ではなく人間可読な名前を表示** — 参照されているノード・グループ・プロファイルが、
  Dependencies・分類ルール・Mutes・Maintenance の各画面で名前に解決されるようになり（ホバーで ID を
  コピー可能）、名前解決が先頭ページだけでなくフリート全体をカバーするようになりました。
- **大規模環境でのメトリクス取り込み / ポーリングの高速化と安定化** — ポーリング結果をマッチャーと非同期
  バッチライターで処理し、SNMP テーブルウォークを 1 セッションにまとめ、依存関係の抑制の再スイープを
  差分化し、アラート設定とスイープ仕様の解決を設定世代ごとにキャッシュするようにしました。大規模フリート
  でのスループットが安定します。
- **ノード詳細のインターフェース一覧の高速化** — ノードのインターフェースごとのスループットと状態を、
  インターフェースごとに複数のクエリを投げるのではなく、ノード全体で一定数の時系列クエリで読み込むように
  しました。多ポートのスイッチでもインターフェース一覧が格段に速く開き、更新も軽くなります。
- **チャート / ヘルス画面の応答性向上** — インターフェース・混雑リンクのヒートマップ・ホストリソースの
  各チャートが独立した系列を並行取得し、URL 監視プローブがプールされた HTTP クライアントを再利用する
  ようになり、これらの画面のレイテンシが短縮されました。
- **エッジ取り込みの既定上限を引き上げ** — syslog / SNMP トラップの送信元ごと・全体のレート制限の既定値
  を引き上げ（毎秒 200 / 5000 メッセージ）、シャーシ規模のイベント量に対応できるようにしました。
- **レポートビューアと確認ダイアログの改善** — レポートビューアがアプリ標準のダイアログ（キーボード
  フォーカス処理、モバイルではボトムシート）になり、レポート・テンプレート・スケジュールの削除がブラウザ
  ネイティブのプロンプトではなくアプリのテーマ付き確認ダイアログを使うようになりました。
- **コンテナのヘルスチェック** — コアイメージが本物のヘルスチェックで稼働状態を報告するようになり、web
  コンテナはコアが応答可能になるまで起動を待つため、コールドスタート時の初回リクエストエラーを避けられ
  ます。

### バグ修正
- **余白と強調の一貫性** — 一部の WebUI スタイルが未定義の余白トークン（余白がつぶれて表示される）や
  スケール外のフォントウェイトを参照していました。これらを共通トークンに統一し、余白と強調が一貫した・
  テーマ対応の表示になるようにしました。
- **チャートの系列色がテーマに追従** — 時系列チャートの色をテーマの系列パレットから取得するようにし、
  ライト / ダークモードに追従し、ステータス（警告 / 重大）色を流用しないようになりました。

### セキュリティ
- **セッションを失効でき、有効期限が付くように** — ユーザーの無効化・降格・パスワードリセット・削除が、
  そのアカウントの有効なセッションを即座に無効化するようになり、セッションにアイドル期間と絶対的な有効
  期限が付きました。新しいログアウトエンドポイントは現在のトークンをサーバ側で失効します。従来はベアラー
  トークンがコア再起動まで有効で、侵害されたアカウントに対する管理操作が発行済みトークンを遮断できません
  でした。
- **ログインのブルートフォース対策** — ログインエンドポイントに、失敗の繰り返しに対するアカウントごとの
  指数的ロックアウトと、全体の試行レート上限を導入し、パスワード推測攻撃と、パスワードハッシュを繰り返し
  計算させる CPU 枯渇の経路を抑制します。
- **取り込み時のメトリクス名の検証** — ポーラーから届くメトリクス名を、時系列ストアに入る箇所で再検証する
  ようにし、不正なメトリクス名が余計な系列やラベルを注入できないようにしました（カーディナリティ / データ
  整合性の保護）。
- **WebUI / API の TLS 案内** — WebUI イメージとデプロイに、HTTPS を終端する方法（コンテナ内の TLS
  サーバブロックのオプトイン、または外部リバースプロキシ）を記載しました。ログインパスワード・ベアラー
  トークン・投入されたデバイス認証情報が、信頼できるネットワークの外に平文で露出しないようにするため
  です。既定は LAN / プロキシ配下向けの平文 HTTP のままで、これは案内と設定のみの変更です。
  > **置き換え済み。** ここで同梱したコメントアウト済みブロックは、コメントアウトされたままでした。
  > TLS は既定で有効になり、平文リスナは廃止されました — 本ファイル冒頭の破壊的変更を参照。

## v0.1.8

**検索可能なパッシブイベントログストア、Events とダッシュボードの強化、SSRF 対策、大規模環境の高速化** —
受信した syslog メッセージと SNMP トラップを検索可能なログストアに保存できるようになり、Events 画面に
フィルタが加わり、ダッシュボードウィジェットのリサイズと高さ変更に対応し、URL 監視プローブを SSRF に対して
強化し、ノード一覧とトポロジが大規模環境で高速に読み込まれるようになりました。

### 新機能
- **全文検索対応のパッシブイベントログストア** — 受信した syslog メッセージと SNMP トラップを、非同期の
  バッチ書き込みで専用のログストア（VictoriaLogs）に保存し、Events 画面から全文検索できるようになりました。
  パッシブイベントが一時的なものではなく、保持・検索可能になります。
- **Events 画面のフィルタ** — イベントを時間範囲・イベント種別・ルールに一致したか・全文 / 正規表現検索で
  絞り込めるようになり、多数のイベントの中から目的のものを見つけやすくなりました。
- **リサイズ・高さ変更が可能なダッシュボードウィジェット** — ダッシュボードウィジェットが、ウィジェット
  ごとの段階的な高さ（編集モードの高さセレクタ付き）と、スナップ整列・隙間のないグリッド上での角ドラッグ
  リサイズに対応しました。編集は Cancel で破棄でき、ウィジェットの内容はリサイズ後のセルいっぱいに広がります。

### 改善
- **大規模環境でのノード一覧 / トポロジの高速化** — ノード一覧とトポロジ画面で、アラートエンジンが未観測の
  ノードのフォールバック状態を、ノードごとに 1 クエリではなく単一の時系列クエリで導出するようになりました。
  大規模なインベントリや、コア再起動直後（エンジンがまだ各ノードの状態を把握していないとき）に特に効果が
  あります。
- **データベース接続プールの調整可能化** — コアの PostgreSQL 接続プールサイズを環境変数
  `YAGRA_PG_MAX_CONNECTIONS` で設定できるようになり、既定値を引き上げました。数万ノード規模でスケジューラ・
  結果取り込み・API を律速していた同時実行の上限が緩和されます。
- **ノード詳細の表示高速化** — ノードの SNMP スカラー値を逐次ではなく並行して読み込むようになり、スカラー
  メトリクスの多いデバイスで Overview タブの表示が速くなりました。

### バグ修正
- **ダッシュボードチャートのちらつき** — フィルモード（高さ追従）のダッシュボードチャートで発生していた
  ResizeObserver 起因のちらつきを修正しました。
- **UI テキストサイズの一貫性** — 残っていた WebUI のテキストサイズ（未定義のスタイルトークンを参照して
  意図しないサイズで表示されていたものを含む）を共通のサイズトークンに統一し、一貫した・テーマ対応の
  サイズで表示されるようにしました。

### セキュリティ
- **URL 監視の SSRF 対策強化** — HTTP(S) 監視プローブが、初回のターゲットだけでなく**すべてのリダイレクト
  ホップ**についても、SSRF フィルタ付きリゾルバ経由でのみ名前解決・接続するようになりました。これにより、
  ループバック / リンクローカル / クラウドメタデータのブロックリストを回避できた 2 つの経路を塞ぎます。
  監視対象のエンドポイントがプローブをブロック対象アドレスに解決されるホスト名へリダイレクトする経路と、
  事前チェックを通過しつつ実際にはブロック対象アドレスへ接続される DNS リバインディングです。プライベート /
  内部レンジは引き続き許可されます（NMS は正当にそれらを監視するため）。

## v0.1.7

**ホストリソース傾向、より広い多言語対応、大規模運用とアップグレードの快適化** — System Health で
コアと全ポーラの CPU / ロード / メモリ / ディスクを時系列表示できるようになり、WebUI の日本語対応が
さらに広がり、ポーリングスケジューラと停止処理を大規模運用とクリーンな再起動に向けて強化しました。

### 新機能
- **System Health のホストリソース傾向** — System Health ページで、**コアと各ポーラ**の **CPU・ロード・
  メモリ・ディスク**を時系列で表示できるようになりました。現在値のスナップショットだけでなく、リソース
  逼迫の推移を確認できます。

### 改善
- **英語 / 日本語ローカライズの拡充** — 残りの WebUI 領域（アプリの骨格＝トップバー・サイドバー・
  パンくず、アラートのトリアージ、共通のノードピッカーと時間範囲コントロール、および残りの画面）を
  翻訳しました。日本語に切り替えたときにカバーされる範囲が大きく広がります。未翻訳のテキストは英語に
  フォールバックします。
- **ナビゲーションの再編** — サイドバーを **Monitor**（監視）と **Configure**（設定）に分類してラベルを
  分かりやすくし、サードパーティの **Integrations** はカタログにまとめました。
- **グレースフルシャットダウン** — コアとポーラが `SIGTERM` / Ctrl-C（`docker compose` の再起動や
  ローリングアップグレードなど）でクリーンに停止し、処理中の作業と処理中の API リクエストをドレインして
  から終了するようになりました。
- **大規模環境でのスケジューリング / 取り込みの高速化** — ポーリングスケジューラが各プールのワーキング
  セットを並行して構築し、通知配信を結果取り込みパスから分離しました。通知先が遅くてもメトリクス取り込み
  が止まらなくなり、数万ノード規模でのスループットが向上します。

### バグ修正
- **UI のサイズ / スタイルの一貫性** — 存在しないスタイルトークンを参照して意図より少し大きく表示されて
  いた WebUI の一部のテキストサイズとモーダルのスタイルを修正しました。

## v0.1.6

**多言語 WebUI と SNMPv3 ノードのインタフェース単位メトリクス** — インターフェース言語を
Settings ▸ Preferences から英語と日本語で即座に切り替えられるようになり、SNMPv3 で監視する
デバイスでも v2c と同じインタフェース単位のメトリクスを収集できるようになりました。

### 新機能
- **インターフェース言語の切り替え（英語 / 日本語）** — 新しい **Settings ▸ Preferences ▸ Language**
  で UI 全体の言語をその場で切り替えられます（リロード不要）。既定は英語で、選択はブラウザごとに
  記憶されます。アプリの骨格（上部ナビゲーション、サイドバー、パンくず、サインイン、Preferences と
  About 画面）と共通の値フォーマット（日時、「5分前」などの相対時刻、ノード状態・HTTP ステータスの
  ラベル、証明書有効期限の表示）が翻訳済みで、日本語リソースは切り替え時のみ遅延読み込みされます
  （既定の英語パスに追加コストなし）。残りの画面は順次翻訳中で、未翻訳のテキストは英語にフォール
  バックするため空欄になりません。
- **SNMPv3 ノードのインタフェース単位メトリクス** — SNMPv3（USM）ノードでも v2c と同様に GETBULK で
  インタフェーステーブルをウォークし、インタフェース単位のカウンタを収集します。従来 v3 ノードは
  スカラーメトリクスに限られていました。

## v0.1.5

**分散トレーシング（OpenTelemetry）** — Yagra が OpenTelemetry トレースを送出できるようになり、
1 回のポーリングを中央 core とポーラをまたいでエンドツーエンドで追跡できます。オプトインで既定は
無効（構造化ログと Prometheus メトリクスは従来どおり）なので、コレクタに向けるまでオーバーヘッドは
ゼロです。

### 新機能
- **OpenTelemetry 分散トレーシング（自己監視）** — `YAGRA_OTEL_ENDPOINT`（または標準の
  `OTEL_EXPORTER_OTLP_ENDPOINT`）に OTLP/HTTP コレクタを設定すると、core とポーラが 1 回の
  ポーリングを 1 本のトレースに繋ぐ span（core の dispatch → ポーラの poll → core の ingest）と、
  北向き API リクエストごとの span を送出します。トレースコンテキストはジョブと結果に載ってバスを
  流れるため、1 本のトレースが分散ポーラをまたぎ、ローリングアップグレード中の古いポーラとも互換
  です。大規模時は毎回トレースする代わりに `OTEL_TRACES_SAMPLER=parentbased_traceidratio`
  （+ `OTEL_TRACES_SAMPLER_ARG`）でサンプリングします。`docker compose --profile tracing up` で
  同梱の Jaeger が起動し、ローカルでトレースを閲覧できます。エンドポイント未設定なら Yagra は従来
  どおりログのみで、トレーシングのオーバーヘッドはありません。**DEPLOYMENT.ja.md ▸ 分散トレーシング
  （OpenTelemetry）** を参照。

### セキュリティ
- **推移的アドバイザリの修正** — `crossbeam-epoch` を 0.9.20 に更新し RUSTSEC-2026-0204 を解消
  （メトリクス依存内で null ポインタを debug 整形した時のみ到達する無効ポインタ参照。Yagra では
  発生しませんが予防的に適用）。

## v0.1.4

**依存関係マネジメント** — アラート抑制を駆動する依存グラフを、閲覧するだけでなく編集できるように
なりました。ノードの上流を WebUI から設定でき、依存抑制が親ダウンのイベントに反応するようになった
ため、親より先に落ちた子は単独で通知される代わりに親の下へ巻き取られます。

### 新機能
- **ノードの依存関係を WebUI から管理** — ノードの上流（親ダウン抑制と根本原因ロールアップを駆動
  するエッジ）を、ノード作成後でも設定・変更・解除できるようになりました。ノード詳細ヘッダに
  **Dependency…** アクションが追加され、新しい **Topology ▸ Dependency view** ページで各ノードの
  上流・現在のステータス・根本原因の帰属を一覧し、各エッジをその場で編集できます。自己依存と循環は
  拒否されます。Network map は既にこれらのエッジを可視化していましたが、本リリースで定義できるように
  なりました。

### バグ修正
- **順序が前後した障害でも依存抑制が漏れない** — 根本原因の帰属がイベント駆動になりました。従来は子が
  最初にダウンした瞬間にのみ親への帰属を計算していたため、親より*先*に落ちた子は巻き取られず単独で
  通知され続けていました。本リリースでは、親がダウンすると既にアクティブな下流アラートを再評価して
  親のインシデントへ巻き取り（単独通知はクローズ）、逆に親が復旧しても子がまだダウンしている場合は
  その子を単独で再通知します。

## v0.1.3

**分散ポーラプール** — 監視対象の近くに配置したポーラへポーリングを分散できるようになりました。
各ノードは **pool** 属性（サイトやリージョン）を持ち、中央サーバがポーラの死活を追跡し、プール内の
ノードを生存ポーラへ一貫ハッシュで割り当て、各ポーラに担当分（working set）をバス経由で配布します。
リモートポーラは中央の NATS バスへアウトバウンド接続する（NAT／ファイアウォール越しに強い）ため、
拠点セグメントを inbound 開放なしで監視できます。本リリースには受動イベント監視、トポロジマップ、
ノード単位イベント表示も含まれます。

### 新機能
- **分散ポーラプール（ロケーション親和割り当て＋フェイルオーバー）** — リモート拠点にポーラを配置し、
  **pool** 属性でノードを割り当てられます（既定は単一プール `default`）。中央コーディネータがポーラの
  ハートビートを監視し、プールの担当を生存ポーラへ一貫ハッシュリングで分配し（ポーラの増減で入れ替えが
  最小になります）、離脱したポーラの担当を同一プールの生存ポーラへ自動的にフェイルオーバーします。
  ポーラは割り当てを working-set スナップショットとして受け取り、以降は差分を適用し、再接続時はフル
  再同期する（再起動をまたいでポーリングの欠落なし）方式です。新しい **Settings ▸ Pollers** ページで
  各ポーラの pool・ステータス・バージョン・working-set サイズ・最終ハートビートを一覧でき、ノードは
  あるが生存ポーラがいないプールを警告し、リモートポーラの設定を生成する **Register poller** ダイアログ
  を備えます。生存ポーラのいないプールは従来の中央ポーリングにフォールバックするため、アップグレードは
  シームレスです。リモート拠点向けの `docker-compose.poller.yml` と、信頼境界を越えてバスを公開する
  ための NATS TLS ＋認証設定を同梱します。
- **受動イベント監視（syslog / SNMP トラップ / Webhook）** — 能動ポーリングに加え、syslog（RFC
  5424/3164）と SNMP v1/v2c トラップを受信し、inbound Webhook を受け付け、運用者定義のルール（部分
  一致／正規表現）で照合してアラートを生成し、発生／解消のライフサイクルを PagerDuty（Events API v2）
  と Jira Service Management（Alerts API）へ転送できます。
- **トポロジ Network map** — 依存グラフの新しい可視化ビュー。ノード同士の関係と、親障害が子へどう
  波及するかを俯瞰できます。
- **ノード単位のイベント表示** — ノード詳細ページに **Events** タブが追加され、Events ページを単一
  ノードに絞り込めます（ノードピッカーと `?node_id=` ディープリンク付き）。イベントログの取得・
  ページング・カラムは両ビューで共有されます。
- **Alerts ▸ Events のフリーテキスト検索** — 任意の検索文字列でイベントログを絞り込めます。

## v0.1.2

**読み取り専用** の Dashboard API による Cisco Meraki 監視。Meraki 機器は SNMP/ICMP ホストと同じ
HostTree に通常のノードとして現れますが、収集は **組織（Organization）単位** で行われ（1 回の
ページング付き組織一括 API 呼び出しで多数の機器をカバー）、大規模な Meraki 環境でも組織の API
レート制限に触れません。さらに、この統合が Meraki に対してできるのは *読み取り* だけです。

### 新機能
- **Cisco Meraki（Dashboard API）監視** — **Settings ▸ Integrations ▸ Cisco Meraki** から読み取り
  専用 API キーで Meraki 組織を追加できます（1 つのキーで複数組織を一括登録可能）。インポート
  ウィザードが組織のネットワークと機器を列挙し、監視対象を選択できます。取り込んだ機器は通常の
  ノードとなり、**組織 → ネットワーク** のグループツリー下に自動配置され、インベントリツリー上で
  **Meraki** バッジが付きます。収集メトリクス（死活、WAN アップリンクの損失／遅延／ステータス、
  クライアント数、トラフィック使用量）は、ノード詳細ページの新しい Cisco Meraki カードに表示され
  ます。組織単位で、収集の一時停止／再開、ティア別のポーリング間隔とレート予算の調整、監視対象
  ネットワークの編集ができます。

### セキュリティ
- **設計上の読み取り専用と多層セーフガード** — この統合が Meraki に発行するのは HTTP **GET のみ**
  で（設定の書き込みは一切行いません）、すべてのリクエストは許可リストに登録された Meraki API
  ホストに限定され、API キーが他所へ送られることはありません。キーは **保存時に暗号化** され（API
  で返却されず、ログにも出力されません）、ポーリングは組織単位の API 上限より十分低いレートに抑えて
  利用者自身のツール用の余裕を残し、Integrations ページの **Meraki ポーリング全体キルスイッチ** で
  すべての Meraki 収集を即座に停止できます。

## v0.1.1

初回リリースに続く小規模アップデート: 専用の About ページ、Pollers から分離した自己健全性ページ、
そして完全に非 root で動作する WebUI コンテナ。

### 新機能
- **Settings ▸ About** — 製品情報と稼働中のバージョンを 1 か所にまとめた新しいページ。稼働中の
  Core / API バージョン（新しい公開エンドポイント `GET /api/v1/version` 経由）、WebUI ビルド
  バージョン、リポジトリリンク、ライセンスを表示します。両方のバージョンを並べて表示することで、
  ローリングアップグレード中の core/web のずれが一目で分かります。

### 改善
- **System Health が独立したページに** — Yagra の自己健全性（ポーリングループのカウンタ、バックエンド
  サービスの到達性、データ網羅率）を Settings ▸ Pollers から専用の Settings ▸ System Health ページに
  移動しました。Pollers の項目は、将来の分散ポーラ設定用のプレースホルダとして予約されています。

### セキュリティ
- **WebUI コンテナが完全に非 root で動作** — web イメージを `nginx-unprivileged` ベースにし、非 root
  ユーザーで動作するようにしました。そのためサーバは 80 ではなくコンテナポート **8080** で待ち受け
  ます。同梱の Docker Compose ファイルはこれに合わせて設定済みのため、標準的なデプロイには影響しません
  （WebUI は既定でホストポート 3000 のまま）。web コンテナのポート 80 を参照していた独自のリバース
  プロキシやオーケストレーションは更新してください。

## v0.1.0

Yagra の初回リリース — ネットワークデバイスやサーバを ICMP / SNMP / API コールで監視する
ネットワーク監視システム（NMS）です。数万ノード規模と分散ポーリングを最初から見据えて設計され、
Rust バックエンドと React WebUI を Docker コンテナとして提供します。

### 新機能
- **監視・ポーリング** — ICMP 死活、SNMP v2c / v3（USM 認証・暗号）のスカラ／テーブル収集
  （マルチインデックステーブルを含む）、TLS 証明書の有効期限追跡付き URL / HTTP(S) エンドポイント
  監視。生カウンタを保存し、レートはクエリ時に算出（ラップ／リセット対応）。グローバルおよび
  Device Profile 単位のポーリング間隔を設定でき、間隔のジッタとデバイス単位のレート制限／
  バックプレッシャを備えます。
- **探索・分類** — IP レンジ／リストに対するネットワーク探索（保存済み資格情報のピッカー付き）、
  sysObjectID / sysDescr による自動デバイス分類で該当する Device Profile を適用、保存済み資格情報を
  参照で試行する Credential Finder（デバイス単位でレート制限、試行値はログに残さない）。
- **デバイスプロファイル・収集** — ロール × ネットワーク OS のプロファイル分類体系、編集可能な
  Collection Templates・Classification Rules・厳選した MIB/OID カタログ（いずれも検索可能）。主要
  ベンダー（Cisco、Huawei — USG ファイアウォールセッションや RAM を含む、Meraki MX/MS、A10）と
  標準のホスト／インタフェース MIB を標準収録。
- **アラート** — 明示的な状態機械に、ドウェル時間ヒステリシス、フラッピング検出、ルート原因への
  ロールアップを伴う依存抑制（親ダウン ⇒ 子を抑制）、メンテナンスウィンドウ、ミュート、
  dedup・グルーピングを備えます。追記専用のアラート履歴はノード／チェック名を人間可読で表示し、
  「What」（メトリクス＋条件）列を持ち、PagerDuty / Jira Service Management など外部ツールからの
  確認応答を読み取り専用で反映します（ADR-015）。メール・Webhook 通知チャネルはリトライ付き。
- **ダッシュボード・可視化** — 管理者がカスタマイズできる Shared Dashboard と、ユーザーごとの複数
  ボード対応 My Dashboard を、ウィジェットライブラリから構築: フリート健全性タイムライン、Top-N
  CPU／メモリ／インタフェース、トラフィック差分、合計スループット、使用率ヒートマップ、地理マップ、
  依存／ルート原因トポロジ、データ網羅率ゲージ、アラート履歴集計、ポーラ／収集の健全性。分割表示の
  ノード詳細を持つノードツリー、インタフェース行内のスパークライン、共有レンジコントロール
  （1h–7d ＋カスタム）と設定帯域の基準線付き時系列グラフ。
- **レポート** — カスタマイズ可能なレポートセクションを HTML / CSV / PDF で出力し、定義の保存と
  スケジュール実行に対応（Dashboard ▸ Reports）。
- **トラブルシュート** — VictoriaMetrics 上で非同期の分析ジョブ（例: 異常検知）を実行する分析
  ツールカタログ。単一ノードまたはグループ（再帰的）にスコープできます。
- **管理** — ロールベースアクセス制御（viewer / operator / admin）とグループスコープの可視性、
  ユーザー管理、すべての設定変更とログインを記録する不変の監査ログ、Yagra 自己健全性監視
  （Settings ▸ Pollers と `GET /api/v1/system-health`）。
- **WebUI** — 単一の型付き REST クライアント上の React + TypeScript SPA。SSE によるライブ更新
  （ベアラー認証）、ライト／ダークテーマ、数万ノードを見据えた仮想化・サーバページングのリスト。
- **デプロイ** — 単一ノードの Docker Compose スタック（Yagra-core / Yagra-poller / Yagra-web /
  PostgreSQL / Redis / NATS / VictoriaMetrics）。非 root イメージで、raw-socket ICMP 用の
  `CAP_NET_RAW` はポーラのみに付与。expand-contract のデータベースマイグレーションでアップグレード時の
  データを保全します。

### セキュリティ
- 監視用の資格情報（SNMP コミュニティ、SNMPv3 USM 資格情報、API トークン）はエンベロープ暗号で
  保存時に暗号化します — AES-256-GCM のデータ鍵を、マウントしたシークレットファイルから読み込む
  鍵暗号化鍵（KEK）でラップします。シークレットはログ出力・API 応答・メトリクス／トレースラベルに
  決して含めません。
- 状態を変更するすべての API エンドポイントは認証と RBAC を強制し、監査ログに記録します。
- URL モニターと通知 Webhook からの外向き HTTP は SSRF を防止します: ループバック、リンクローカル、
  クラウドメタデータ宛先を拒否します（IPv4 マップ IPv6 形式や HTTP リダイレクト経由を含む）。一方で、
  正当なプライベート／内部レンジは監視可能なままです。
- URL モニターの TLS 証明書検証は既定で有効で、無効化はモニター単位の明示的なオプトインです。
